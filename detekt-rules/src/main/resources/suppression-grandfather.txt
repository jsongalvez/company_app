# Frozen #465 grandfather of pre-existing @Suppress sites (path-suffix:suppressed-id).
# Generated once; never grows. New suppressions need a #<ticket> comment instead.
# #600 burn-down complete (8/8): all entries resolved — fixed, ticketed, or confirmed orphan.
