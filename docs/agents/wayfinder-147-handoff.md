# Handoff — Wayfinder Map #89 (Frontend Rebuild), Session 45

## What this is

A wayfinder session on map **#89** ("Frontend rebuild — from scratch to fully-integrated UI"). Session 45 ran the **#97-grad Dashboard build**: **#147 ("Build — Session Dashboard (graduated from #97)")** — `wayfinder:task` — was grilling-graduated (the clock-out decision), created, wired as a child of #89, claimed, run through the phased code-review loop (**5 passes, 7 HARD-class findings, all fixed**), resolved, and closed. Resolution comment: https://github.com/jsongalvez/company_app/issues/147#issuecomment-5257541872. 6 fix commits `66cde9f`→`f622807` on `ralph/company-app-full-build` (unpushed; each pre-commit gate passed: ktlint/detekt/786 backend tests/cleanliness/shared-compile/Postgres). Map #89 updated (Decisions-so-far #147 entry; the #109 clock-out fog line rewritten as half-shipped — logout still future; frontier paragraph rewritten with the #147 outcome + the #97-grad fog line RESOLVED). The loop register gained a new class (**fix-that-didn't-land**) and the layout-starvation occurrence bump.

**Next-session state:** **0 unblocked tickets** — unchanged. The frontend build chain advances: the next chartable work is the deferred inline-edit ticket (from #147), the HITL gates (#117, the new notifications→SessionDetail path), or another fog graduation.

## Session outcome

**#147 (Build — Session Dashboard) — grilling-graduated + created + resolved + closed (HITL grilling → AFK build).**

- **The grilling (frontier round 1, 6 questions; Q3 re-pitched + re-grilled after a "what is a cap" /wait-what and a "why (i), is there no better way" challenge)**: Q1 drawer-footer clock-out (not BranchSelect); Q2 confirm dialog (opposite of my recommendation — user chose confirm); Q3 **clear-to-empty** — the user's pushback on re-fetching caps produced a BETTER answer: caps → empty on clock-out, NO re-fetch (zero caps consumers pre-clock-in — verified by grep; next clock-in refreshes wholesale; fail-closed); Q4 combined ticket (backend + frontend in one session, the #140 precedent); Q5 inline editing deferred to its own ticket (the #142-class state machine stays out); Q6 one enriched dashboard response (rejected the alternatives on evidence: relaxing the splits gate = the #131 leak class; a separate endpoint = double polling surfaces for data that moves together).
- **Backend shipped**: `GET /api/branches/{branchId}/dashboard/today` — enriched session rows (clientName, practitioner names, voided-ness via `active_session_voids`, structured concerns) + the caller's LIVE commission (replicates `recalculate` eligibility — clocked-in-at-sale-time + manual inclusions; NOT the split rows; AGENTS.md Sessions section documents it). Gate = the caller's active clock-in at the branch today (universal post-clock-in per #92; cross-branch 403; 404-before-gate). 14 tests (9 service + 5 route authz incl. the real-JWT 401 via the #128 harness precedent). k6: setup now creates a branch + clocks in; 3-run p95 2.73/2.76/2.47 ms → threshold 200 ms; `baseline-results.md` updated.
- **Frontend shipped**: `SessionDashboardViewModel` (30s completion-then-wait join-based poll; stale 2 / error 5 counted from BOTH failure hooks — HTTP via `onNonSuccess`, transport via the new additive `ApiCallHandler.onError`; 401 swallowed + paused; 403 → forbidden card + poll stop + retry; keep-last-results; DisposableEffect pause/resume — mobile push pauses, desktop pane never leaves); commonMain screen (Q2 cards + hairline + divider, Q3 voided rendering, Q6 states incl. ₱0 empty + branch context); `SessionList` expect/actual (desktop table + selection + Refresh row; mobile cards + manual-only PullToRefreshBox); desktop master-detail 0.6/0.4 (#91); mobile `Route.SessionDetail` carries the enriched row via nav args (notifications path renders a limited state — its own fog); drawer clock-out footer + confirm dialog (live-read double-tap guard, stale-error reset, inline error); `SessionState` clock-state slots (attendanceId/branchDayId written at clock-in) + `clearClockState`; `AttendanceViewModel.clockOut` returns Job + sync Loading. Desktop suite 141→**214** (+73).
- **Phased loop: 5 passes — 7 HARD-class findings, all fixed in-ticket; exit at pass 5 with 0 HARD.** Pass 1 (5 HARD): exception-path failures bypassed the stale/error counters (dead network = silently frozen data — the Q5b breach; fixed via the onError hook); Q2 hairline border missing (restored + divider); desktop selection dead (`selectedSessionId` hardcoded null — P1+P3+P4 independently); PullToRefresh fired every 30s poll (Q5 "silent" breach); retryAfterForbidden double-launch (Loading pre-set inside the coroutine — the guard saw Idle). Pass 2 (2 HARD): the summary divider's `fillMaxHeight` starved the list to zero height (**the #144 layout-starvation class, 2nd occurrence** — P3+P4 independently); the accepted-401-test SOFT re-rated HARD (the in-repo `ReportsReadScopeAuthzTest.createAppWithJwt` precedent proved my "harness precedent" was precedent of omission). Pass 3 (2 HARD): the JWT harness diverged from its own cited precedent (`"/api/*"` literal vs `${ApiRoutes.API_PREFIX}*` — silent-miss drift); **my pass-3 confirm guard read the `collectAsState()` snapshot, not the VM's live StateFlow — it no-oped in exactly the same-frame window it claimed to close (weaker than its own #140 precedent)**. Pass 4 (1 HARD): **the pass-4 filter fix NEVER LANDED** (python string mismatch — imports added, body unchanged; two dead imports; ktlint green only by UP-TO-DATE caching) — the fix-that-didn't-land class, NEW in the register. Pass 5: 0 HARD — verified by byte-parity + compilation, not inspection.
- **Accepted SOFTs (8, reasons in the resolution)**: empty-state create-session CTA absent (spec-conformant — the target UX is the SessionCreate fog; a dead button is worse); route path literals (tracked map fog — a single constant would be inconsistent); Ktor-retry-coupled test hit-asserts (deterministic under virtual time, documented); 401-with-lastData stale frame until the global redirect (transient); isManualRefreshing latch during an in-flight poll (truthful — a fetch is running); desktop empty/ERRORED lacks a refresh button (auto-poll + Retry cover); post-Success confirm-tap one-frame window (cosmetic — nav fires same frame); JWT-harness duplication of the precedent (repo pattern).
- **Pre-existing observations (out of delta, recorded)**: `:composeApp:testDebugUnitTest` has a cross-class teardown flake (SessionBootstrapViewModelTest's `resetMain()`-then-`clear()` dispatches to a dead Main — **reproduces on clean HEAD** (stash-verified): 194 tests, 1 failure; android unit tests are in NO gate); `ReliefAccessRepository.hasActiveClockIn` duplicates `AttendanceRepository`'s.
- **Red-first/verification notes**: two REAL VM bugs were caught by my own tests before any phase (the StateFlow-conflation failure-counting trap — StateFlow dedupes equal Error values, so a collector-based counter never advances — probe-proven; and the 401/403 state-wedge — onNonSuccess swallowing the state write left Loading forever, killing every future poll). The Ktor HttpRequestRetry plugin retries 5xx (4 handler hits per poll) and the Auth plugin re-sends 401s once (2 hits per poll) — both probe-proven, tests calibrated around them.

## Patterns + learnings (cumulative across sessions)

- **Session-45 additions**:
  - **The "fix-that-didn't-land" class**: a claimed fix whose body replacement silently missed (imports added, filter unchanged). Caught by phases READING the file, never the commit message. Now a register class — when a fix commit is claimed, phases verify the body, not the imports.
  - **The layout-starvation class recurred**: the divider's `fillMaxHeight` inside a wrap-height Row starved the list below (the #144 shape). The `Row(Modifier.height(IntrinsicSize.Min))` + `fillMaxHeight`-on-children pattern is the canonical fix; register bumped to 2.
  - **A guard is only as good as its read**: the `collectAsState()` snapshot is stale within the same frame — caller-frame guards must read the VM's StateFlow `.value` live (the #140 clockIn precedent). My first "fix" was weaker than its own precedent; pass 3 caught it.
  - **Probe-proven Ktor realities for VM tests**: HttpRequestRetry retries thrown exceptions + 5xx (4 hits/poll); the Auth plugin re-sends 401s once (2 hits/poll). Test cadence math must account for both, or threshold tests silently mis-assert.
  - **StateFlow conflation kills collector-based failure counting**: consecutive identical Error values never re-emit — count in the fetch hooks (onNonSuccess/onError), never in a `_dashboardState.collect`.
  - **Accepted-SOFT re-rating works**: the 401-test SOFT became HARD in pass 2 when the phase found the in-repo JWT-harness precedent ("precedent of omission, not impossibility") — the re-examine-and-re-rate-upward hand-off mechanism did its job.
- Prior-session patterns unchanged: AFK flow = quality gate, phased loop per `docs/agents/code-review-loop.md`, resolution comment → close → map Decisions-so-far + frontier paragraph + handoff. k6 landed for the new endpoint (the #140 precedent — backend endpoint + frontend consumer → k6 lands, threshold tightened from a 3-run baseline).
- **iOS compile is pre-existing-broken** (from #120 handoff): `:composeApp:compileKotlinIosSimulatorArm64` fails on HEAD (6 expect-without-actual incl. `AuditLogEntryList` — all pre-date this session; pre-push gate excludes iOS intentionally). Note: the new `SessionList` expect has no iOS actual — consistent with the 5 pre-existing split lists.
- **`testDebugUnitTest` has a pre-existing 1-failure flake** (see above) — not gated, not mine, recorded.
- **Flyway migration numbering**: V1–V18 taken; next is **V19** (`ls backend/src/main/resources/db/migration/` before picking). No migration in this session.
- **Test-DB pollution on partial `--tests` runs**: `bash scripts/clean-test-db.sh` before AND after partial backend runs (composeApp desktop tests don't touch Postgres). The k6 setup now CREATES a branch + CLOCKS IN on the test DB — clean-test-db truncation also kills the seeded owner mid-backend-run (restart the backend after cleaning before re-running k6).

## Current frontier (verified live post-session)

Per `gh issue list --state open`: only #89 (map), #110 (standalone hardcoded-month test fix, NOT a child), #139 (standalone OpenAPI map) open. **0 open children of #89** — #147 created+closed this session. The #97-grad fog line is RESOLVED; the map's standing fog lines are unchanged except the #109 clock-out line (half-shipped — logout still future).

## Recommended next picks

- **Inline editing for the dashboard (deferred from #147)** — the #97 Q4 locked model (type/status/finalPrice, pessimistic, per-control, mid-edit polling ownership) needs 2 new backend PATCH endpoints (type, finalPrice) + the per-field edit state machine (the #142-class reservoir). The most concrete AFK-able next build; the Q4/Q5 locked decisions carry over verbatim.
- **Merged Finance & Reports build** (from #101 + #105) — still gated on the #117 expense-GET-includes-soft-deleted question (HITL UX decision; ungraduated).
- **The notifications→SessionDetail data path** — the #147 SessionDetail renders a limited state when navigated from Notifications (row=null). Needs its own grilling: a session-detail GET endpoint + its read gate (attendance-scoped like the dashboard? capability-scoped? the #138 read-gate rule) — a genuine HITL decision, then a small backend + screen build.
- **Remaining Not-yet-specified candidates** — unchanged from the #145 handoff (user-create flow, non-admin self-slot-edit, F7, desktop token-storage, pushed-route topbar, audit branch-name, detekt gate strategy, #110, route-path constants, Clients search-field pop-back reset, keep-last-results port, hardcoded-month test dates).

## How to drive the next session (wayfinder "Work through the map")

1. Load the map (https://github.com/jsongalvez/company_app/issues/89), the wayfinder skill, tracker conventions (`docs/agents/issue-tracker.md` → "Wayfinding operations").
2. Session-46 has NO unblocked ticket — pick from "Recommended next picks". The dashboard inline-edit build is the most concrete next move (the Q4/Q5 locked decisions + the #147 backend patterns carry over). If the user prefers HITL, the #117 or the notifications→SessionDetail grilling opens its gate.
3. Claim BEFORE work: `gh issue edit <n> --add-assignee @me` — verify no concurrent sessions.
4. AFK flow: red-first falsification, /implement per module AGENTS.md, the phased loop with `git diff <last-pass-commit>` per pass, batch-fix commits, exit at one full pass with zero HARD. The register now has SIX classes — check the fix-that-didn't-land class on every fix commit (read the file, not the diff stat). For the inline-edit build: the #142 phase templates in `docs/agents/code-review-loop.md` are the closest surface precedent.
5. Post the answer as a **resolution comment**, then `gh issue close <n>`, then append a context pointer to map #89's Decisions-so-far (fetch body → modify → `gh issue edit 89 --body-file <modified>` — update the frontier paragraph AND clear graduated fog lines).
6. Graduate fog (create-then-wire): `gh issue create --label wayfinder:task` → child via `gh issue edit <n> --parent 89` (verify via the child's `parent` field — `gh issue view <n> --json parent`).
7. **One-ticket-per-session limit.** When done, stop and write `docs/agents/wayfinder-<N>-handoff.md` — follow this session's format.

## Map state at session-end

Map #89 body updated this session:
- Decisions-so-far: new #147 entry (full shipped-scope summary + the 5-pass/7-HARD loop + the register-class notes + the #97-grad RESOLVED statement + resolution link), inserted at the end of the section.
- The #109 fog line rewritten: clock-out's paired `NotificationState.clear()` is SHIPPED; a future logout button must do the same alongside the full clear.
- Frontier paragraph: rewritten — #147 outcome first (5 passes, 7 HARD-class, fix-that-didn't-land + layout-starvation register notes), **#97-grad Dashboard build COMPLETE** statement, 0 unblocked held, what remains chartable listed (inline-edit ticket, HITL gates).
- Not-yet-specified: the #147-deferred fog lines added (inline editing; the notifications→SessionDetail data path).

## Suggested skills for next session

- **`/wayfinder`** — the parent workflow; re-load to follow "Work through the map" steps (note: next session must pick/graduate a ticket — 0 unblocked).
- **`/grilling` + `/domain-modeling`** — for the notifications→SessionDetail gate decision (HITL) or the #117 gate.
- **`/implement`** — for the inline-edit build (the #142 per-field state-machine shapes + the #147 backend/VM patterns carry over).
- **`docs/agents/code-review-loop.md`** — the register now carries six classes; the #147 resolution is the reference for a 5-pass frontend build convergence + the fix-that-didn't-land verification discipline.
- **`/handoff`** — when the chosen ticket is resolved and the session is near its limit, compact + write `docs/agents/wayfinder-<N>-handoff.md`.
