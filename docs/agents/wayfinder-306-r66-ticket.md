## Question

Build: make shared relief-access status the sole Kotlin owner. Replace backend `ReliefStatus` with shared `ReliefAccessStatus`, preserve PostgreSQL and wire values, and verify relief behavior.
