# Handoff — Wayfinder Map #89 (Frontend Rebuild), Session 41

## What this is

A wayfinder session on map **#89** ("Frontend rebuild — from scratch to fully-integrated UI"). Session 41 continued the Tier-1 audit stream: the user picked **User Management** (the handoff's recommendation) as the third audit surface. The audit ticket **#143 ("Audit — User Management screens (phased loop)")** — `wayfinder:task`, AFK — was created, wired as a sub-issue of #89, claimed, run through the phased code-review loop (**5 passes, 5 HARD findings, all fixed**), resolved, and closed. Resolution comment: https://github.com/jsongalvez/company_app/issues/143#issuecomment-5249317726. 5 fix commits `5af39c2`→`561590b` on `ralph/company-app-full-build` (unpushed; each pre-commit-gate passed: ktlint/detekt/backend-tests/cleanliness/shared-compile/Postgres). Map #89 updated (Decisions-so-far #143 entry + the misplaced #142 entry moved out of Not-yet-specified into Decisions-so-far + frontier paragraph rewrite + new keep-last-port fog line).

**Next-session state:** **0 unblocked tickets** — unchanged. The audit stream's remaining Tier 1 surfaces (**Audit Log, backend grants #132–#134, day-gates #136–#138**) are listed in the map's frontier paragraph, pending the user's go-ahead — each graduates as `wayfinder:task` "Audit — <surface> (phased loop)" children of #89.

## Session outcome

**#143 (Audit — User Management screens (phased loop)) — created + resolved + closed (AFK).**

- **Created the ticket** (surface picked by the user: User Management — the handoff's lean), wired sub-issue of #89, claimed, then the usual AFK flow. Spec = the ORIGINAL tickets' resolutions (#106 D2–D5 locked outline + #135 build decisions 1–6 + round-1/round-2 outcomes).
- **The audit found a shallow reservoir — the shallowest Tier-1 surface yet**: 5 HARD findings over 5 passes, fixed in 5 batch commits (vs 13/9 in #141, 18/21 in #142 — consistent with #135 being the freshest two-round-reviewed build at audit time; the surface was built against an already-hardened backend).
  - **The reload-vs-mutation race was the deep well (2 of 5 HARDs, one class iterated)**: (a) pass-1 — the Refresh button wasn't gated on in-flight mutations; a reload landing mid-swap let `swapSlotsInPlace` re-apply on the fresh list (permanent local/backend desync, no error — the button gate can't cover non-click triggers); (b) pass-2 — `LaunchedEffect(Unit)` refires on rotation/re-entry bypassed the button gate entirely, same double-apply window. **Converged via the VM-level guard**: `loadUsers()` early-returns while `_inFlight` is non-empty (the locally-mutated list IS the authority mid-mutation — in-flight mutations can only exist after a Success load); pass-3 refined it to sit ABOVE the `actionErrors` clear (a skipped load leaves the list untouched, so its errors still describe current state). Test-pinned with a request-count harness counter.
  - **Invisible error surface (1)**: card-triggered slot-edit failures (desktop Edit / android tap-to-edit — mobile's only slot-edit path) keyed errors to the user row, which renders errors only when EXPANDED — a collapsed row got zero failure feedback (D4's "403/404/409 → inline error" broken). Fix: the slot card claims swap + selected-branch slot errors; the row filter excludes those keys (no double-render); other-branch + no-branch-selected slot errors stay on the row.
  - **Stale-error lifetime (1)**: `actionErrors` cleared only on same-key retry → "Deactivate failed: 500" persisted beside fresh post-Refresh data; swap errors reappeared on branch reselect. Fix: `loadUsers` clears the error surface when it actually replaces the list.
  - **Slot-dialog message-truth (1, regressed twice inline)**: the overflow classification used `toIntOrNull` → "32768" got "Slot must be 1 or greater" (DTO slot is Short/SMALLINT); pass-2's `toLongOrNull` fix re-broke for > Long.MAX inputs. **Converged the #142 way: extracted as pure tested `slotInputError`** with digit-ness classification (all-digit strings failing Long parse are certainly > 32767).
  - **Rejected HARDs (false premises, both test-proven)**: pass-3's "dialog dispatch while _users is Loading" — AlertDialog is modal (Refresh/rows unreachable while a dialog is open) and rotation resets `remember` (dialog closes first); pass-4's "Unicode-digit mislabel" — Kotlin Short/Long parsing is digit-aware, `toShortOrNull("٥")` = 5, the input is VALID (pinned in tests; the ASCII-only assumption was wrong).
- **Shipped**: the VM-level loadUsers guard (belt: Refresh button `enabled = !mutationsDisabled`); swap in-flight key normalizes pair order (same-frame reversed double-tap → one request, tested); `onEditSlot` passes the tapped `UserSlotRow` (`slotEditTargetFor` deleted — no id lookup, no silent no-op); desktop row-level dimming matches android (was name-text-only); StatusBadge renders unknown statuses raw (was `else → INACTIVE`) + maxLines=1; search field disabled during users Error; dialog overflow message truth; the guard-above-clear refinement.
- **Tests**: `UserManagementViewModelTest` 20 → **25** (+5: stale-error clear on reload; in-flight loadUsers skip with request-count pin; skipped-load error preservation — the guard-ordering contract; reversed-pair single-request dedupe; `slotInputError` class matrix incl. both overflow windows + Unicode-digit validity pins). Desktop suite **174 green**, 0 failures. Backend untouched (the #132–#134 integration checks held — no client/server contract drift on this surface).
- **Accepted residuals (documented in code)**: throwaway `mutation` flow via `launch` rather than `launchUnit` (the handler's state param; success hook needed — contract drift noted, load-bearing; a `launchUnit` `onSuccess` hook + AGENTS.md update is a candidate when the handler next changes); collapse-mid-flight hides row errors (active-hide; re-shows on expand; state preserved); non-selected-branch swap errors render nowhere until reselect (scoped keys — trigger surface gone; reload clears); dialogs not gated on `mutationsDisabled` (unreachable: modal + gated openers); client-fabricated `deactivatedAt` on the bodyless 204 (decision 5); `onSwap` unused on android (expect/actual symmetry); BranchPicker switch mid-swap (error preserved for its branch); `parseSlotInput(input)!!` double-parse invariant (KDoc-documented + test-pinned).
- **Finding rate (the calibration)**: 5 HARDs over 5 passes on ~5 files; 3 of 5 were the reload/mutation + error-lifetime interaction class; 1 was an invisible-feedback gap the two-round #135 review missed; 1 was a message-truth regression needing pure-function extraction. **The audit premise holds a third time — remaining Tier 1 surfaces still worth running; User Management needs no follow-up.**
- **App-wide follow-up surfaced (not this ticket's scope)**: keep-last-results still exists only in Notifications + Clients — **User Management's list reloads + flashes the spinner on every push/re-entry** (Audit Log, Remittance same) — keep-last port for the remaining screens remains open; the #143 in-flight loadUsers guard is the reload-race shape such a port must keep. The Clients search-field pop-back reset (D9 deviation) remains a candidate small fix.

## Patterns + learnings (cumulative across sessions)

- **Session-41 additions**:
  - **The convergence curve: 3 → 2 → 1 → 0 → 0.** One HARD (the reload-vs-mutation race) needed TWO iterations to converge — the first fix (button gate) was structurally incomplete because it reasoned about *click triggers only*; the converged form (VM-level guard) reasons about *the invariant* (no load while the list is locally authoritative). The test that pins it asserts the invariant's observable (request count), not the fix's mechanism.
  - **Guard ordering is a contract**: the pass-3 refinement (guard above the error-clear) was a genuine behavioral change with a dedicated test — small reorderings of state operations carry semantics.
  - **False-premise rejections are evidence the loop works**: two phase-flagged HARDs were disproven with tests (modal-dialog input blocking; Unicode-aware parsing). Both rejections needed EMPIRICAL checks (a test run) rather than code reading — when a phase finding rests on a platform/library behavior claim, verify it by running the claim before fixing.
  - **The pure-function extraction lesson held a second time**: the message-truth decision regressed twice inline (toIntOrNull → toLongOrNull) and converged only as `slotInputError` + tests. Same class as #142's gate decisions.
  - **A fresh-two-round-reviewed build audits shallower** (5 HARDs vs 13/18): the reservoir scales with how recently the surface was reviewed AND how much interaction machinery it has (Clients' edit state machine > Notifications' poll chain > User Management's simple mutations).
- Prior-session patterns unchanged: AFK flow = quality gate, phased loop per `docs/agents/code-review-loop.md`, resolution comment → close → map Decisions-so-far + frontier paragraph + handoff. k6 N/A (no endpoint changes). Red-first falsification N/A (audit, not build).
- **iOS compile is pre-existing-broken** (from #120 handoff): `:composeApp:compileKotlinIosSimulatorArm64` fails on HEAD (6 expect-without-actual incl. `UserSlotOrderList` — 5 of 6 pre-date #143) — pre-push gate excludes iOS intentionally.
- **Flyway migration numbering**: V1–V18 taken; next is **V19** (`ls backend/src/main/resources/db/migration/` before picking).
- **Test-DB pollution on partial `--tests` runs**: `bash scripts/clean-test-db.sh` before AND after partial backend runs (composeApp desktop tests don't touch Postgres).
- **Map-body hygiene**: session 40's #142 entry sat in "## Not yet specified" (a closed ticket — misplaced); moved into Decisions-so-far this session.

## Current frontier (verified live post-session)

Per `gh issue list --state open`: only #89 (map), #110 (standalone hardcoded-month test fix, NOT a child), #139 (standalone OpenAPI map) open. **0 open children of #89** — #143 created+closed this session.

## Recommended next picks

- **The rest of Tier 1 (the audit stream)** — the user's go-ahead is pending; the frontier paragraph lists the surfaces: **Audit Log, backend grants #132–#134, day-gates #136–#138**. **Recommendation: Audit Log next** — it's the remaining screen audit (spec = #104/#121–#124 resolutions; it has a paged browse + cursor + reload surface, the keep-last-inapplicable shape), and it was NOT built by this session's most-recent code generation era... actually the freshest-since-audit argument now favors the **backend grants #132–#134** (never loop-reviewed as a surface; the #142/#143 audits both leaned on their behavior as constraint sources — an audit would check the other direction). Either is defensible; surface-pick is the user's call as before. For each: graduate as `wayfinder:task` "Audit — <surface> (phased loop)" children of #89; P1 spec = the ORIGINAL tickets' resolutions; run the loop; fix HARDs in-ticket; record per-surface verdict + finding rate.
- **#97-grad Build — Dashboard** (prototype exists: `prototype/0097-session-dashboard`) — the natural next frontend build; the clock-out → dashboard-state-clear transition fog (#97 outline) is a decision inside it. The #141/#142/#143 audits hardened the notification chain, client edit surface, AND the reload-race guard shape this build's session-end paths touch — read `NotificationBadgeHost` + the App.kt clear paths + `ClientState` + the #143 loadUsers guard as constraint sources.
- **Merged Finance & Reports build** (from #101 + #105) — still gated on the #117 expense-GET-includes-soft-deleted question (HITL UX decision).
- **Relief-request dialog on BranchSelect** — #91's one-liner, needs its own grilling.
- **Remaining Not-yet-specified candidates** — unchanged from the #142 handoff (user-create flow, non-admin self-slot-edit, F7, desktop token-storage, pushed-route topbar, audit branch-name, detekt gate strategy, #110, route-path constants, Clients search-field pop-back reset) + **new this session**: keep-last-results port for User Management/Audit Log/Remittance (VM-held list + no-refire gating, keeping the #143 reload-race guard shape).

## How to drive the next session (wayfinder "Work through the map")

1. Load the map (https://github.com/jsongalvez/company_app/issues/89), the wayfinder skill, tracker conventions (`docs/agents/issue-tracker.md` → "Wayfinding operations").
2. Session-42 has NO unblocked ticket — pick from "Recommended next picks". If continuing the audit stream: confirm the surface with the user (Audit Log or backend grants recommended), graduate "Audit — <surface> (phased loop)" as `wayfinder:task` child of #89, claim, run the loop per `docs/agents/code-review-loop.md` (P1 spec = the original tickets' resolutions; the #141/#142/#143 finding rates + residual lists are the calibration references; **extract decision points as pure internal functions + tests EARLY — it converged #142 and #143 both**; **when a phase finding rests on a platform/library behavior claim, verify it by running the claim (test) before fixing — the #143 false-premise rejections both needed this**).
3. Claim BEFORE work: `gh issue edit <N> --add-assignee @me` — verify no concurrent sessions.
4. AFK flow: red-first falsification, /implement per module AGENTS.md, the phased loop with `git diff <last-pass-commit>` per pass (first pass = the committed surface; hand untracked files explicitly), batch-fix commits, exit at one full pass with zero HARD.
5. Post the answer as a **resolution comment** (per-pass record + per-surface verdict + finding rate), then `gh issue close <N>`, then append a context pointer to map #89's Decisions-so-far (fetch body → modify → `gh issue edit 89 --body-file <modified>` — update the frontier paragraph AND clear graduated fog lines).
6. Graduate fog (create-then-wire): `gh issue create --label wayfinder:task` → sub-issue via `gh issue edit <n> --parent 89` (verify via the child's `parent` field via GraphQL — the REST read can be stale).
7. **One-ticket-per-session limit.** When done, stop and write `docs/agents/wayfinder-<N>-handoff.md` — follow this session's format.

## Map state at session-end

Map #89 body updated this session:
- Decisions-so-far: new #143 entry (full shipped-scope summary + the 5-HARD class breakdown + the two false-premise rejections + Tier-1 calibration + resolution link); the misplaced #142 entry moved from Not-yet-specified into Decisions-so-far (next to #141).
- Not-yet-specified: new keep-last-port fog line (surfaced by #143); the #102 read-history fog line stays.
- Frontier paragraph: rewritten with the #143 audit outcome first (created+closed, 5 loop passes, 5 HARDs, finding rate + false-premise notes), User Management removed from the pending list, #142/#141 reworded "closed previous session" with the duplicated pending-list trimmed, 0 unblocked held.

## Suggested skills for next session

- **`/wayfinder`** — the parent workflow; re-load to follow "Work through the map" steps (note: next session must pick/graduate a ticket — 0 unblocked).
- **`docs/agents/code-review-loop.md`** — the phased loop is now proven across three surfaces; the #143 resolution is the reference for a shallow surface + false-premise rejections + the two-iteration race-class convergence.
- **`/implement`** — for either the Dashboard build or the next audit (audits fix HARDs in-ticket).
- **`/grilling` + `/domain-modeling`** — if the #97-grad Dashboard build graduates: the clock-out question is a decision (HITL-ish); same for the merged Finance build's #117 gate.
- **`/handoff`** — when the chosen ticket is resolved and the session is near its limit, compact + write `docs/agents/wayfinder-<N>-handoff.md`.
