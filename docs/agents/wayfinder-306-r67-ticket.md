## Question

Build: make Branch Select ViewModel lifecycle-owned. Replace raw `remember` construction in mobile and desktop navigation with lifecycle-aware `viewModel {}` while preserving clock-in and capability-refresh sequencing.
