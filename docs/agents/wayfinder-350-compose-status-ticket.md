## Question

How should Compose consume already-typed `SessionStatus` and `UserStatus` values? Remove `.name` plus local string constants from gross-income and user-management state/rendering paths while preserving wire decoding and visible status labels.
