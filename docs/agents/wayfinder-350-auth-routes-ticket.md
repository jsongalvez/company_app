## Question

How should `AuthViewModel` consume shared auth route constants? Replace duplicate login/register request literals with `ApiRoutes.AUTH_LOGIN` and `ApiRoutes.AUTH_REGISTER`, preserving byte-equivalent paths and endpoint logging behavior with focused request-path coverage.
