# Handoff — Wayfinder Map #89 (Frontend Rebuild), Session 73

## What this is

This session ran from the VPS. The frontier was EMPTY; per the #175 handoff's recommended pick #1, this session shipped **ticket #174 — Build — Mirror-only KeepLastByKey split + loadSent stale-gate port** — the #166 P5 graduate fused with the #173 P5 graduate's loadSent stale-gate holdout (the two were natural neighbours: the split touches exactly the file the holdout needs). Deliverable: **4 commits `d710aee`→`a77c1b7`** on `ralph/company-app-full-build` (each pre-commit gate green: ktlint/detekt/backend tests/cleanliness/shared-compile/Postgres; desktop 389→**392**, 0 failures, +3 tests). **Ticket #174 — CLOSED.** The #166 mirror-only-split fog line is gone end-to-end, and the 13th hand-rolled stateless stale guard (the #173 holdout) is dead.

## Session outcome

- **4 commits** on `ralph/company-app-full-build`: `d710aee` (the split + the port + 3 tests), `5149ef9` (pass-1 batch: comment SOFTs), `0c712f0` (pass-2 batch: comment precision), `a77c1b7` (P5: drop KeyedMirror.freshest). Branch is **ahead 35, NOT pushed** (standing "commit local not pushed" pattern).
- **The split**: new `KeyedMirror<K, T>` (KeepLast.kt) owns the per-key mirror (`_lastByKey`/`lastByKey`/`commit`) with no `InFlightGuard`; `KeepLastByKey` composes `KeyedMirror` + `InFlightGuard`, public API unchanged — Remittance + the existing KeepLastTest suite stayed green untouched (the fog's "2 consumers" claim grep-verified: Remittance full-shape, ReliefInvite mirror-only). ReliefInvite's `keptSent` → `KeyedMirror<String, List<ReliefInviteResponse>>()`; the screen gate `sentByKey[branchId]` (BranchSelectScreen:428) byte-identical.
- **The port**: `loadSent`'s hand-rolled `if (stamp == sentStamp) { commit }` in-hook guard (the 13th stateless stale guard, the #173 holdout) → `stale = { stamp != sentStamp }`, in-hook `if` dropped, commit unconditional inside transform. Ordering family rides the SKIP gate, never the #165 substitution shape (commit target is a keyed mirror, not a UiState — substitution would loop the re-issue). Newest-launch-wins preserved by construction (launch-captured `stamp` vs `sentStamp` at landing — same two reads, same instant as the old in-hook guard). **Deliberate semantics consequence (the #173 precedent)**: the gate runs BEFORE transform, so a stale body is never deserialized — a stale+malformed body no longer throws inside transform (was → onError no-op → inert; now → skipped → inert); state/UI behavior identical, only the wasted parse and the stale landing's spurious error-log are gone.
- **Negative controls (P4-verified)**: the two existing newest-launch-wins pins (`stale branch load never commits under the new branch panel`; `stale same-branch load never overwrites the retract-reload's fresh list`) are the port's control — both go red with the gate removed (the b1-late-response null assert and the RETRACTED-preserved assert respectively).
- **Loop: 2 passes + P5 + P5 loop-back, exit on 0 HARD.** Pass 1: 0 HARD all four (P1 1 inert CLAIM-FALSE — "observable behavior identical" phrasing, carved out in-ticket; P2 4 SOFT; P4 2 SOFT, 2 two-sighted: the "#162 KeyedMirror shape" anachronism — KeyedMirror is the #166 P5 graduate born in #174, #162 created KeepLastByKey — and the "no in-flight surface" test item, compile-time-unassertable in commonTest) → batch `5149ef9`. Pass 2: 0 HARD all four, 2 SOFTs fixed in-ticket → batch `0c712f0` (P2 truth-class corner: "observable behavior identical" qualified to state/UI — the wasted parse AND the stale landing's spurious error-log are gone; P4: transform comment's `lastByKey[panelBranch]` → `sentByKey[panelBranch]`, the actual screen gate name). Exit: 0 HARD. **P5**: P5a 1 ARCH graduate (**mirror sync projections are test-surface, not production surface** — `freshest` is test-only on both mirrors, deletion test bites only KeepLastTest); P5b 1 fix-in-ticket (`KeyedMirror.freshest` dropped — zero production callers, the fog's seam naming is exactly commit/lastByKey; `KeepLastByKey.freshest` rewired to `mirror.lastByKey.value[key]`, value-identical) → batch `a77c1b7`; P5 loop-back: 0 HARD all four.
- Accepted SOFTs (2 two-sighted, ride to fog): the "no in-flight surface" test item (compile-time-unassertable — member absence; the 3 written tests cover the other 3 spec items) + the `freshest` drop vs the ticket letter (fix-shape-1 listed `freshest(key)` on KeyedMirror; P5b removed it as test-only surface — recorded in the resolution as the deviation note; truth-class held).
- Ticket lifecycle: #174 created with `Part of #89` + native sub-issue link (`-F sub_issue_id=<db-id>`), claimed, worked, closed with resolution comment. Map #89 edited diff-verified (4 intended edits only: child-table row, #174 Decisions-so-far entry APPENDED after #173, four fog lines rewritten — mirror-only split CLOSED→successor graduate, loadSent holdout CLOSED, partition line SHARPENED, naming-coherence marker FIRED — and the frontier paragraph rewritten). Post-push verify diff showed content identical (one trailing-blank-line artifact from GitHub's API, benign).

## Current frontier (verified post-session)

**Frontier EMPTY** — 0 open sub-issues of #89. Open standalone tickets unchanged: #139 (OpenAPI map), #110 (hardcoded-month test fix). Standing fog lines (changed from #175: the mirror-only split is CLOSED and replaced by its successor graduate; the loadSent holdout is CLOSED and absorbed into the SKIP gate; the naming-coherence marker is FIRED; the stale-response partition is SHARPENED to documentation-only):

- **Stateful stale-failure legs (#173's P5 graduate — NOW THE TOP fog candidate)** — the #165 stamp guards ONLY the success commit; a superseded stateful landing that FAILS still writes `UiState.Error` onto the moved-on surface (`ApiCallHandler.launch` :74/:84). Deliberately pinned by #165's own "non-success → Error even with a structurally-stale stamp" test (the documented contract, not a regression), but the "superseded landing is inert" guarantee the stateless gate provides at 12+ sites (now including the ported loadSent) does NOT hold for the stateful failure legs. Fix-shape (a decision when it graduates): gate the onNonSuccess default-Error + onError state writes on the stamp too, or justify the loud stale failure. **#174 removed the loadSent ordering holdout that previously tied it — the split (#174) makes this the clean next pick; a decision ticket (grilling-lite/AFK analysis) first, then a build; the #173 stale-gate seam is the precedent for the fix shape.** Marker: the stateful family's next change, or a report of a stale-Error clobber.
- **Unifier-family naming coherence (marker FIRED by #174's split)** — `ActionTracker.begin` vs `InFlightGuard`/`KeepLastByKey` `tryBegin`: same Boolean "may fail" shape, different names. #174 (the mirror-only split) was the fog's "family's next change" event, so the marker is fired and the decision is now LIVE; the rename was deliberately out of #174's scope (recorded in its resolution). The documented leaning: `begin` → `tryBegin` would be the fix. A small AFK decision ticket (the leaning is documented and mechanical — ~9 call sites + 7 test sites), or wait for the next family change.
- **Mirror sync projections are test-surface, not production surface (NEW — #174's P5 graduate)** — `freshest` was test-only on both mirrors (`KeepLastByKey.freshest` pre-existed as #162 test surface; `KeyedMirror.freshest` was dropped in-ticket as a zero-producer dead projection); both mirrors' production readers use `lastByKey.value[key]`. Marker: the next KeepLast-family split must not re-bless a test-only sync projection.
- **Stale-response guards partitioned (SHARPENED by #173 + #174)** — the repo now has four stale families (substitution / ordering / coalescing / skip). #174 absorbed the loadSent holdout into the SKIP gate, so the ordering family rides the stateless gate in-handler exactly like the generation guards; the partition rule holds and the "13th hand-rolled guard" enumeration is retired (zero hand-rolled stale guards remain in the stateless family). Marker: a THIRD stale-response site re-opens the partitioning question.
- Unchanged from #175: transport-pin family fixture (marker: a pin in a THIRD VM file); stateless onError message-convention pair (marker: the onError family's next change); update-one map-if sub-shape (marker: a SECOND KeepLast-family site); the DayStatus shared/domain move; the #92 Q4/Q5 403-refresh machinery; the #158-graduated relief-surface trichotomy state (its "generation-aware ApiCallHandler variant" half is CLOSED by #173); the two #160 graduates (shared-Exposed-query-op-helpers; screen-scoped-VM-seam); k6 for branch-scoped writes (needs #106 seeding); the root-AGENTS.md stale pre-push-iOS line; the hardcoded-month test dates (#110); the user-creation/role-assignment flow (#106); the request-flow grant/deny UI gap (ReliefAccessViewModel unwired); the standalone OpenAPI map (#139). The #171 ARCH pair (Bootstrap 401-split; Bootstrap+Dashboard handled-silently seam) still rides the onError-family fog.

## Recommended next pick

The frontier is EMPTY; the standing fog lines are the only chartable work. AFK-able picks in order:

1. **Stateful stale-failure legs (#173's P5 graduate — now the top candidate, untied by #174)** — the split (#174) removed the loadSent ordering holdout that previously competed for the top slot; this is the lone design-surface item left in the stale family. More design surface than a port: a decision ticket first (grilling-lite/AFK analysis — the #165 contract to revisit is documented), then a build. The #173 stale-gate seam is the fix-shape precedent (gate the onNonSuccess default-Error + onError state writes on the stamp too, or justify the loud stale failure).
2. **Unifier-family naming coherence (marker FIRED by #174)** — small AFK decision ticket with a documented leaning (`begin` → `tryBegin`, mechanical ~9 prod + 7 test call sites), or fold into the next family change. The marker is fired — the decision is live, not deferred.
3. **Relief loadSent holdout** — CLOSED (absorbed into the #173 gate by #174); nothing left to port — the stateless stale family is fully hand-rolled-guard-free.
4. **Transport-pin family fixture** — marker only (same two test files); wait for a third VM file.
5. The other items (DayStatus shared/domain move, #92 Q4/Q5 machinery, message-convention pair as a drive-by) need a human or a specific trigger — take only if present.

If the migration's runbook/persistence work isn't done (Coolify GitHub App for the private repo — the standing critical follow-up), that's the human-side item; the wizard run itself is COMPLETE.

## Migration notes (VPS handover — follow-ups still standing)

- **The switch holds** — eighth VPS session, toolchain stable. Pre-commit gate green on all 4 commits. Postgres reachable at localhost:5432; `company_app_test` clean. (Note: the Android SDK is absent on the VPS — `:composeApp:testDebugUnitTest` fails on `SDK location not found`; desktop tests + ktlintCheck + the backend gate run fine. Pre-push multi-target compile remains a push-time concern, per the standing "not pushed" pattern.)
- Standing human/runbook follow-ups unchanged from #175: Coolify GitHub App (or token-in-URL) for the private-repo app resource; wizard bootstrap should pin Asia/Manila; gh-auth stage needs `gh auth setup-git`; Postgres stage guard-create `company_app_test`; the test suite needs a TZ fixture (this VPS is already Asia/Manila); check for other Windows-mount-hidden exec bits; shellcheck still not installed; ntfy topic rotation; the loop-infra zombie-detector real exercise. `/tmp/opencode` ownership is stable (ubuntu-owned).

## Patterns + learnings

- **The handoff's fusion call was right and cheap.** The #175 pick #1 explicitly cross-wired the loadSent holdout into the split ticket ("the split touches exactly the file the holdout needs") — and it was: the whole ticket was 4 small commits in 3 files, with the port landing on the very `keptSent` line the split rewired. Fusing two marker-fired graduates into one natural-neighbour ticket beat leaving the holdout dangling for a trivial standalone port.
- **The naming-coherence marker fired exactly as the fog predicted.** #174's split WAS the fog's "family's next change" event — the marker fired, and the decision is now live with a documented leaning. The discipline that paid: recording it as marker-FIRED in the resolution + map (out of scope, deliberately), NOT silently folding the rename in or letting the fired marker rot. The map's fog language ("re-opens the naming decision") is doing its job — a future session has a precise, live decision to take.
- **P5b's freshest drop was a genuine catch on the new seam class.** The ticket letter listed `freshest(key)` on KeyedMirror (mirroring the #162 KeepLastByKey surface); P5b's deletion test proved it zero-producer (both mirrors' production readers use `lastByKey.value[key]`), and the fog's seam naming was exactly commit/lastByKey. The fix was a 1-line drop + rewire + test rework — cheap, and the deviation note (the resolution's truth-class discipline) kept the ticket letter honest.
- **The "inert CLAIM-FALSE" carve-out repeated.** Pass 1's P1 flagged "observable behavior identical" as marginally false (the stale+malformed body's logError vanishes post-port); pass 2's P2 re-rated the SAME line as a truth-class corner, and the in-ticket qualification ("state/UI behavior identical; only the wasted parse and the stale landing's spurious error-log are gone") is what shipped. The #173 precedent (the identical phrase) was accepted unqualified; this session's qualification is the sharper version — worth noting the phrase should carry the log carve-out from now on.
- **Prior-session patterns unchanged:** claim-first, facts-via-code, one-ticket-per-session, commit local not pushed, gate green before commit, the phased loop until 0 HARD, negative-controlled pins (the two existing loadSent pins doubled as the port's controls), diff-verified map edits (4 intended edits, appended-after not replaced).

## Constitution update (this session)

**No new register classes.** The loop ran clean (2 passes + P5, exit on 0 HARD); no HARD findings at all — the passes were comment-precision work. The P5 observations are architecture residue, not bug classes: one graduates a new fog line (mirror sync projections are test-surface), one fixes in-ticket (KeyedMirror.freshest drop). Truth-class conceptually bumped (the pass-2 qualification of my own "observable behavior identical" claim — caught by P2 pre-ship), but no new register row; the existing truth-class row already carries the #173 count.

## Critical follow-ups (human)

- **Coolify GitHub App** (or token-in-URL) for the private repo — needed before the app-resource stage works; still open since #169.
- **shellcheck** still not installed — `shellcheck -S warning scripts/vps-migration-wizard.sh` on the VPS.
- **ntfy topic rotation** from session 61 stands (`wf-0w9uaukg` in the gitignored `.wayfinder-loop.env`).
- **Loop-infra zombie-detector** still needs a real exercise (a deliberate 10-min-sleep sub-agent probe during a review phase).
- **The branch is ahead 35 and NOT pushed** — the next push (k6 baseline + multi-target compile, ~3 min; note the VPS lacks the Android SDK, so multi-target compile may need the SDK set up or a skip) is a deliberate act; fine to leave until the map needs remote sync or the user wants it.
- Wizard fold-in follow-ups (#169) unchanged: Asia/Manila pin, `gh auth setup-git`, guard-create test DB, TZ fixture, exec-bit sweep (`git ls-files -s | grep 100644` on scripts).

## Suggested skills for next session

- **`/wayfinder`** — "Work through the map" (frontier empty → AFK fog-graduation; the stateful stale-failure-legs decision ticket is the natural pick, with the naming-coherence decision-ticket (marker-fired, leaning documented) close behind).
- **`/implement` + `docs/agents/code-review-loop.md`** — for the build ticket; register watch-lenses: **truth-class** (including the driver's OWN comment deltas and "observable behavior identical"-style phrasings — the pass-2 catch this session), **vacuous-test**, **fix-that-didn't-land**, **keyed-mirror ordering**.
- **`/writing-for-agents`** — any agent-facing markdown edit (AGENTS.md, docs/agents/, the handoff).
- **`docs/agents/decision-loop.md`** — for any HITL design ticket (stateful stale-failure legs would be the natural first user; standing frame applies). The naming-coherence ticket is AFK-leaning but small enough that a grilling pass would be cheap if a human wants the rename confirmed.
