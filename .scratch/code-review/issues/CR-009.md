# CR-009 — deduplicate test helpers

## Summary

Consolidated ~120+ duplicated private test helper methods across 24 `*PostgresTest.kt` files into the shared `DatabaseTestHelper` object.

## Changes

### New centralized helpers added to `DatabaseTestHelper.kt`:
- `insertTestUser(id, prefix)` — replaces 22 private `insertUser` wrappers
- `insertTestBranch(id, name?, branchType?)` — replaces 18 private `insertBranch` helpers
- `createBranchDayForToday(branchId)` — replaces 7 private `createBranchDay` helpers
- `insertTestClient(id?)` — replaces 7+ private `insertClient` helpers
- `insertTestCategory(id, name?)` — replaces 4 private `insertCategory` helpers
- `insertTestProduct(id, name?, categoryId, ...)` — replaces 3 private `insertProduct` helpers
- `insertTestSession(id, clientId, branchDayId, ...)` — replaces raw JDBC SQL session inserts
- `insertTestCompensation(branchDayId, userId, amount, assignedBy)` — replaces 3 private helpers
- `insertTestExpense(branchDayId, userId, amount, deleted?)` — replaces 3 private helpers
- `revokeAllCapabilities(userId)` — replaces 6 private `revokeCapabilities` helpers

### Test files updated (24 files):
All `*PostgresTest.kt` files updated to use centralized helpers. Private helpers removed.
Raw JDBC SQL session inserts in `RemittanceServicePostgresTest` and `MonthlyRemittanceSummaryServicePostgresTest` replaced with Exposed DSL.

### Net impact: -888 lines across 25 files.

**Status:** ✅ done
