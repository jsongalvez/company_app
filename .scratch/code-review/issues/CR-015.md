# CR-015: ~50× duplicated ViewModel try/catch/log/state boilerplate in composeApp

**Source:** Critical F -- found in chunk 4 (standards hard: "~50× duplicated ViewModel boilerplate")

**What:**
Every API call in composeApp ViewModels repeats the same pattern of viewModelScope.launch, try/catch, UiState.Loading/Success/Error, and logInfo/logError calls. This pattern repeated ~72 times across 19 ViewModels.

**Spec reference:** `AGENTS.md` composeApp logging convention

**Files:** All ViewModels under `composeApp/src/commonMain/kotlin/.../viewmodel/`

**Fix:**
1. Extract a shared `ApiCallHandler` class with `launch()` and `launchUnit()` methods
2. Migrate all 19 ViewModels
3. Update `AGENTS.md` to document the helper

**Priority:** medium
**Status:** ✅ done
