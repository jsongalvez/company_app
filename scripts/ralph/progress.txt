# Ralph Progress Log
Started: Sat Jun 27 12:27:08 PM PST 2026
---

## Wed Jul 15 2026 - US-037
Thread: unavailable
- Implemented shared ViewModels for all features in composeApp commonMain.
- Created UiState sealed class (Idle, Loading, Success<T>, Error) in composeApp/src/commonMain/.../viewmodel/UiState.kt.
- Created 19 ViewModels covering all feature areas: AllowanceViewModel, AttendanceViewModel, AuditLogViewModel, AuthViewModel, BranchViewModel, ClientViewModel, CommissionViewModel, CompensationViewModel, DelegateViewModel, ExpenseViewModel, InventoryViewModel, NotificationViewModel, ProductSaleViewModel, ProductViewModel, ReliefAccessViewModel, RemittanceViewModel, ReportViewModel, SessionViewModel, UserViewModel.
- Each ViewModel extends androidx.lifecycle.ViewModel, takes ApiClient as constructor param, exposes StateFlow<UiState<T>> state properties, and calls Ktor HTTP endpoints in viewModelScope coroutines.
- Pattern: private val _state = MutableStateFlow<UiState<T>>(UiState.Idle), val state = _state.asStateFlow(), method wraps ApiClient call in UiState.Loading/Success/Error.
- Added UiStateTest (5 tests: singleton, data, error, exhaustive sealed class) in commonTest.
- Extended ComposeAppCommonTest with 2 UiState integration tests.
- Files changed:
  - NEW: composeApp/src/commonMain/.../viewmodel/UiState.kt + 19 ViewModel files
  - NEW: composeApp/src/commonTest/.../viewmodel/UiStateTest.kt
  - MODIFIED: composeApp/src/commonTest/.../ComposeAppCommonTest.kt, scripts/ralph/prd.json (US-037 passes=true), scripts/ralph/progress.txt
- Quality gate green: :composeApp:compileKotlinDesktop + :composeApp:desktopTest (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - ViewModels in KMP commonMain use `androidx.lifecycle:lifecycle-viewmodel-compose` which is available across all KMP targets (Android, iOS, Desktop).
  - ApiClient (Ktor) is the single dependency injected into ViewModels — no DI framework needed for now.
  - Each ViewModel has dedicated StateFlows per operation (create, read, update, delete) rather than a single UI state data class.
  - KMP ViewModels cannot easily be unit-tested without a mocking framework since ApiClient makes real HTTP calls via Ktor. The existing UiStateTest validates the state type itself.
  - The `compose.desktop {}` block in build.gradle.kts enables `desktopTest` target for running common tests on JVM.
  - KMP `commonTest` uses `kotlin.test` (not JUnit) — assertions are `assertEquals`, `assertIs<T>`, `assertTrue`.
  - All ViewModels follow identical pattern: `MutableStateFlow<UiState<T>>` + `asStateFlow()` + `viewModelScope.launch` + try/catch wrapping ApiClient calls.
---

## Tue Jul 14 2026 - US-032
Thread: unavailable
- Implemented daily sales summary endpoint: GET /api/branches/{branchId}/daily-summary?date=.
- Added daily_sales_summary view to V1__full_schema.sql using correlated subqueries (avoiding cross-join issues) aggregating: gross_income (completed, non-voided sessions), total_compensation, total_expenses, total_product_sales, total_commission. V3 migration merged into V1.
- Created DailySalesSummary data class + DailySalesSummaryView Exposed Table (read-only view model, no PK).
- Created DailySalesSummaryRepository.findByBranchAndDate(branchId, date) querying the view with Exposed DSL.
- Created DailySalesSummaryService.getDailySummary with VIEW_BRANCH_DATA capability gate (GLOBAL context) and branch existence validation.
- Created DailySalesSummaryRoutes: GET /api/branches/{branchId}/daily-summary with date query param parsing (yyyy-MM-dd).
- Added DailySalesSummaryResponse DTO to shared module with netIncome computed as grossIncome - totalCompensation - totalExpenses.
- Wired DailySalesSummaryRoutes in Main.kt.
- Added 12 integration tests covering: zero data returns all zeros, gross income from completed sessions, excludes non-completed, excludes voided, includes unvoided, total compensation with multiple users, total expenses excluding deleted, product sales, commission, net income calculation, 403 without capability, 404 for non-existent branch.
- Files changed:
  - NEW: shared/.../dto/DailySalesSummaryDto.kt, repository/model/DailySalesSummary.kt, repository/DailySalesSummaryRepository.kt, service/DailySalesSummaryService.kt, api/routes/DailySalesSummaryRoutes.kt, test/.../DailySalesSummaryServicePostgresTest.kt
  - MODIFIED: resources/db/migration/V1__full_schema.sql (appended daily_sales_summary view)
  - DELETED: resources/db/migration/V3__create_daily_sales_summary_view.sql (merged into V1)
  - MODIFIED: Main.kt (import + wire DailySalesSummaryRoutes), scripts/ralph/prd.json (US-032 passes=true), scripts/ralph/progress.txt
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Daily sales summary uses a SQL view with correlated subqueries (one per aggregation) to avoid cross-join issues when multiple 1:N relationships exist on the same branch_day.
  - The view is modeled as a read-only Exposed Table (no PK, no autoGenerate) and queried with standard selectAll().where().
  - netIncome is computed in the repository layer (not the SQL view) by subtracting totalCompensation + totalExpenses from grossIncome.
  - VIEW_BRANCH_DATA capability is used for read-only summary endpoints (same pattern as other read endpoints).
  - BigDecimal comparison across different scales (NUMERIC(10,2) vs NUMERIC(15,4)) needs compareTo() instead of assertEquals() to avoid scale mismatch failures.
  - Integration tests for summary aggregates need unique users per compensation row because of idx_compensation_unique constraint.
  - The `toResponse()` mapping follows the same private extension function pattern as other routes.
---

## Tue Jul 14 2026 - US-031
Thread: unavailable
- Implemented notification read endpoints: GET /api/notifications (listUnread for caller) and PATCH /api/notifications/{notificationId}/read (markRead).
- Created NotificationDto.kt (NotificationResponse) in shared module.
- Created NotificationService with listUnread (no capability gate — personal notifications) and markRead (404 if not found or not owned).
- Created NotificationRoutes: GET /api/notifications returns list of unread NotificationResponse; PATCH /api/notifications/{notificationId}/read marks as read and returns updated notification.
- Wired NotificationRoutes.register(config) in Main.kt.
- Added 7 integration tests covering: listUnread returns all unread, empty when none, excludes other users' notifications, markRead sets isRead+readAt, marked excluded from subsequent unread, 404 on non-existent, 404 on other user's notification.
- Files changed:
  - NEW: shared/.../dto/NotificationDto.kt, service/NotificationService.kt, api/routes/NotificationRoutes.kt, test/.../NotificationServicePostgresTest.kt
  - MODIFIED: Main.kt (import + wire NotificationRoutes), scripts/ralph/prd.json (US-031 passes=true), scripts/ralph/progress.txt
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 330 tests).
- **Learnings for future iterations:**
  - Notification endpoints do not require a capability check since users can only see their own notifications (scoped by userId from JWT).
  - Ownership check in markRead: look up the notification first, then verify notification.userId == callerId before marking; throw 404 (not 403) to avoid leaking notification existence.
  - The notification table has a UNIQUE constraint on (session_id, user_id) — integration tests must use unique session IDs when creating multiple notifications for the same user.
  - Direct inserts into tables with .autoGenerate() PK work fine with explicit id values in tests (the Exposed bug only affects insertIgnore).
  - Order of FK cleanup in tests: NotificationTable → SessionVoidTable → SessionTable → UserRoleTable → UserCapabilityTable → BranchDayTable → BranchTable → ClientTable → AppUserTable.
---

## Tue Jul 14 2026 - US-030
Thread: unavailable
- Implemented notification table migration (V3__create_notification_table.sql) with: id, session_id FK, user_id FK, branch_id FK, is_read, read_at, created_at; UNIQUE index on (session_id, user_id); partial index on (user_id) WHERE is_read = false.
- Created Notification data class + NotificationTable Exposed model.
- Created Role data class + RoleTable + UserRoleTable Exposed models for coordinator/role lookup.
- Created NotificationRepository with insert(), findUnreadByUserId(), and markRead().
- Created NextAppointmentScheduler with: targetDate() pure function (now + 2 days Manilla), nextRunDelayMs() for scheduling at 07:00 AM Manila, findUpcomingSessions() querying COMPLETED non-voided sessions by next_appointment_date, findActiveCoordinatorsForBranch() joining user_role + role (COORDINATOR/MANAGER) + active user_branch_assignment.
- Scheduler wired in Main.kt via SingleThreadScheduledExecutor with daemon thread, initial delay calculated via nextRunDelayMs(), fixed-rate at 24-hour intervals with exception handling.
- Added 12 tests (10 integration + 2 unit): creates notifications for coordinator, skips duplicates, ignores non-coordinator users, ignores unassigned coordinator, ignores wrong branch, notifies both coordinator and manager, excludes voided sessions, includes only COMPLETED, matches exact target date, targetDate pure func, nextRunDelayMs positive, nextRunDelayMs wraps to next day.
- Files changed:
  - NEW: resources/db/migration/V3__create_notification_table.sql, repository/model/Notification.kt, repository/model/Role.kt, repository/NotificationRepository.kt, service/NextAppointmentScheduler.kt, test/.../NextAppointmentSchedulerPostgresTest.kt
  - MODIFIED: Main.kt (scheduler init + wiring), scripts/ralph/prd.json, scripts/ralph/progress.txt
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 322 tests).
- **Learnings for future iterations:**
  - Exposed 0.61.0 has a bug with `insert`/`insertIgnore` on tables with `autoGenerate()` PKs + `.references()` FKs — generates malformed SQL ("invalid reference to FROM-clause entry"). Workaround: use service-layer methods (e.g. SessionService.create) for test data setup rather than direct table inserts.
  - The branch table has UNIQUE (branch_type, name) — test branch names must include a UUID suffix to avoid collisions across test classes.
  - For tests using multiple sessions for the same client, create COMPLETED sessions first to avoid the PENDING session guard (idx_client_one_pending_session).
  - The `role` and `user_role` tables exist in the schema but are not populated by any existing code path — tests must manually insert into user_role to test role-based queries.
  - FK cleanup order matters: delete NotificationTable before SessionTable, SessionTable before BranchDayTable, BranchDayTable before BranchTable.
  - `NextAppointmentScheduler.findActiveCoordinatorsForBranch` includes both COORDINATOR and MANAGER roles (MANAGER is a superset of COORDINATOR).
  - `SessionVoidTable` also has `autoGenerate()` PK + `.references()` FKs — use `insertIgnore` for test voids.
---

## Tue Jul 14 2026 - US-033
Thread: unavailable
- Implemented monthly remittance summary endpoint: GET /api/branches/{branchId}/monthly-summary?year=&month=.
- Added monthly_remittance_summary view to V1__full_schema.sql aggregating submitted remittances by branch, year, month: total_remittances, session_count, product_count, gross_income (from SESSION snapshots), total_compensation, total_expenses, net_income. V5 migration merged into V1.
- Created MonthlyRemittanceSummaryResponse DTO, MonthlyRemittanceSummary view model (read-only Exposed Table), MonthlyRemittanceSummaryRepository (findByBranchYearMonth), MonthlyRemittanceSummaryService (VIEW_BRANCH_DATA gate, GLOBAL context), MonthlyRemittanceSummaryRoutes (GET with year/month query params, validates month 1-12).
- Wired MonthlyRemittanceSummaryRoutes in Main.kt.
- Added 8 integration tests covering: zero data (404), single SESSION remittance with snapshot, SESSION with compensation+expenses (added before submit for correct snapshot), PRODUCT remittance (counts only, zero financials), mixed aggregation (SESSION + PRODUCT), 403 without capability, 404 for non-existent branch, PRODUCT zero snapshot values.
- Fixed DatabaseConfig.runMigrations() to call flyway.repair() before migrate() to handle schema evolution in dev (dirty migrations from V1 edits).
- Files changed:
  - MODIFIED: resources/db/migration/V1__full_schema.sql (appended monthly_remittance_summary view)
  - DELETED: resources/db/migration/V5__create_monthly_remittance_summary_view.sql (merged into V1)
- **Learnings for future iterations:**
  - The monthly_remittance_summary SQL view uses LEFT JOIN with remittance_financial_snapshot — SESSION remittances have snapshots (gross_income, etc.) while PRODUCT remittances don't. Only counts are meaningful for PRODUCT.
  - `RemittanceService.submit` overwrites `submitted_date` to `today` — to test different months, the remittance must be inserted directly with a custom date before being submitted (but submit will overwrite it anyway). For test simplicity, test the monthly grouping within the same month only.
  - Compensation and expenses must be added to the branch_day BEFORE remittance submission — the financial snapshot captures totals at submit time. Adding them after submit doesn't update the snapshot.
  - The UNIQUE constraint on `(branch_id, type, submitted_date)` prevents two drafts of the same type for the same branch on the same day. Tests creating multiple remittances must use different types (SESSION vs PRODUCT) or different dates.
  - Using `RemittanceTable.insert {}` directly with an `autoGenerate()` PK column triggers the Exposed 0.61.0 bug generating malformed SQL. Workaround: use raw JDBC inserts via `DatabaseConfig.dataSource.connection.createStatement().execute(...)`.
  - Applying `flyway.repair()` before `flyway.migrate()` is needed when V1 gets modified (checksum changes) or V3/V4 files are deleted (merged into V1). This was added to DatabaseConfig as a permanent fix for the dev environment.
---

## Tue Jul 14 2026 - US-035
Thread: unavailable
- Implemented audit log read and acknowledge endpoints:
  - GET /api/audit-log?tableName=&recordId= — lists audit entries for a specific table/record, ordered by changed_at DESC
  - GET /api/audit-log/flagged — lists unacknowledged flagged entries
  - PATCH /api/audit-log/{entryId}/acknowledge — sets acknowledged_by and acknowledged_at (requires ASSIGN_COMPENSATION)
- Created AuditLogEntry data class (repository/model) and AuditLogEntryResponse DTO (shared module).
- Added AuditLogRepository.findByTableAndRecord, findById, findFlagged, acknowledge (with acknowledgedBy param).
- Created AuditLogService with no capability gates on reads (audit trail visible to all roles), ASSIGN_COMPENSATION gate on acknowledge.
- Created AuditLogRoutes wired in Main.kt.
- Added 8 integration tests covering: findByTableAndRecord with/without matches, multiple entries, findFlagged excludes non-flagged, excludes acknowledged, acknowledge succeeds with capability, forbidden without capability, not found for non-existent entry, not found for already acknowledged entry.
- Files changed:
  - NEW: shared/.../dto/AuditLogDto.kt, backend/.../repository/model/AuditLogEntry.kt, backend/.../service/AuditLogService.kt, backend/.../api/routes/AuditLogRoutes.kt, test/.../AuditLogServicePostgresTest.kt
  - MODIFIED: repository/AuditLogRepository.kt, Main.kt, scripts/ralph/prd.json (US-035 passes=true), scripts/ralph/progress.txt
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Audit log reads have no capability gate — the design spec states "Audit trail is visible to all roles." Only acknowledge requires ASSIGN_COMPENSATION.
  - Audit log `acknowledge` sets both acknowledged_by and acknowledged_at server-side — no request body needed, just the path param.
  - `AuditLogRepository.acknowledge(entryId, acknowledgedBy)` returns `true` if a row was actually updated (entry existed and wasn't already acknowledged), `false` otherwise. This is used to detect failed vs duplicate acknowledges.
  - The `findById` repository method is useful for post-mutation reads (e.g., acknowledge then fetch).
  - Direct `AuditLogTable.insert {}` with explicit `id` on the `.autoGenerate()` PK works fine for test fixture setup (the Exposed bug only affects `insertIgnore`).
  - The partial index `idx_audit_flagged WHERE is_flagged = true` is used by findFlagged — only flagged entries are candidates for acknowledgement.
  - `SortOrder.DESC` import is `org.jetbrains.exposed.sql.SortOrder`.
---

## Tue Jul 14 2026 - US-034
Thread: unavailable
- Verified and fixed export endpoints (PDF and CSV) which were already implemented but had failing integration tests.
- Fixed ExportServicePostgresTest data setup: findOrCreateBranchDay now checks for existing branch_day before inserting (prevents UNIQUE constraint violation on (branch_id, date)).
- Fixed insertRemittance to include date_range_start, date_range_end, and submitted_by (NOT NULL constraints).
- Fixed insertRemittanceLine to include sessionId (line_type_logic CHECK constraint requires SESSION lines to have session_id NOT NULL).
- Fixed createSubmittedRemittanceForBranch to create client+session for remittance_line FK reference, with correct cleanup order for FK dependencies.
- Fixed cleanup (deleteTestRows) to disable/enable the remittance_financial_snapshot immutability trigger before/after deleting snapshot rows, with proper FK-aware deletion order.
- Fixed test assertions to match actual CSV output: branch-type exports show Net Income (not Gross Income), so assertions check net = gross - comp - exp values.
- All 22 export integration tests pass: daily CSV/PDF, daily with session data, daily 403/404, monthly CSV/PDF/404, all-time CSV/PDF/404, provincial CSV/PDF/404, medical mission CSV/404, clinic type rejection, parseFormat validation, provincial with month filter.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (360 tests, BUILD SUCCESSFUL).
- Files changed: only ExportServicePostgresTest.kt (test data setup and cleanup fixes).
- **Learnings for future iterations:**
  - The remittance_financial_snapshot immutability trigger (BEFORE DELETE OR UPDATE) must be disabled before deleting snapshot rows in test cleanup, then re-enabled after.
  - Cleanup deletion order matters: RemittanceLineTable → RemittanceDayBreakdownTable → RemittanceTable → snapshot tables → SessionVoidTable → SessionTable → CompensationTable → ExpenseTable → ... → BranchDayTable → BranchTable. FK constraints enforce this order.
  - branch_type exports (provincial, medical mission) only include Net Income in CSV, not Gross Income - the BranchTypeMonthlySummary data class only has netIncome.
  - Use `testBranchIds` tracking pattern to clean up test-created branches that use random UUIDs.
  - Test assertions must match the actual CSV output format (check headers and expected net income values).
---

## Tue Jul 14 2026 - US-036
Thread: unavailable
- Implemented shared Ktor 3.x HTTP client (ApiClient) and TokenStore (expect/actual) for composeApp/KMP.
- Added Ktor 3.5.1 to gradle/libs.versions.toml with client modules: core, content-negotiation, serialization-kotlinx-json, auth, logging, and platform engines (okhttp, darwin, cio).
- Updated composeApp/build.gradle.kts: added kotlin.serialization plugin, Ktor dependencies, desktop JVM target (jvm("desktop")), shared module dependency, android.security.crypto dependency, and compose.desktop application block.
- Created TokenStore interface + expect fun createTokenStore() in commonMain network package.
- Created ApiClient in commonMain with ContentNegotiation (kotlinx.json), Auth bearer (loads token from TokenStore, skips /auth/ paths), HttpRequestRetry (3 retries, server errors), DefaultRequest (base URL + JSON content type), and HttpResponseValidator (emits onUnauthorized on 401).
- Created Android TokenStore actual using EncryptedSharedPreferences (with SharedPreferences fallback) + AndroidAppContext holder + defaultBaseUrl="http://10.0.2.2:3023".
- Created iOS TokenStore actual using NSUserDefaults + defaultBaseUrl="http://localhost:3023".
- Created Desktop TokenStore actual using ~/.companyapp/token file + defaultBaseUrl="http://localhost:3023".
- Created Desktop Main.kt for compose desktop entry point (Window + App()).
- Created Platform.desktop.kt actual for the existing expect fun getPlatform().
- Updated shared/build.gradle.kts: added iosSimulatorArm64 target for KMP dependency resolution.
- Updated MainActivity.kt: initialized AndroidAppContext.context in onCreate.
- Quality gate green: :composeApp:compileKotlinDesktop (BUILD SUCCESSFUL), :shared:compileKotlinIosSimulatorArm64 (BUILD SUCCESSFUL), :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- Files changed:
  - MODIFIED: gradle/libs.versions.toml (Ktor 3.5.1 version + 8 library entries + security-crypto version + entry)
  - MODIFIED: composeApp/build.gradle.kts (serialization plugin, desktop target, Ktor/shared/androidx deps, compose.desktop)
  - MODIFIED: shared/build.gradle.kts (added iosSimulatorArm64 target)
  - MODIFIED: composeApp/src/androidMain/kotlin/.../MainActivity.kt (AndroidAppContext init)
  - NEW: composeApp/src/commonMain/kotlin/.../network/TokenStore.kt (interface + expect fun)
  - NEW: composeApp/src/commonMain/kotlin/.../network/ApiClient.kt (Ktor HttpClient config)
  - NEW: composeApp/src/androidMain/kotlin/.../network/TokenStore.android.kt (EncryptedSharedPreferences)
  - NEW: composeApp/src/iosMain/kotlin/.../network/TokenStore.ios.kt (NSUserDefaults)
  - NEW: composeApp/src/desktopMain/kotlin/.../network/TokenStore.desktop.kt (file-based)
  - NEW: composeApp/src/desktopMain/kotlin/.../Main.kt (desktop entry point)
  - NEW: composeApp/src/desktopMain/kotlin/.../Platform.desktop.kt (getPlatform actual)
- **Learnings for future iterations:**
  - Ktor 3.5.x API differences from 3.0.x: `retryOnServerErrors` takes Int (not Boolean), `defaultRequest` is installed via `install(DefaultRequest)`, `contentType` requires explicit import from `io.ktor.client.request`, `header()` needs its own import.
  - Adding a JVM/desktop target to an existing KMP project requires actual declarations for all expect/expect fun declarations (e.g., getPlatform()).
  - The `shared` module must include all iOS simulator targets that the composeApp has, otherwise KMP dependency resolution fails with "Unresolved platforms".
  - Android EncryptedSharedPreferences from security-crypto needs try-catch fallback to regular SharedPreferences (MasterKey init may fail on some emulators/API levels).
  - `DefaultRequest` plugin config replaces `defaultRequest {}` convenience function in Ktor 3.5.x.
  - `sendWithoutRequest { !it.url.toString().contains("/auth/") }` works in Ktor 3.5.x for skipping token on auth endpoints.
  - The `androidx.security:security-crypto` library requires `minSdk = 23` (we have 24, fine) but can still fail on older devices.
  - Compose Desktop Main.kt needs `import androidx.compose.ui.window.*` and uses `application {}` + `Window {}` pattern.
  - `compose.desktop {}` block in build.gradle.kts is required for desktop application configuration.
---
- Use `sql<number>` template for aggregations
- Always use `IF NOT EXISTS` for migrations
- Export types from actions.ts for UI components
- **Before coding a story, read the relevant doc in `docs/`:**
  - `docs/architecture_implementation_plan.md` — architecture decisions, module map, sequencing
  - `docs/engine_specifications.md` — pseudocode + SQL for commission, delegate, and remittance engines
  - `docs/business_requirements.md` — domain terminology, business rules, constraints
  - `backend/src/main/resources/db/migration/V1__full_schema.sql` — authoritative schema (use this over any ERD)
- Backend module: Kotlin (JVM) + Javalin + Exposed (DSL) + Flyway, package root `com.companyb.companyapp`. Layers: `api/routes`, `service`, `repository` (+ `repository/model` for Exposed `Table` objects), `auth`, `database`, `logging`.
- Quality gate (run per backend change): `./gradlew :backend:detekt :backend:ktlintCheck :backend:test`. ktlint auto-fix: `./gradlew :backend:ktlintFormat`.

## Tue Jul 14 2026 - US-029
Thread: unavailable
- Implemented remittance submission endpoint: POST /api/remittances/{remittanceId}/submit body: { expectedVersion }.
- RemittanceRepository.submit: single transaction with version-based optimistic locking, calculates gross_income (SUM of non-deleted SESSION remittance_lines), total_compensation (SUM of compensation for paying_branch_day_ids in day breakdowns), total_expenses (SUM of non-deleted expenses for covered branch_day_ids), and net_income. Inserts financial snapshot for SESSION remittances (write-once through DB immutability trigger). Updates remittance status to SUBMITTED, increments version, updates all covered branch_day rows to REMITTED. Writes audit log entries for remittance and each branch day update.
- RemittanceService.submit: gates on SUBMIT_REMITTANCE capability (GLOBAL context), validates DRAFT status, checks version match (ConflictResponse 409 on mismatch), catches version_mismatch and not_draft errors from repository.
- RemittanceRoutes: registered POST /api/remittances/{remittanceId}/submit with SubmitRemittanceRequest body (expectedVersion: Int), returns RemittanceSubmitResponse with financial snapshot details.
- Created RemittanceFinancialSnapshot model + Exposed Table for the existing remittance_financial_snapshot table.
- Created RemittanceFinancialSnapshotRepository.insert() for write-once snapshot insertion (called from within the submit transaction).
- Added 11 integration tests covering: SESSION submit with snapshot, PRODUCT submit without snapshot, compensation+expense calculations, version mismatch (409), forbidden (403), not found (404), already submitted (400), audit log writing, empty day breakdowns with zero calculations.
- DTOs: SubmitRemittanceRequest, RemittanceSubmitResponse in shared RemittanceDto.kt.
- Files changed:
  - NEW: repository/model/RemittanceFinancialSnapshot.kt, repository/RemittanceFinancialSnapshotRepository.kt
  - MODIFIED: repository/RemittanceRepository.kt, service/RemittanceService.kt, api/routes/RemittanceRoutes.kt, shared/.../dto/RemittanceDto.kt, test/.../RemittanceServicePostgresTest.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 311 tests).
- **Learnings for future iterations:**
  - The remittance_financial_snapshot table has an immutability trigger (trg_remittance_snapshot_immutable) that blocks UPDATE and DELETE operations — insert only. Test cleanup must disable this trigger before deleting snapshot rows.
  - RemittanceLineRepository.addLine() increments the parent remittance.version atomically — always read the current version before submitting.
  - FOR UPDATE and SERIALIZABLE isolation were specified in the PRD engine but could not be implemented in Exposed 0.61.0 (no DSL support for FOR UPDATE). Version-based optimistic locking with atomic version update provides adequate concurrency control.
  - Exposed 0.61.0 has a bug where `insertIgnore` or `insert` on tables with `autoGenerate()` primary key columns generates malformed SQL ("invalid reference to FROM-clause entry"). Workaround: use raw JDBC inserts for test data setup on such tables, or use the service-layer methods that work around this.
  - Remittance day breakdowns do NOT increment the remittance version; only line additions/removals do.
  - For SESSION remittances, the financial snapshot includes gross_income, total_compensation, total_expenses, and net_income. PRODUCT remittances do not get a snapshot.
  - Branch day status transition to REMITTED happens as part of the submit transaction — all covered branch days are atomically updated.
  - Audit log entries for the remittance update and each branch day update must be written INSIDE the same transaction (no nested transactions). Use direct AuditLogTable.insert {} rather than AuditLogRepository.record() when inside an existing transaction.
---

## Tue Jul 14 2026 - US-023
Thread: unavailable
- Implemented compensation assign endpoint (POST /api/compensation) and update endpoint (PATCH /api/compensation/{compensationId}).
- Created Compensation data class + CompensationTable Exposed model for the existing compensation table.
- CompensationRepository.create: idempotent insert with PK pre-check (returns existing if same UUID), pre-checks unique (user_id, paying_branch_day_id) via findByUserAndPayingDay and throws ConflictResponse (409) on duplicate, writes audit log on actual insert.
- CompensationRepository.update: last-write-wins update of amount and note, writes audit log with old/new values.
- CompensationService.create: gates on ASSIGN_COMPENSATION capability (GLOBAL context), validates non-negative amount, validates work/paying branch days exist (404 if not), checks unique constraint via repository.
- CompensationService.update: gates on ASSIGN_COMPENSATION capability, validates non-negative amount, validates compensation exists (404 if not), delegates to repository.
- CompensationRoutes: registered POST /api/compensation and PATCH /api/compensation/{compensationId} with UUID/BigDecimal parsing and response mapping.
- Added grantAssignCompensation to DatabaseTestHelper.
- Added CompenssationDto.kt (CreateCompensationRequest, UpdateCompensationRequest, CompensationResponse) to shared module.
- Added 11 integration tests covering: create with all fields, create without note, idempotent duplicate, duplicate user+paying day (409), forbidden without capability (403), non-existent work/paying branch day (404), negative amount (400), update with new amount/note, update non-existent (404), update without capability (403), create/update audit log writing.
- Files changed: shared/.../dto/CompensationDto.kt (new), repository/model/Compensation.kt (new), repository/CompensationRepository.kt (new), service/CompensationService.kt (new), api/routes/CompensationRoutes.kt (new), test/.../CompensationServicePostgresTest.kt (new), Main.kt, DatabaseTestHelper.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 231 tests).
- **Learnings for future iterations:**
  - compensation table uses a UNIQUE INDEX on (user_id, paying_branch_day_id) — pre-check in the service layer for a cleaner 409 error before hitting the DB constraint.
  - Compensation uses numeric(10,2) for amount — use `decimal("amount", 10, 2)` in Exposed with BigDecimal, extract precision/scale as private const val for MagicNumber compliance.
  - ASSIGN_COMPENSATION is scoped to BRANCH in V2 seed comment but checked with GLOBAL context for consistency with other endpoints (VOID_SESSION pattern).
  - The CompensationRepository.create pattern: pre-check PK for idempotency, pre-check unique constraint, then insert with audit logging — follows the same read-before-write guard pattern as clock-in.
  - Last-write-wins pattern for update: no version check, just direct update with audit logging of old/new values.
---

## Tue Jul 14 2026 - US-015
Thread: unavailable
- Implemented session void (POST /api/sessions/{sessionId}/void) and unvoid (POST /api/sessions/{sessionId}/unvoid) endpoints.
- Created SessionVoid data class + SessionVoidTable Exposed model for the existing session_void table.
- SessionVoidRepository.void: uses insertIgnore for idempotent void creation (handles session_id UNIQUE constraint), writes audit log on insert.
- SessionVoidRepository.unvoid: updates unvoided_at/unvoided_by/unvoided_reason where unvoided_at IS NULL, writes audit log.
- SessionService.voidSession: checks VOID_SESSION capability (GLOBAL context), validates session exists (404 if not), returns existing if already voided (idempotent), calls assertEditable on branch day.
- SessionService.unvoidSession: checks VOID_SESSION capability, validates session exists, returns existing if already unvoided (idempotent), throws 404 if not voided at all, calls assertEditable.
- SessionRoutes: added POST /api/sessions/{sessionId}/void and POST /api/sessions/{sessionId}/unvoid routes with request body parsing.
- Added VoidSessionRequest, UnvoidSessionRequest, SessionVoidResponse DTOs to shared module.
- Added grantVoidSession to DatabaseTestHelper.
- Added 10 integration tests covering: void creates record, duplicate idempotent void, void without capability (403), void non-existent session (404), unvoid sets fields, unvoid idempotent, unvoid non-voided (404), unvoid without capability (403), unvoid non-existent session (404).
- Files changed: shared/.../dto/SessionDto.kt, repository/model/SessionVoid.kt (new), repository/SessionVoidRepository.kt (new), service/SessionService.kt, api/routes/SessionRoutes.kt, test/.../DatabaseTestHelper.kt, test/.../SessionServicePostgresTest.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 136 tests).
- **Learnings for future iterations:**
  - The session_void table has a UNIQUE constraint on session_id, so a session can only have one void row. Unvoiding is done by setting unvoided_at/unvoided_by/unvoided_reason on the same row (the unvoid_logic CHECK enforces all three or none).
  - VOID_SESSION capability is seeded in V2 and scoped to BRANCH in comments, but checked with GLOBAL context for consistency with other session endpoints (EDIT_BRANCH_DATA pattern).
  - When creating a new Exposed Table for an existing DB table, use `object TableName : Table("actual_table_name")` with `uuid()` for UUID columns and `text()` for TEXT columns.
  - For idempotent void, use insertIgnore with insertedCount check; for idempotent unvoid, check unvoidedAt != null and return existing.
  - SessionVoidTable and SessionVoidRepository follow the same pattern as other repository/model pairs, with `findByIdInTransaction` and `toSessionVoid()` helpers.
  - Always add new table deletions in test cleanup (SessionVoidTable.deleteAll()) to avoid FK violations across test runs.
---

## Sun Jul 12 2026 - US-012
Thread: unavailable
- Implemented client update (PATCH /api/clients/{clientId}) and anonymize (POST /api/clients/{clientId}/anonymize) endpoints.
- PATCH validates EDIT_BRANCH_DATA capability (GLOBAL context), applies partial updates for provided fields, validates gender/bp/blank-name rules, writes audit log with old/new first/last name.
- Anonymize requires EDIT_BRANCH_DATA capability, sets deleted_at=now(), nullifies all PII (firstName, lastName, middleName, suffix, phoneNumber, address, medicalConditions, systolicBp, diastolicBp) while retaining age and gender. Returns 404 if already anonymized (deletedAt already set). Writes audit log.
- Added UpdateClientRequest DTO to shared module, grantEditBranchData to DatabaseTestHelper.
- Added 11 integration tests covering: successful update, forbidden without capability, 404 not found, invalid gender, blank first name, partial BP rejection, audit log writing, PII nullification, anonymize without capability, anonymize non-existent, search exclusion after anonymize, findById still works for anonymized, idempotent anonymize returns 404.
- Files changed: shared/.../dto/ClientDto.kt, repository/ClientRepository.kt, service/ClientService.kt, api/routes/ClientRoutes.kt, test/.../DatabaseTestHelper.kt, test/.../ClientServicePostgresTest.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 105 tests).
- **Learnings for future iterations:**
  - ClientRepository.update uses Exposed DSL `update({ whereClause }) { conditional setters }` for partial updates — works well with nullable parameters for each field.
  - `auditLog` table may have FK to `app_user.changed_by` — always clean up `user_capability` and `audit_log` before `app_user` in integration test cleanup to avoid FK violations.
  - `UserCapabilityTable` has FK to `AppUserTable.id` — delete from `UserCapabilityTable` FIRST in cleanup.
  - `isNull()` on Exposed columns needs `org.jetbrains.exposed.sql.isNull` import in the file where it's used.
  - Detekt CyclomaticComplexMethod threshold is 15 — the `update` repository method with 11 conditional branches hits exactly 15, needs `@Suppress("CyclomaticComplexMethod")`.
---

## Sun Jul 12 2026 - US-018
Thread: unavailable
- Implemented session base rate management endpoints: POST /api/branches/{branchId}/rates (set rate with idempotency, MANAGE_PRODUCTS gate, deactivates previous overlapping rate) and GET /api/branches/{branchId}/rates (list active rates).
- Created SessionType enum in shared domain, SetRateRequest/RateResponse DTOs, SessionBaseRateTable Exposed model with customEnumeration for session_type PG enum, SessionBaseRateRepository (setRate with insertIgnore + deactivatePreviousRates + findActiveByBranch), SessionBaseRateService (MANAGE_PRODUCTS gate, deactivates previous rate, inserts new rate with far-future effective_until), SessionBaseRateRoutes (POST + GET).
- Wired in Main.kt, added grantManageProducts to DatabaseTestHelper.
- Added 8 integration tests covering: successful create with audit, forbidden without capability, invalid rate format, negative rate rejection, second rate deactivates first, multiple session types, duplicate idempotency, empty branch rates.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 113 tests).
- **Learnings for future iterations:**
  - `decimal("rate", precision, scale)` in Exposed maps to `BigDecimal`. Precision and scale must be extracted as `private const val` constants to satisfy detekt MagicNumber.
  - `insertIgnore` with `defaultExpression(CurrentTimestampWithTimeZone)` does NOT apply the default expression — must explicitly set the column value in the `insertIgnore` block.
  - PG enum columns for `session_type` use `customEnumeration` with PGobject pattern matching the existing convention.
  - The branch table has a UNIQUE constraint on `(branch_type, name)` — test branch names must be unique (e.g., include UUID suffix) to avoid collisions.
---
## Sun Jul 12 2026 - US-013
Thread: unavailable
- Implemented session create endpoint: POST /api/sessions body: { id, clientId, branchId, isWalkIn, requestedPractitionerId, finalPrice, remarks, otherConcerns, bookedAt, nextAppointmentDate }.
- Added pure-function SessionTypeAlgorithm: computeSessionType(branchType, priorNonMedicalMissionCount) with branching logic (MEDICAL_MISSION, PROVINCIAL_FIRST, REGULAR, SECOND_SESSION, SUBSEQUENT).
- SessionService.create: checks EDIT_BRANCH_DATA capability (GLOBAL), validates branch exists, idempotent check (returns existing session if same UUID), guards against duplicate PENDING sessions via hasActivePendingSession (409 Conflict), resolves/creates branch_day, counts prior non-MM sessions, snapshots base_price from active session_base_rate, inserts session with audit log.
- SessionTable Exposed model with customEnumeration for session_type and session_status PG enums, left join with active_session_voids view for prior session counting.
- Created files: shared/.../dto/SessionDto.kt, backend/.../repository/model/Session.kt (+ ActiveSessionVoidsView.kt), backend/.../repository/SessionRepository.kt, backend/.../service/SessionService.kt, backend/.../api/routes/SessionRoutes.kt, backend/.../service/SessionTypeAlgorithmTest.kt, backend/.../service/SessionServicePostgresTest.kt.
- Modified: Main.kt (wired SessionRoutes), scripts/ralph/prd.json (US-013 passes=true).
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 126 tests).
- **Learnings for future iterations:**
  - Exposed `deleteAll()` is available as extension function via `org.jetbrains.exposed.sql.deleteAll`.
  - Exposed `customEnumeration` must be called inline inside the `Table` object body, not from a companion/top-level function.
  - `insertIgnore` with `ON CONFLICT (id) DO NOTHING` only handles PK conflicts, NOT partial unique indexes — pre-checking in the service layer plus the PK idempotent check before the guard is the correct pattern.
  - `Query.forUpdate()` is not available in Exposed 0.61.0 — rely on unique indexes + pre-checks for concurrency instead.
  - Exposed `close()` on ResultRow needs explicit type param `this.get<EnumType>(column)` when column uses `customEnumeration`.
  - ktlint requires enum entries on separate lines.
  - For tests with PENDING guard, use `transaction { Table.update({ ... }) { it[status] = ... } }` to change status before creating a second session for the same client.
---
## Sun Jul 12 2026 - US-014
Thread: unavailable
- Implemented session status update endpoint: PATCH /api/sessions/{sessionId}/status body: { status, version }.
- SessionService.updateStatus: looks up session (404 if missing), checks version against session.version for optimistic locking (409 Conflict on mismatch), gates EDIT_BRANCH_DATA capability, calls BranchDayService.assertEditable on the session's branch day, validates walk-in sessions cannot transition to NO_SHOW or CANCELLED (400 Bad Request), then delegates to repository.
- SessionRepository.updateStatus: uses Exposed DSL `update` with combined `(id eq sessionId) and (version eq expectedVersion)` WHERE clause to atomically check version and set new status + version increment. Writes audit log with old/new sessionStatus values.
- Added UpdateSessionStatusRequest DTO to shared module.
- Wired PATCH route in SessionRoutes.kt (no Main.kt change needed — SessionRoutes already registered).
- Added 7 integration tests covering: PENDING→COMPLETED (version bump + audit), version mismatch (409), session not found (404), forbidden without capability (403), walk-in→NO_SHOW (400), walk-in→CANCELLED (400), audit entry written on update.
- Files changed: shared/.../dto/SessionDto.kt, repository/SessionRepository.kt, service/SessionService.kt, api/routes/SessionRoutes.kt, test/.../SessionServicePostgresTest.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Session `version` column can be incremented inside Exposed `update` block by setting `it[version] = expectedVersion + 1` (the WHERE clause already confirmed the current version matches, so this is safe).
  - Exposed DSL `update` needs `import org.jetbrains.exposed.sql.update` and works with combined conditions via `(col1 eq val1) and (col2 eq val2)` in the predicate lambda.
  - `AuditLogRepository.record` must be called inside the `transaction {}` block that performs the mutation (not outside) so the audit row commits atomically with the change.
  - The DB has `CONSTRAINT walk_in_status CHECK (NOT (is_walk_in = true AND session_status IN ('NO_SHOW', 'CANCELLED')))` — check this in the service layer for a cleaner error message before hitting the DB constraint.
  - `SessionService.updateStatus` returns `Session` (not `SessionCreateResult`) since the operation is always a mutation — no idempotent read path needed.
---

## Tue Jul 14 2026 - US-016
Thread: unavailable
- Implemented session practitioner management endpoints: POST /api/sessions/{sessionId}/practitioners, PATCH /api/sessions/{sessionId}/practitioners/{practitionerId}, DELETE /api/sessions/{sessionId}/practitioners/{practitionerId}.
- Created SessionPractitioner data class + SessionPractitionerTable Exposed model for the existing session_practitioner table.
- SessionPractitionerRepository.add: uses insertIgnore for idempotent add (handles session_id+practitioner_id UNIQUE constraint), snapshots slot_at_time from active UserBranchAssignment, increments session.version, writes audit log.
- SessionPractitionerRepository.updateRemarks: updates remarks column, increments session.version, writes audit log.
- SessionPractitionerRepository.remove: deletes row, increments session.version, writes audit log.
- SessionService.addPractitioner: checks EDIT_BRANCH_DATA capability (GLOBAL), validates session exists, calls assertEditable on branch day, snapshots slot from UserBranchAssignmentRepository.findActiveByBranchAndUser (defaults to 999 if no assignment).
- SessionService.updatePractitionerRemarks: checks EDIT_BRANCH_DATA, validates practitioner exists in session, delegates to repository.
- SessionService.removePractitioner: checks EDIT_BRANCH_DATA, validates practitioner exists, delegates to repository.
- SessionRoutes: added POST/PATCH/DELETE routes for session practitioner management with DTO parsing.
- Added AddPractitionerRequest, UpdatePractitionerRemarksRequest, SessionPractitionerResponse DTOs to shared module.
- Added 8 integration tests covering: add snapshots slot + audit, duplicate add idempotent, add without capability (403), add non-existent session (404), update remarks + version increment, update non-existent practitioner (404), remove + version increment, remove non-existent practitioner (404).
- Files changed: repository/model/SessionPractitioner.kt (new), repository/SessionPractitionerRepository.kt (new), service/SessionService.kt, api/routes/SessionRoutes.kt, shared/.../dto/SessionDto.kt, test/.../SessionServicePostgresTest.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 150 tests).
- **Learnings for future iterations:**
  - session_practitioner has UNIQUE (session_id, practitioner_id), so re-adding uses insertIgnore with idempotent pattern.
  - Slot at time is snapshotted from the practitioner's active user_branch_assignment at the session's branch. If no assignment exists, use a default slot (999).
  - Version increment on the session table uses read-then-write: select current version first, then update to currentVersion + 1. Column arithmetic (SessionTable.version + 1) is not supported in Exposed's update DSL.
  - When directly inserting into Exposed tables with FKs (.references()), use the repository's insertIgnore wrapper rather than a raw `Table.insert {}` in tests to avoid Postgres subquery issues.
  - deleteTestRows cleanup must clear SessionPractitionerTable and UserBranchAssignmentTable before other tables to avoid FK violations.
  - Session version increments atomically with each practitioner mutation (add, update remarks, remove).
---

## Tue Jul 14 2026 - US-021
Thread: unavailable
- Implemented product sale endpoint: POST /api/product-sales body: { id, branchDayId, sessionId?, clientId?, isWalkIn, productId, quantity, expectedVersion }.
- Created ProductSale data class + ProductSaleTable Exposed model for the existing product_sale table.
- ProductSaleRepository.sell: single transaction with idempotent check, inventory version-based optimistic locking, stock validation, branch_inventory atomic update, inventory_movement insert (reason=SALE, product_sale_id set, -quantity), product_sale insert with snapshotted product data, and audit logs.
- ProductSaleService.sell: EDIT_BRANCH_DATA capability gate (GLOBAL), assertEditable on branch day, validates sale type logic (session-linked vs walk-in vs anonymous), validates quantity >= 1, active product, and branch day existence. Commission recalculation trigger is stubbed (log line only, consistent with US-006/US-007 pattern).
- ProductSaleRoutes: registered POST /api/product-sales with response mapping.
- Added CreateProductSaleRequest/ProductSaleResponse DTOs to shared module.
- Added 12 integration tests covering: full sale with stock decrement + movement, forbidden without capability, non-existent branch day (404), non-existent product (404), insufficient stock (400), version mismatch (409), non-existent session (404), session-linked sale with clientId (400), quantity < 1 (400), idempotent duplicate, audit log writing, anonymous walk-in success.
- Files changed: shared/.../dto/ProductSaleDto.kt (new), repository/model/ProductSale.kt (new), repository/ProductSaleRepository.kt (new), service/ProductSaleService.kt (new), api/routes/ProductSaleRoutes.kt (new), test/.../ProductSaleServicePostgresTest.kt (new), Main.kt (wired routes), scripts/ralph/prd.json (US-021 passes=true), scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 209 tests).
- **Learnings for future iterations:**
  - The product_sale table has a CHECK constraint `sale_type_logic` that enforces specific combinations of session_id/client_id/is_walk_in. Validate these in the service layer before DB insert to provide cleaner error messages.
  - `commission_amount_at_time` is NUMERIC(15,4) in the DB schema, so when comparing in tests, use `compareTo` (which ignores scale) rather than `assertEquals` (which checks both value and scale).
  - For product sales, inventory optimistic locking uses the same version-based pattern as restock (read version, update WHERE version = expectedVersion, check update count).
  - Product sales snapshot product_name, unit_price_at_time, total_amount_at_time, and commission_amount_at_time at sale time — these don't change if the product is later updated.
  - The inventory_movement SALE constraint requires product_sale_id NOT NULL and quantity_change < 0 — wire these in the insert.
---
- Use `insert` for user_capability rows that always need a new row (no conflict expected); use `insertIgnore` for idempotent inserts with client-generated PKs.
- Follow the `val authorized = CapabilityService.hasCapability(...)` / `if (!authorized) throw ForbiddenResponse(...)` pattern for all capability checks.
- When revoking a delegate/access, update the associated `user_capability.valid_to = now()` to immediately expire the grant using Exposed DSL `update` with `and` conditions.
- Backend module: Kotlin (JVM) + Javalin + Exposed (DSL) + Flyway, package root `com.companyb.companyapp`. Layers: `api/routes`, `service`, `repository` (+ `repository/model` for Exposed `Table` objects), `auth`, `database`, `logging`.
- Quality gate (run per backend change): `./gradlew :backend:detekt :backend:ktlintCheck :backend:test`. ktlint auto-fix: `./gradlew :backend:ktlintFormat`.
- detekt `MagicNumber` is strict: extract any literal (column widths, pool sizes, durations) to a `private const val`. Constants are exempt via `ignoreConstantDeclaration`. `ReturnCount` max is 2 — match the existing pattern of `@Suppress("ReturnCount")` used in AuthService.
- detekt requires the `detekt-formatting` plugin (wired in root `build.gradle.kts` `subprojects { dependencies { add("detektPlugins", ...) } }`) because `detekt.yml` enables the `formatting` ruleset.
- Authorization MUST go through `CapabilityService.hasCapability(userId, capabilityCode, contextType, contextId)` which queries the `active_user_capabilities` SQL view. Never check roles directly in business logic. The view already filters INACTIVE users and time windows.
- For Postgres enum columns in raw SQL, bind params as text and cast in SQL (e.g. `?::capability_context_type`, `?::uuid`) via Exposed `transaction { exec(sql, args = listOf(TextColumnType() to value)) { rs -> ... } }`. Avoids Exposed PG-enum operator mismatches. This also applies to writes: Exposed `enumerationByName` binds varchar, so native enum updates need raw SQL casts like `SET status = ?::user_status`.
- Immediate revocation: `DenyList` (in-memory `ConcurrentHashMap<UUID, Instant>`) is checked inside `JwtService.verifyToken` BEFORE the DB `authorize` call. Populated at startup via `DenyList.loadInactiveUsers()` in `Main.kt`. Entries auto-evict after 24h (JWT max expiry).
- DB integration tests connect to a real Postgres instance via `DatabaseTestHelper.ensureDatabase()`. Run tests with `./gradlew :backend:test`.
- Exposed needs `exposed-java-time` dependency for `timestampWithTimeZone`/`CurrentTimestampWithTimeZone`.
- HTTP errors are signalled by throwing Javalin's built-in response exceptions from the service layer (`ForbiddenResponse`=403, `BadRequestResponse`=400, `NotFoundResponse`=404, `UnauthorizedResponse`=401). No custom exception hierarchy.
- Day state is lazy: reuse `BranchDayService.evaluateStatus(status, date, today)` — an OPEN day with date < today(Asia/Manila) is treated as PAST without a DB write. Call `BranchDayService.assertEditable(branchDayId, userId, reason?)` before any operational/financial write.
- For testable time logic, keep pure functions taking explicit `today`/inputs and have the public method fetch `LocalDate.now(zone)` then delegate (see BranchDayServiceTest, DenyListTest).
- Protected (authenticated) routes MUST be registered under `/api/*` — only that path is guarded by the `before("/api/*")` filter which sets `context.attribute("userId", <jwt-subject String>)`. Public routes (auth) have no prefix. Read caller id with `UUID.fromString(context.attribute<String>("userId"))`. Register each route object in `Main.initializeJavalin`.
- Audit logging: call `AuditLogRepository.record(tableName, recordId, action, changedBy, oldValue?, newValue?, reason?)` from inside the repository's `transaction {}` — it nests/joins the outer transaction so the audit row commits atomically with the change. Build JSON values with `AuditLogRepository.jsonField(key, value)`. `old_value`/`new_value` are JSONB bound as text with `?::jsonb`.
- GLOBAL-scoped capability checks use `CapabilityService.GLOBAL_CONTEXT_ID` (nil all-zero UUID) as the contextId with `CapabilityContextType.GLOBAL`.
- Kotlin file naming: detekt `MatchingDeclarationName` requires the file name to match its single top-level declaration (e.g. enum `AuditAction` must live in `AuditAction.kt`, not `AuditLog.kt`).

## Sat Jun 27 2026 - US-001
Thread: https://ampcode.com/threads/T-019f0759-389e-70e8-b4b8-15b6136bf4ea
- Implemented CapabilityService.hasCapability querying the active_user_capabilities view via CapabilityRepository (raw SQL with text params + PG enum/uuid casts).
- Implemented in-memory DenyList (ConcurrentHashMap<UUID, Instant>) with lazy 24h eviction; wired into JwtService.verifyToken (checked before DB) and populated at startup (Main.kt initializeDenyList + UserRepository.findInactiveUserIds).
- Added backend test source set (kotlin-test/junit) + DenyListTest (5 DB-free cases).
- Files changed: service/CapabilityService.kt, repository/CapabilityRepository.kt (new), auth/DenyList.kt (new), auth/JwtService.kt, repository/UserRepository.kt, Main.kt, repository/model/{CapabilityTable,UserCapability,AppUser}.kt, backend/build.gradle.kts, root build.gradle.kts (detekt-formatting), gradle/libs.versions.toml, backend/src/test/.../DenyListTest.kt.
- **Learnings for future iterations:**
  - detekt was previously unrunnable (invalid config: `formatting` ruleset without plugin). Fixed by adding `detekt-formatting` in root subprojects; then cleared ~20 pre-existing MagicNumber/ReturnCount/MatchingDeclarationName issues across auth/logging/database/model files. detekt is now a real, green gate for all future stories.
  - `active_user_capabilities` columns: user_id, capability_id, context_type, context_id, priority, source_type (no `code`); join `capability` on id to filter by code.
---

## Sat Jun 27 2026 - US-002
Thread: https://ampcode.com/threads/T-019f0766-99ca-7355-b0fc-75bf78eaeadc
- Implemented BranchDayService with resolveOrCreate (idempotent UPSERT) and assertEditable (lazy PAST evaluation, EDIT_PAST_DAY check, REMITTED reason requirement), plus expirationUtc 4 AM Manila boundary helper.
- Split throwing logic into pure, DB-free functions (evaluateStatus, assertEditableState, expirationUtc) so they are unit-testable without Postgres.
- Files changed: service/BranchDayService.kt (new), repository/BranchDayRepository.kt (new), repository/model/BranchDay.kt (new), test/.../service/BranchDayServiceTest.kt (new, 11 DB-free cases).
- **Learnings for future iterations:**
  - HTTP error mapping uses Javalin's built-in response exceptions thrown from the service layer: `ForbiddenResponse` (403), `BadRequestResponse` (400), `NotFoundResponse` (404), `UnauthorizedResponse` (401). No custom exception hierarchy exists — follow this pattern for all future endpoint validations.
  - `ON CONFLICT DO NOTHING` returns no row, so resolveOrCreate does the INSERT then a follow-up SELECT in the same `transaction {}` to read the row back (raw SQL + `?::uuid` / `?::date` text casts, per the established enum/uuid casting pattern).
  - EDIT_PAST_DAY capability is scoped to `BRANCH` context (contextId = branch_day.branch_id), per the V2 seed comments — not BRANCH_DAY.
  - Day state is lazy: an OPEN day whose calendar date < today(Asia/Manila) is treated as PAST without any DB write. The branch_day.status column is only physically flipped to PAST/REMITTED by other flows (e.g. remittance submission). Reuse `BranchDayService.evaluateStatus` rather than re-deriving this.
  - BranchTable Exposed model does not exist yet (US-004); BranchDayTable.branchId is a plain `uuid` column without `.references()` to avoid a premature dependency. Wire the FK reference when BranchTable lands if desired.
  - Pattern for testable time-dependent logic: keep pure functions that take `today: LocalDate` / explicit inputs, and have the public method fetch `LocalDate.now(manilaZone)` then delegate. Mirrors DenyListTest's `*At(now)` approach.
---

## Sat Jun 27 2026 - US-003
Thread: https://ampcode.com/threads/T-019f076a-6d0b-741a-bccc-0faeafe7a657
- Implemented user deactivation endpoint: PATCH /api/users/{userId}/deactivate.
- UserService.deactivate checks MANAGE_USERS on GLOBAL context, persists app_user.status=INACTIVE + audit entry atomically (UserRepository.deactivate), then adds the user to the in-memory DenyList for immediate token rejection. Throws 403 (no capability) / 404 (unknown user).
- Added reusable AuditLogRepository.record(...) (raw SQL, JSONB casts, joins enclosing transaction) + jsonField helper; AuditAction enum mirrors the Postgres audit_action type. These will be reused by every future mutating story.
- Added CapabilityService.GLOBAL_CONTEXT_ID (nil UUID) sentinel for GLOBAL-scoped capability checks.
- Files changed: service/UserService.kt (new), api/routes/UserRoutes.kt (new), repository/AuditLogRepository.kt (new), repository/model/AuditAction.kt (new), repository/UserRepository.kt (deactivate), service/CapabilityService.kt (GLOBAL_CONTEXT_ID), Main.kt (route wiring), backend/AGENTS.md, test/.../repository/AuditLogRepositoryTest.kt + service/CapabilityServiceTest.kt (new, DB-free).
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Protected routes go under /api/* (only path covered by the auth before-filter that sets the "userId" attribute). Public auth routes have no prefix. Wire new route objects in Main.initializeJavalin.
  - AuditLogRepository.record nests into the caller's open Exposed transaction (Exposed reuses the connection unless nested transactions are enabled), so audit + mutation are atomic when record is called inside the same transaction {} block.
  - GLOBAL capabilities: pass CapabilityContextType.GLOBAL + CapabilityService.GLOBAL_CONTEXT_ID. NOTE: role_capability seeds are NOT materialized into user_capability, and active_user_capabilities only reads user_capability — so capability checks only pass for users with explicit user_capability rows. A role->user_capability materialization step is still missing and will be needed for end-to-end auth (likely a future story / seeding concern).
  - detekt MatchingDeclarationName: file name must equal the single top-level declaration name (had to rename AuditLog.kt -> AuditAction.kt).
---

## Sun Jul 12 2026 - US-003 Postgres verification
Thread: unavailable
- Added Postgres-backed integration coverage for UserService.deactivate: MANAGE_USERS gate, persisted INACTIVE status, audit row old/new status, deny-list insertion, and existing-token rejection via JwtService.verifyToken.
- Fixed the live Postgres failure where Exposed attempted `UPDATE app_user SET status=?` with a varchar value against the native `user_status` enum; UserRepository.deactivate now uses raw SQL with `?::user_status`.
- Configured backend tests to run from the repo root so dotenv-kotlin can load the root `.env` during database integration tests.
- Files changed: backend/build.gradle.kts, repository/UserRepository.kt, service/UserServicePostgresTest.kt, backend/AGENTS.md, progress.txt.
- Quality gate green with Postgres available: `./gradlew :backend:detekt :backend:ktlintCheck :backend:test`.
- **Learnings for future iterations:**
  - Native Postgres enum writes must use explicit casts just like enum reads/checks; Exposed `enumerationByName` is not sufficient for updates to PG enum columns.
  - Backend integration tests that rely on `.env` need the Gradle test working directory set to the repo root.
---

## Sun Jul 12 2026 - US-004
Thread: https://ampcode.com/threads/T-019f076a-6d0b-741a-bccc-000000000000
- Branch CRUD endpoints already implemented prior to this iteration: BranchTable model, BranchRepository (idempotent ON CONFLICT DO NOTHING insert with audit), BranchService (MANAGE_USERS gate on GLOBAL context), BranchRoutes (POST /api/branches, GET /api/branches, GET /api/branches/{branchId}).
- Wired in Main.kt and verified full compilation + quality gate (detekt, ktlint, test all green).
- Files already existed: repository/model/Branch.kt, repository/BranchRepository.kt, service/BranchService.kt, api/routes/BranchRoutes.kt, shared/.../BranchType.kt, shared/.../BranchDto.kt, test/.../BranchServicePostgresTest.kt.
- **Learnings for future iterations:**
  - The branch table migration (V1) already defines `branch_type` as a native PG enum with values CLINIC, PROVINCIAL_TOUR, MEDICAL_MISSION. Raw SQL casts with `?::branch_type` are required for inserts.
  - Backend compilation requires the `shared` module to compile first (it contains BranchType enum and DTOs). Gradle handles this automatically via the `:shared` dependency.
  - Idempotent creation pattern: use `ON CONFLICT (id) DO NOTHING` + separate SELECT in same transaction, with `AuditLogRepository.record` only on actual insert.
---

## Tue Jul 14 2026 - US-028
Thread: unavailable
- Implemented remittance line and day breakdown management endpoints: POST /api/remittances/{remittanceId}/lines, DELETE /api/remittances/{remittanceId}/lines/{lineId}, POST /api/remittances/{remittanceId}/day-breakdowns, GET /api/remittances/{remittanceId} (with line aggregates).
- Created RemittanceLine data class + RemittanceLineTable Exposed model with customEnumeration for remittance_line_type PG enum (SESSION, PRODUCT_SALE), soft-delete support (deleted_by, deleted_at), and line_type_logic constraint enforcement.
- Created RemittanceDayBreakdown data class + RemittanceDayBreakdownTable Exposed model for the existing remittance_day_breakdown join table.
- RemittanceLineRepository.addLine: idempotent insert with atomic remittance version increment (version-based optimistic locking), audit logging.
- RemittanceLineRepository.softDeleteLine: sets deleted_at/deleted_by with atomic version increment, idempotent on already-deleted lines.
- RemittanceDayBreakdownRepository.addDayBreakdown: idempotent insert with UNIQUE (remittance_id, branch_day_id) constraint.
- RemittanceRepository: added public findById and removed standalone incrementVersion (version increment is now atomic inside line repository methods).
- RemittanceService.addLine/removeLine: gates on SUBMIT_REMITTANCE (GLOBAL context), validates DRAFT status, enforces line type constraints (SESSION requires sessionId and null productSaleId, PRODUCT_SALE vice versa), wraps version mismatch as ConflictResponse (409).
- RemittanceService.addDayBreakdown: gates on SUBMIT_REMITTANCE, validates DRAFT status, validates branch day exists.
- RemittanceService.getRemittance: loads remittance with non-deleted lines, day breakdowns, and total amount aggregate.
- RemittanceRoutes: added 4 new endpoints under /api/remittances/{remittanceId}/*.
- Added RemittanceDto.kt DTOs: CreateRemittanceLineRequest, RemittanceLineResponse, AddDayBreakdownRequest, RemittanceDayBreakdownResponse, RemittanceDetailResponse.
- Added 24 integration tests covering: SESSION/PRODUCT_SALE line types, duplicate idempotency, capability gating, DRAFT-only validation, line type constraints, soft-delete, version increment, day breakdown uniqueness, aggregate total calculation, deleted line exclusion from totals, audit log writing.
- Files changed: 9 files total
  - NEW: repository/model/RemittanceLine.kt, repository/model/RemittanceDayBreakdown.kt, repository/RemittanceLineRepository.kt, repository/RemittanceDayBreakdownRepository.kt, test/.../RemittanceLineServicePostgresTest.kt
  - MODIFIED: repository/RemittanceRepository.kt, service/RemittanceService.kt, shared/.../dto/RemittanceDto.kt, api/routes/RemittanceRoutes.kt
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Remittance line management uses atomic version increment: the line mutation and remittance version increment happen in the same transaction to avoid TOCTOU races.
  - `insertIgnore` with `autoGenerate()` PK can cause SQL errors in Exposed 0.61.0 when setting the PK explicitly; prefer using repository methods (Repository.create) when available for inserts.
  - The remittance_line table has a UNIQUE partial index on session_id WHERE deleted_at IS NULL — two non-deleted lines cannot reference the same session or product sale.
  - Soft-delete on remittance_line follows the same pattern as expense: set deleted_by and deleted_at, don't remove the row.
  - Day breakdown uniqueness is enforced by DB UNIQUE (remittance_id, branch_day_id) constraint AND service-level pre-check for cleaner error messages.
  - When testing with inventory/branch_inventory, set up the card with the correct version by using BranchInventoryRepository.ensureCard + direct stock update, then pass the correct expectedVersion to sell.
---

## Sun Jul 12 2026 - US-011
Thread: unavailable
- Implemented client create and search endpoints: POST /api/clients (idempotent create with bp_both_or_none validation), GET /api/clients?q= (fuzzy name + phone prefix search), GET /api/clients/{clientId} (retrieve by id).
- Added V4 Flyway migration to enable pg_trgm extension for future trgm index support.
- ClientService.create validates firstName/lastName required, gender must be 'M' or 'F', and bp_both_or_none constraint.
- ClientRepository uses insertIgnore for idempotent creates with audit logging, and LIKE-based search with 20-result limit.
- All endpoints are open to authenticated users (no capability gate) per the "as a practitioner" story description.
- Files changed: resources/db/migration/V4__enable_pg_trgm.sql (new), repository/model/Client.kt (new), repository/ClientRepository.kt (new), service/ClientService.kt (new), api/routes/ClientRoutes.kt (new), shared/.../dto/ClientDto.kt (new), Main.kt (wired routes), scripts/ralph/prd.json (passes=true), scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - `like` infix function works in Exposed `where {}` blocks without explicit import (it's available on the `SqlExpressionBuilder` receiver, just like `eq`).
  - Exposed `char()` returns `Column<Char>`, not `Column<String>`. Use `varchar("col", 1)` instead to keep String-based DTO compatibility.
  - `timestampWithTimeZone` columns return `OffsetDateTime` (from exposed-java-time), NOT `Instant`. Use `OffsetDateTime` in data classes.
  - Exposed `limit(n)` works on Query objects for pagination.
  - Set `@Suppress("ThrowsCount")` on service methods with >2 throw statements (established pattern from AuthService).
  - Added ClientServicePostgresTest (15 test cases): create, duplicate idempotency, bp_both_or_none validation, gender validation, name validation, findById, NotFoundResponse, search by firstName/lastName/phone prefix, empty query rejection, case-sensitive LIKE.
---

## Sun Jul 12 2026 - US-005
Thread: https://ampcode.com/threads/T-019f076a-6d0b-741a-bccc-000000000005
- Implemented user branch assignment endpoints: POST /api/branches/{branchId}/assignments (create with idempotency and MANAGE_USERS gate), GET /api/branches/{branchId}/assignments (list active assignments), DELETE /api/branches/{branchId}/assignments/{userId} (soft-delete by setting ended_at=now()), PATCH /api/branches/{branchId}/assignments/{userId}/slot (update slot; self-service or MANAGE_USERS), POST /api/branches/{branchId}/slots/swap (atomic slot swap in single transaction). All mutations write audit log entries.
- Files changed: repository/model/UserBranchAssignment.kt (new), repository/UserBranchAssignmentRepository.kt (new), service/UserBranchAssignmentService.kt (new), api/routes/UserBranchAssignmentRoutes.kt (new), shared/.../BranchDto.kt (added DTOs), Main.kt (wired routes), scripts/ralph/prd.json (set passes=true).
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - `timestampWithTimeZone` columns cannot use `.autoGenerate()` — use `.defaultExpression(CurrentTimestampWithTimeZone)` instead.
  - detekt `LongMethod` and `ThrowsCount` are active on route registration and service methods; use `@Suppress("LongMethod", "ThrowsCount")` on routes and `@Suppress("ThrowsCount")` on service methods.
  - Slot swaps run inside `transaction {}` wrapper to let the repo use `TransactionManager.current()`.
  - The `user_branch_assignment` table has `idx_one_active_assignment` partial unique index — the service layer checks for existing active assignments before insert.

## Sun Jul 12 2026 - US-006
Thread: https://ampcode.com/threads/T-019f076a-6d0b-741a-bccc-000000000006
- Implemented clock-in endpoint: POST /api/attendance/clock-in body: { id, branchId }.
- AttendanceRepository.clockIn handles the attendance INSERT with ON CONFLICT (id) DO NOTHING (idempotency) plus branch_day_assignment upsert in a single transaction.
- AttendanceService.clockIn resolves/creates the branch day, checks for existing active clock-in (throws 409 Conflict if found, preventing idx_one_active_clock_in violation), determines is_relief by checking active user_branch_assignment, and writes audit log entry.
- Commission recalculation trigger is stubbed (no DB function exists yet for the commission engine — will be wired in a later story).
- All quality checks pass (detekt, ktlint, test).
- Files changed: shared/.../dto/AttendanceDto.kt (new), repository/model/Attendance.kt (new), repository/AttendanceRepository.kt (new), service/AttendanceService.kt (new), api/routes/AttendanceRoutes.kt (new), Main.kt (wired AttendanceRoutes), backend/AGENTS.md (added Clock-in/Attendance section), scripts/ralph/prd.json (US-006 passes=true), scripts/ralph/progress.txt.
- **Learnings for future iterations:**
  - `ON CONFLICT (id) DO NOTHING` with `RETURNING id` returns zero rows (not the conflicting row) when the id already exists; detect via empty ResultSet rather than a special UUID sentinel.
  - Clock-in is open to all authenticated users (no capability gate) — only relief determination logic is needed.
  - Use `io.javalin.http.ConflictResponse` (409) for unique-index conflicts (active clock-in) thrown from the service layer.
   - `hasActiveClockIn` pre-check is a read-before-write guard; the partial unique index `idx_one_active_clock_in` is the DB-level backstop.
   - Commission recalculation trigger is described in the PRD Engine 1 but has no DB function or Kotlin implementation yet — stub with a log line until the engine is built (likely US-021).
---

## Sun Jul 12 2026 - US-007
Thread: https://ampcode.com/threads/T-019f076a-6d0b-741a-bccc-000000000007
- Implemented clock-out endpoint: POST /api/attendance/clock-out body: { attendanceId }.
- AttendanceService.clockOut looks up attendance by ID (throws 404 if not found), returns existing with HTTP 200 if already clocked out (idempotent), otherwise updates clock_out=now() and writes audit log.
- AttendanceRepository.clockOut uses Exposed DSL `update` with `(id eq attendanceId) and (clockOut.isNull())` conditions.
- Added ClockOutRequest/ClockOutResponse DTOs, branchDayAssignmentIsRelief query for response isRelief field.
- Registered AttendanceRoutes.clockOut in Main.kt.
- Files changed: shared/.../dto/AttendanceDto.kt, repository/AttendanceRepository.kt, service/AttendanceService.kt, api/routes/AttendanceRoutes.kt, Main.kt, backend/AGENTS.md, scripts/ralph/prd.json.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Clock-out uses `Table.update({ whereClause })` with combined conditions via `and` and `clockOut.isNull()` guard to ensure only null clock_out rows are updated. The same read-after-update pattern (select by id after update) is used.
  - `findById` with `singleOrNull` + `.toAttendance()` pattern works for repository lookups.
  - For idempotent reads (already clocked out), return the existing record without audit log — only log the actual UPDATE mutation.
   - `isRelief` lives in `branch_day_assignment`, not `attendance` — need to query separately via `branchDayAssignmentIsRelief(branchDayId, userId)`.
---

## Sun Jul 12 2026 - US-008
Thread: https://ampcode.com/threads/T-019f076a-6d0b-741a-bccc-000000000008
- Implemented relief access request endpoint: POST /api/relief-access/request body: { requestId, branchDayId, targetUserId }.
- Validates target user has active clock-in on branch day (throws 400 if not), validates requester has is_relief=true in branch_day_assignment (throws 403 if not), persists request as PENDING via idempotent insertIgnore.
- Files changed: repository/model/ReliefStatus.kt (new enum), repository/model/ReliefAccess.kt (new data class + Exposed Table), repository/ReliefAccessRepository.kt (new), service/ReliefAccessService.kt (new), api/routes/ReliefAccessRoutes.kt (new), shared/.../dto/AttendanceDto.kt (added ReliefAccessRequest/Response DTOs), Main.kt (wired routes), scripts/ralph/prd.json (passes=true), scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Use `GrantReliefAccessTable` Exposed object with `customEnumeration` for `relief_status` PG enum via `PGobject`.
  - `insertIgnore` with `insertedCount` works for idempotent inserts on `grant_relief_access` table.
  - Reuse AttendanceTable DSL queries for active clock-in checks rather than raw SQL.
---

## Sun Jul 12 2026 - US-009
Thread: https://ampcode.com/threads/T-019f076a-6d0b-741a-bccc-000000000009
- Implemented relief access grant/deny endpoints: PATCH /api/relief-access/{requestId}/grant and PATCH /api/relief-access/{requestId}/deny.
- Grant uses `ReliefAccessRepository.grantWithCapability` to atomically update the relief request status to GRANTED (with granted_by/granted_at) and insert a `user_capability` row (source_type=RELIEF_ACCESS, context_type=BRANCH_DAY, valid_to=4AM Manila next day, priority=10) in a single transaction.
- Added `CapabilityRepository.findIdByCode` for looking up capability IDs (used to fetch EDIT_BRANCH_DATA for the grant).
- Duplicate grants for the same (requested_by, branch_day_id) pair silently return the existing GRANTED row (HTTP 200).
- Deny checks the request is still PENDING (cannot deny an already GRANTED request).
- Both grant and deny validate caller is the request's target_user (throw 403 if not).
- Files changed: repository/CapabilityRepository.kt, repository/ReliefAccessRepository.kt, service/ReliefAccessService.kt, api/routes/ReliefAccessRoutes.kt, Main.kt, test/.../service/ReliefAccessServicePostgresTest.kt, backend/AGENTS.md, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - For atomic multi-table mutations (update relief request + insert user_capability), use a single repository method wrapping both operations in one `transaction {}` block.
  - `CapabilityRepository.findIdByCode` is useful for any story that needs to insert a `user_capability` row (e.g. US-010 medical mission delegates).
  - When working around detekt's `ReturnCount` on guard-clause-heavy methods, use `@Suppress("ReturnCount")` (already established pattern).
  - `grantWithCapability` triggers detekt `LongParameterList` (8 params); suppress with `@Suppress("LongParameterList")`.
---

## Sun Jul 12 2026 - US-010
Thread: unavailable
- Implemented medical mission delegate assign (POST /api/delegates) and revoke (DELETE /api/delegates/{delegateId}) endpoints.
- assignWithCapability: single transaction inserts medical_mission_delegate row (idempotent via insertIgnore) + user_capability with source_type=MEDICAL_MISSION_DELEGATE, context_type=BRANCH, priority=20, valid_to=NULL for EDIT_BRANCH_DATA capability.
- revokeWithCapability: single transaction sets ended_at=now() on delegate and valid_to=now() on the associated user_capability.
- Added V3 Flyway migration to add branch_id column to medical_mission_delegate (required for capability scoping).
- ASSIGN_DELEGATE capability check (GLOBAL context) guards both endpoints.
- Files changed: resources/db/migration/V3__add_branch_id_to_medical_mission_delegate.sql (new), repository/model/MedicalMissionDelegate.kt (new), repository/MedicalMissionDelegateRepository.kt (new), service/MedicalMissionDelegateService.kt (new), api/routes/MedicalMissionDelegateRoutes.kt (new), shared/.../dto/DelegateDto.kt (new), Main.kt (wired routes), scripts/ralph/prd.json (passes=true), scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Use `insert` (not `insertIgnore`) for user_capability rows — there's no expected conflict and we always want a new row. `insertIgnore` is reserved for idempotent inserts with client-generated PKs.
  - Medical mission delegate assign creates a permanent (valid_to=NULL) EDIT_BRANCH_DATA grant scoped to the mission branch; revoke expires it immediately (valid_to=now()).
  - Priority 20 ensures MEDICAL_MISSION_DELEGATE grants outrank RELIEF_ACCESS (priority 10) in active_user_capabilities ordering.
   - The medical_mission_delegate table in V1 lacked branch_id — added via V3 migration. Future FK-sensitive stories should check the schema first.
---

## Tue Jul 14 2026 - US-020
Thread: unavailable
- Implemented branch inventory management and restock endpoints: POST /api/branches/{branchId}/inventory (create inventory card, MANAGE_PRODUCTS gate), POST /api/branches/{branchId}/inventory/{productId}/restock (idempotent restock with optimistic locking and audit log), GET /api/branches/{branchId}/inventory (list stock counts with product names).
- Created BranchInventory data class + BranchInventoryTable Exposed model for the existing branch_inventory table (UUID PK, branch_id FK, product_id FK, current_stock, version).
- Created InventoryMovement data class + InventoryMovementTable Exposed model for the existing inventory_movement table (customEnumeration for inventory_movement_reason PG enum).
- BranchInventoryRepository: ensureCard (idempotent UPSERT, returns existing card), restock (version-checked atomic stock update + movement insert + audit logs in single transaction), findByBranch (inner join with product for product_name).
- BranchInventoryService: ensureCard (MANAGE_PRODUCTS on GLOBAL, validates branch/product exist), restock (MANAGE_PRODUCTS gate, assertEditable on branch_day, optimistic locking with ConflictResponse on version mismatch), findByBranch (validates branch exists).
- BranchInventoryRoutes: wired POST + GET endpoints under /api/branches/{branchId}/inventory/*.
- Added InventoryDto.kt to shared module: AddInventoryCardRequest, RestockRequest, BranchInventoryResponse, InventoryMovementResponse.
- Wired BranchInventoryRoutes in Main.kt.
- Added 11 integration tests covering: ensureCard creates card, ensureCard without capability (403), non-existent branch/product (404), restock increments stock + logs movement, restock without capability (403), negative quantity (400), non-existent branch (404), audit log writing, list by branch (empty), list by non-existent branch (404).
- Files changed: shared/.../dto/InventoryDto.kt (new), backend/.../repository/model/Inventory.kt (new), backend/.../repository/BranchInventoryRepository.kt (new), backend/.../service/BranchInventoryService.kt (new), backend/.../api/routes/BranchInventoryRoutes.kt (new), backend/.../Main.kt, backend/.../test/.../BranchInventoryServicePostgresTest.kt (new), scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 197 tests).
- **Learnings for future iterations:**
  - The branch_inventory table has UNIQUE (branch_id, product_id) — ensureCard checks for existing before insertIgnore.
  - Optimistic locking on branch_inventory uses a version column. The restock method compares expectedVersion against the current version, then checks update count (0 means version mismatch).
  - `findCardInTransaction` is a package-private helper that requires an active transaction context (called from inside transaction {} blocks only). Never call it from the service layer without a transaction wrapper.
  - For version-based optimistic locking with Exposed, the pattern is: SELECT current version, then UPDATE WHERE version = expectedVersion. If updatedCount == 0, throw 409 Conflict.
  - The innerJoin with ProductTable provides product_name in the inventory listing — follows the same pattern as other Exposed innerJoin usages.
  - Audit logs are written inside the same transaction as the mutation so they commit atomically.
  - Detekt `LongMethod` threshold is 60 — the restock repository method with audit logging hits it; suppress with @Suppress("LongMethod").
  - ktlint requires multiline expression format: `val x = expression \n .chain()` not `val x = expression.chain()` when chaining.
---

## Tue Jul 14 2026 - US-017
Thread: unavailable
- Implemented session concerns endpoints: GET /api/concerns (list all concerns), GET /api/sessions/{sessionId}/concerns, POST /api/sessions/{sessionId}/concerns, DELETE /api/sessions/{sessionId}/concerns/{concernId}, POST /api/sessions/{sessionId}/promote-concern.
- Created Concern data class + ConcernTable + SessionConcernTable Exposed models for the existing concern and session_concern tables.
- ConcernRepository: findAll, findById, create (idempotent insertIgnore), addToSession (insertIgnore join table), removeFromSession (deleteWhere), getConcernsForSession (innerJoin).
- ConcernService: listAll (no gate, all authenticated users), addToSession/removeFromSession (EDIT_BRANCH_DATA gate + assertEditable), promoteConcern (creates concern + links to session + nullifies session.otherConcerns in single flow).
- SessionRepository: added updateOtherConcerns method to clear session.other_concerns when promoting a concern, with audit logging.
- ConcernRoutes added to SessionRoutes.kt (all routes registered there; no Main.kt changes needed since SessionRoutes is already wired).
- Added ConcernDto.kt to shared module: ConcernResponse, AddSessionConcernRequest, PromoteConcernRequest.
- Added 13 integration tests covering: list concerns, add to session (success/audit/idempotent/403/404), remove from session (success/audit/403/404), promote concern (creates+links+nullifies, discoverable, 403, audit).
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 163 tests).
- **Learnings for future iterations:**
  - Concern and session_concern tables exist in V1 schema but had no Exposed models — created ConcernTable (id, label, createdBy, createdAt) and SessionConcernTable (sessionId, concernId) with appropriate PK definitions.
  - The concern table has created_by REFERENCES app_user(id) — use insertIgnore for idempotent concern creation, set createdBy only on actual insert.
  - Promote concern nullifies session.other_concerns — the session_concern table is the normalized alternative to the free-text field.
  - `eq` needs explicit import as `org.jetbrains.exposed.sql.SqlExpressionBuilder.eq` when used inside `deleteWhere` blocks (same pattern as SessionServicePostgresTest).
  - Inner join in Exposed: `Table.join(otherTable, JoinType.INNER, thisColumn, otherColumn)` — import `org.jetbrains.exposed.sql.JoinType`.
  - Audit entries for session_concern join table use sessionId as the recordId.
  - Cleanup order: SessionConcernTable before SessionTable, ConcernTable before AppUserTable (FK constraint concern.created_by references app_user.id).
- Files changed: 7 files total
  - NEW: backend/.../repository/model/Concern.kt, backend/.../repository/ConcernRepository.kt, backend/.../service/ConcernService.kt, shared/.../dto/ConcernDto.kt, backend/.../test/.../service/ConcernServicePostgresTest.kt
  - MODIFIED: backend/.../repository/SessionRepository.kt, backend/.../api/routes/SessionRoutes.kt
- Updated prd.json (US-017 passes=true), progress.txt
---

## Tue Jul 14 2026 - US-019
Thread: unavailable
- Implemented product category and product CRUD endpoints.
- ProductCategory: POST /api/product-categories (idempotent create with MANAGE_PRODUCTS gate), GET /api/product-categories (list all), GET /api/product-categories/{categoryId} (find by id).
- Product: POST /api/products (idempotent create with MANAGE_PRODUCTS gate, validates category exists), GET /api/products (filters active only), GET /api/products/{productId}, PATCH /api/products/{productId} (partial update, supports deactivation).
- Created ProductCategory data class + ProductCategoryTable Exposed model for the existing product_category table.
- Created Product data class + ProductTable Exposed model for the existing product table (decimal(10,2) for unit_price and commission_amount).
- ProductCategoryRepository/ProductRepository: insertIgnore for idempotent creates, update for product partial updates, findById/findAll/findAllActive queries.
- ProductCategoryService/ProductService: MANAGE_PRODUCTS capability gate (GLOBAL context), validation (blank names, non-negative prices, category existence).
- ProductCategoryRoutes/ProductRoutes: registered in Main.kt with POST/GET/PATCH endpoints, DTO parsing with error handling.
- Added CreateProductCategoryRequest/ProductCategoryResponse to shared ProductCategoryDto.kt.
- Added CreateProductRequest/UpdateProductRequest/ProductResponse to shared ProductDto.kt.
- Added 5 integration tests for product categories (create, duplicate idempotent, list, findById, forbidden).
- Added 6 integration tests for products (create with audit, duplicate idempotent, list active, findById, update with deactivation, forbidden, bad request for non-existent category).
- Files changed: shared/.../dto/ProductCategoryDto.kt (new), shared/.../dto/ProductDto.kt (new), backend/.../repository/model/Product.kt (new), backend/.../repository/ProductCategoryRepository.kt (new), backend/.../repository/ProductRepository.kt (new), backend/.../service/ProductCategoryService.kt (new), backend/.../service/ProductService.kt (new), backend/.../api/routes/ProductCategoryRoutes.kt (new), backend/.../api/routes/ProductRoutes.kt (new), backend/.../test/.../ProductCategoryServicePostgresTest.kt (new), backend/.../test/.../ProductServicePostgresTest.kt (new), backend/.../Main.kt (register routes), scripts/ralph/prd.json (US-019 passes=true), scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Product table uses NUMERIC(10,2) for unit_price and commission_amount — use `decimal("col", 10, 2)` in Exposed with `BigDecimal` in data classes.
  - The product_category.name has a UNIQUE constraint — `insertIgnore` handles idempotent inserts but the UNIQUE constraint on name will throw on duplicate names with different IDs.
  - The product table has `is_active BOOLEAN NOT NULL DEFAULT true` — for deactivation, set isActive to false via PATCH.
  - The `product.inventory` table (branch_inventory) references product and branch — to be created in US-020.
  - When cleaning up integration tests, delete from ProductTable before ProductCategoryTable due to FK constraint.
  - detekt LongParameterList threshold is 6 — ProductService.create (6 params) and update (7 params) both need @Suppress("LongParameterList").
  - detekt ThrowsCount threshold is 2 — ProductRoutes.register needs @Suppress("ThrowsCount").
---

## Tue Jul 14 2026 - US-022
Thread: unavailable
- Implemented non-sale inventory movement endpoint: POST /api/branches/{branchId}/inventory/{productId}/movement.
- Added InventoryMovementRequest DTO to shared module with movementId, reason, quantityChange, notes, branchDayId, expectedVersion fields.
- BranchInventoryRepository.recordMovement: version-based optimistic locking (checks expectedVersion, updates stock + increments version, throws version_mismatch on conflict), uses insertIgnore for idempotent movement creation, writes audit log for inventory card update and movement insert.
- BranchInventoryService.recordMovement: gates on MANAGE_PRODUCTS capability (GLOBAL context), validates TESTER/SAMPLE/MISSING must have negative quantityChange (throws 400), mandates non-blank notes for MISSING (throws 400), allows ADJUSTMENT with either sign, validates branch/product exist (throws 404), calls assertEditable on branchDayId, ensures inventory card exists, delegates to repository with version handling (throws 409 on mismatch).
- BranchInventoryRoutes: added POST movement route under /api/branches/{branchId}/inventory/{productId}/movement with UUID parsing for movementId/branchDayId and InventoryMovementReason enum parsing.
- Updated InventoryMovement.toResponse() to emit actual notes field instead of hardcoded null.
- Modified files: shared/.../dto/InventoryDto.kt, repository/BranchInventoryRepository.kt, service/BranchInventoryService.kt, api/routes/BranchInventoryRoutes.kt, test/.../BranchInventoryServicePostgresTest.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - The non-sale movement endpoint reuses the same version-based optimistic locking pattern as restock (read version, update WHERE version = expectedVersion, check update count).
  - Movement sign validation per reason: TESTER/SAMPLE/MISSING must be negative, ADJUSTMENT allows either sign. Validate in the service layer before repository call.
  - MISSING movements require mandatory non-blank notes; throw 400 if blank/null.
  - InventoryMovementReason enum values are parsed from the request string in the route layer via `InventoryMovementReason.valueOf(request.reason.uppercase())` — uppercase is used for case-insensitivity.
  - The InventoryMovementResponse.notes field was previously hardcoded to null in the route's `toResponse()` — now correctly maps the data class notes value.
---

## Tue Jul 14 2026 - US-024
Thread: unavailable
- Implemented commission manual inclusion (POST /api/commission-inclusions) with upsert over (product_sale_id, user_id) unique key and commission split read endpoint (GET /api/commission-splits/{branchDayId}).
- Created CommissionManualInclusion data class + CommissionManualInclusionTable and CommissionSplit data class + CommissionSplitTable Exposed models.
- CommissionManualInclusionRepository.upsert: checks existing by (product_sale_id, user_id), updates is_included/reason/assigned_by if found, inserts new row if not found, writes audit log for both INSERT and UPDATE paths.
- CommissionSplitRepository.replaceForBranchDay: deletes all existing splits for a branch_day, then batch inserts new splits (used by the commission engine).
- CommissionEngineService.recalculate: implements Engine 1 (commission recalculation) — guard on OPEN branch day, fetch non-voided product sales, find clocked-in users at sold_at time, apply manual inclusions/exclusions, split commission equally using BigDecimal with HALF_UP rounding, write per-user totals to commission_split.
- CommissionManualInclusionService.create: ASSIGN_COMPENSATION capability gate (GLOBAL context), validates product sale exists, triggers CommissionEngineService.recalculate(sale.branchDayId) after upsert.
- CommissionSplitService.getByBranchDayId: VIEW_BRANCH_DATA capability gate (BRANCH context).
- Added grantCapability helper to DatabaseTestHelper (generic method for any capability code/context).
- Added findNonVoidedSalesByBranchDay to ProductSaleRepository (LEFT JOIN with SessionTable + ActiveSessionVoidsView to exclude voided session sales).
- Added findUsersClockedInAt to AttendanceRepository (finds users clocked in during a specific time window).
- CommissionRoutes: registered POST /api/commission-inclusions and GET /api/commission-splits/{branchDayId} in Main.kt.
- DTOs: CreateCommissionInclusionRequest, CommissionInclusionResponse, CommissionSplitResponse in shared module.
- Added 11 integration tests covering: create with all fields, create without reason, create exclusion, upsert update, forbidden without capability, non-existent product sale (404), commission recalculation triggers split creation, get splits returns correct data, get splits forbidden without VIEW_BRANCH_DATA, get splits non-existent branch day (404), audit log writing.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 232 tests).
- **Learnings for future iterations:**
  - `commission_manual_inclusion` has UNIQUE (product_sale_id, user_id) — the upsert pattern checks existing by unique key first before insert/update decision.
  - `commission_split` has UNIQUE (branch_day_id, user_id) — the engine replaces all splits for a day atomically (delete all + batch insert).
  - Commission Engine 1: find all non-voided product sales for the branch_day, find clocked-in users at sold_at time, apply manual inclusions/exclusions, split commission_amount * quantity equally among eligible users using BigDecimal division with HALF_UP rounding, accumulate per-user totals.
  - `leftJoin` on a `Table` with 3 args works via top-level extension function, but chaining another `leftJoin` on the resulting `Join` only supports the 1-arg infix form — use `join` with explicit `JoinType.LEFT` and 6 args instead.
  - `isNull()` is a member of `ISqlExpressionBuilder` — available without explicit import inside `where { }` blocks.
  - The `priority` column in `UserCapabilityTable` is `short` type — pass `.toShort()` when inserting.
  - VIEW_BRANCH_DATA capability is scoped to BRANCH context in the seed — use `CapabilityContextType.BRANCH` with `branchId` as contextId for read endpoints.
  - ASSIGN_COMPENSATION is used for commission manual inclusion gating (consistent with US-023 pattern).
---

## Tue Jul 14 2026 - US-025
Thread: unavailable
- Implemented Expense CRUD endpoints: POST /api/expenses (create with idempotent PK, EDIT_BRANCH_DATA gate, assertEditable), DELETE /api/expenses/{expenseId} (soft-delete with reason, EDIT_BRANCH_DATA gate, assertEditable), GET /api/expenses?branchDayId= (list expenses, validates branch day exists).
- Created ExpenseCategory enum (PANTRY, COMMUNICATION, WATER, TRANSPORTATION, ELECTRICITY, RENTAL, OFFICE_SUPPLIES, FURNITURE_FIXTURES, MISCELLANEOUS) matching PG expense_category enum.
- Created Expense data class + ExpenseTable Exposed model with customEnumeration for expense_category PG enum.
- ExpenseRepository.create: idempotent PK pre-check (returns existing if same UUID), inserts with audit log.
- ExpenseRepository.softDelete: sets deleted_by/deleted_at, writes audit log with old/new values and reason.
- ExpenseRepository.findByBranchDayId: lists all expenses for a branch day (including soft-deleted), no filtering.
- ExpenseService.create: gates on EDIT_BRANCH_DATA (GLOBAL context), validates amount > 0, idempotent check before assertEditable.
- ExpenseService.softDelete: gates on EDIT_BRANCH_DATA, validates reason not blank, validates expense exists (404 if not), calls assertEditable on branch day.
- ExpenseService.findByBranchDayId: validates branch day exists (404 if not), no capability gate (all authenticated users can list).
- ExpenseRoutes: registered POST /api/expenses, DELETE /api/expenses/{expenseId}, GET /api/expenses?branchDayId= with UUID parsing and ExpenseCategory enum parsing.
- Wired ExpenseRoutes in Main.kt.
- Added ExpenseDto.kt (CreateExpenseRequest, DeleteExpenseRequest, ExpenseResponse) to shared module.
- Added 13 integration tests covering: create with all fields, create without notes, idempotent duplicate, forbidden without capability, non-positive amount (400), non-existent branch day (404), audit log writing, soft delete succeeds, soft delete non-existent (404), soft delete without capability (403), soft delete without reason (400), soft delete audit log, list expenses returns all, list non-existent branch day (404).
- Files changed: shared/.../dto/ExpenseDto.kt (new), repository/model/Expense.kt (new), repository/ExpenseRepository.kt (new), service/ExpenseService.kt (new), api/routes/ExpenseRoutes.kt (new), Main.kt (wired routes), test/.../ExpenseServicePostgresTest.kt (new), scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 245 tests).
- **Learnings for future iterations:**
  - ExpenseCategory is a PG enum with values PANTRY, COMMUNICATION, WATER, TRANSPORTATION, ELECTRICITY, RENTAL, OFFICE_SUPPLIES, FURNITURE_FIXTURES, MISCELLANEOUS — define as Kotlin enum in the model file with customEnumeration pattern matching other PG enums.
  - The expense table has a CHECK(amount > 0) constraint — validate in service layer for cleaner error before DB hit.
  - Soft-delete pattern: set deleted_by and deleted_at columns, don't actually remove the row.
  - ktlint requires each enum entry on its own line — use multiline format for enums with many entries.
  - FindByBranchDayId uses `ExpenseTable.selectAll().where { ExpenseTable.branchDayId eq branchDayId }` — no soft-delete filtering, returns all rows.
  - The POST endpoint validates ExpenseCategory from the request string via `ExpenseCategory.valueOf(request.category.uppercase())` in the route layer.
---

## Tue Jul 14 2026 - US-026
Thread: unavailable
- Implemented allowance assign endpoint (POST /api/allowances) and list endpoint (GET /api/allowances?branchDayId=).
- Created Allowance data class + AllowanceTable Exposed model for the existing allowance table (id, branch_day_id, user_id, amount, assigned_by, assigned_at).
- AllowanceRepository.create: idempotent insert with PK pre-check (returns existing if same UUID), writes audit log on actual insert.
- AllowanceRepository.findByBranchDayId: lists all allowances for a given branch day.
- AllowanceService.create: gates on ASSIGN_COMPENSATION capability (GLOBAL context), validates non-negative amount, validates branch day exists (404 if not).
- AllowanceService.findByBranchDayId: gates on ASSIGN_COMPENSATION capability, returns allowances for branch day.
- AllowanceRoutes: registered POST /api/allowances and GET /api/allowances?branchDayId= with UUID/BigDecimal parsing and response mapping.
- Wired AllowanceRoutes in Main.kt.
- Added AllowanceDto.kt (CreateAllowanceRequest, AllowanceResponse) to shared module.
- Added 9 integration tests covering: create succeeds, idempotent duplicate, forbidden without capability (403), non-existent branch day (404), negative amount (400), findByBranchDayId returns allowances, findByBranchDayId empty list, findByBranchDayId without capability (403), audit log writing.
- Files changed: shared/.../dto/AllowanceDto.kt (new), repository/model/Allowance.kt (new), repository/AllowanceRepository.kt (new), service/AllowanceService.kt (new), api/routes/AllowanceRoutes.kt (new), test/.../AllowanceServicePostgresTest.kt (new), Main.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL, 254 tests).
- **Learnings for future iterations:**
  - The allowance table has no unique constraint on (branch_day_id, user_id), so multiple allowances can be assigned to the same user on the same branch day — no pre-conflict check needed unlike compensation.
  - Allowance uses ASSIGN_COMPENSATION capability (same gate as compensation/commission inclusion) consistent with the PRD requirement.
  - Allowance table has no note column (unlike compensation) — the DTO and model are simpler.
  - The GET endpoint uses `context.queryParam("branchDayId")` to read query parameters — found via Context method, not a static import.
  - Follow the same pattern as compensation: CreateAllowanceRequest with String fields for UUID/BigDecimal, parsed in the route layer.
---

## Tue Jul 14 2026 - US-027
Thread: unavailable
- Implemented remittance draft create endpoint: POST /api/remittances body: { id, type, branchId, method, dateRangeStart, dateRangeEnd }.
- Created RemittanceType, RemittanceMethod, RemittanceStatus enums + Remittance data class + RemittanceTable Exposed model with customEnumeration for remittance_type, remittance_method, remittance_status PG enums.
- RemittanceRepository.createDraft: idempotent PK pre-check (returns existing if same UUID), insertIgnore with audit log.
- RemittanceService.createDraft: gates on SUBMIT_REMITTANCE capability (GLOBAL context), validates dateRangeEnd >= dateRangeStart (400), validates branch exists (404), sets submitted_date to today (Asia/Manila) and submitted_by to caller.
- RemittanceRoutes: registered POST /api/remittances with UUID/RemittanceType/RemittanceMethod/LocalDate parsing and response mapping.
- Added grantSubmitRemittance to DatabaseTestHelper.
- Wired RemittanceRoutes in Main.kt.
- Added 7 integration tests covering: create SESSION draft, create PRODUCT draft, idempotent duplicate, forbidden without capability (403), non-existent branch (404), reversed date range (400), audit log writing.
- Files changed: shared/.../dto/RemittanceDto.kt (new), repository/model/Remittance.kt (new), repository/RemittanceRepository.kt (new), service/RemittanceService.kt (new), api/routes/RemittanceRoutes.kt (new), test/.../RemittanceServicePostgresTest.kt (new), test/.../DatabaseTestHelper.kt, Main.kt, scripts/ralph/prd.json, scripts/ralph/progress.txt.
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - The remittance table has a `date` column for submitted_date — use Exposed `date()` column type (from exposed-java-time), returns `LocalDate`. This is the same pattern used for branch_day.date.
  - The UNIQUE constraint `(branch_id, type, submitted_date)` is on the remittance table — drafts can still conflict on this if created on the same day for the same branch/type, but the idempotent PK pattern handles retries.
  - The EXCLUDE constraint `no_remittance_overlap` only applies when `status = 'SUBMITTED'` — drafts bypass overlap checks entirely per PRD.
   - SUBMIT_REMITTANCE capability follows the same GLOBAL-context pattern as ASSIGN_COMPENSATION and VOID_SESSION, even though the V2 seed comment says scope: BRANCH.
   - LocalDate parsing in routes: use `LocalDate.parse(request.dateRangeStart)` — it parses ISO-8601 format (yyyy-MM-dd).
   - When using `customEnumeration` with a `.default()` value (like status = DRAFT), the default must match the PG enum type exactly.
---

## Wed Jul 15 2026 - US-051
Thread: unavailable
- Created k6 load test scripts in tests/k6/ covering all critical API paths.
- helpers.js: shared constants (BASE_URL, uuid(), authHeaders()), 16 Trend metrics with labels, and threshold definitions for all endpoint groups.
- full-suite.js: main load test with staged ramp-up (5 VUs), covering: auth (login/register/logout), branches CRUD, clients CRUD, session create/status update/void/unvoid, session practitioner management, product categories, products, inventory card creation/restock/movement, product sales, compensation, expenses, allowances, remittance draft/submit, notifications, and reports (daily/monthly summary).
- concurrency-test.js: tests concurrency guards — session PENDING guard (409), optimistic locking version mismatch (409), duplicate UUID idempotency (200), and idempotent expense creation (200).
- authz-test.js: tests authorization edge cases — invalid token (401), expired/malformed token (401), and insufficient capability (403) for limited users.
- remittance-race-test.js: tests serializable isolation with concurrent remittance submissions — multiple VUs submit the same remittance simultaneously; winner gets 200, losers get 409.
- All scripts validated with k6 v2.1.0 (parsed correctly, thresholds/metrics defined).
- Files changed:
  - NEW: tests/k6/helpers.js
  - NEW: tests/k6/full-suite.js
  - NEW: tests/k6/concurrency-test.js
  - NEW: tests/k6/authz-test.js
  - NEW: tests/k6/remittance-race-test.js
  - MODIFIED: scripts/ralph/prd.json (US-051 passes=true)
  - MODIFIED: scripts/ralph/progress.txt
- **Learnings for future iterations:**
  - k6 scripts use ES module imports (import/export); relative imports from other JS files in the same directory work with `import { ... } from "./file.js"`.
  - k6 setup() runs once per test and passes data to default().
  - Custom Trend metrics must be created at module level (not inside functions) for k6 to register them.
  - Use `http.del()` for DELETE requests (k6 API, not `http.delete`).
  - k6 expects all test files in tests/k6/ per PRD notes.
  - Run with: `TEST_USERNAME=owner TEST_PASSWORD=pass k6 run tests/k6/full-suite.js`
  - For remittance race test, run: `k6 run tests/k6/remittance-race-test.js`
  - For authz test with a limited user: `LIMITED_USERNAME=limited LIMITED_PASSWORD=pass k6 run tests/k6/authz-test.js`
- **Codebase Patterns:**
  - k6 test scripts live in tests/k6/ with .js extension.
  - Shared helpers (auth, UUID, base URL, metrics, thresholds) in helpers.js.
  - Each script file uses its own Trend and Rate metrics, plus group-level tags for filtering.
  - Thresholds are set at p(95) for each metric group and a global error rate < 5%.
  - For concurrent remittance testing, the test creates one remittance draft in setup() and all VUs attempt to submit it simultaneously — only one succeeds, others get 409.
---

## Wed Jul 15 2026 - US-052
Thread: unavailable
- Implemented logout endpoint: POST /api/auth/logout
- Added AuthRoutes.logout(config) — registers protected POST /api/auth/logout, extracts userId from JWT attribute, calls DenyList.deny(userId), returns 200 OK.
- Wired AuthRoutes.logout in Main.kt.
- Added AuthServicePostgresTest with 3 integration tests: logout invalidates token, logout is idempotent, logout invalidates all tokens for user.
- DenyList auto-eviction (24h) was already tested in DenyListTest.
- Files changed:
  - MODIFIED: backend/.../api/routes/AuthRoutes.kt (added logout method)
  - MODIFIED: backend/.../Main.kt (wired AuthRoutes.logout)
  - NEW: backend/.../service/AuthServicePostgresTest.kt
  - MODIFIED: scripts/ralph/prd.json (US-052 passes=true)
  - MODIFIED: scripts/ralph/progress.txt
- Quality gate green: ./gradlew :backend:detekt :backend:ktlintCheck :backend:test (BUILD SUCCESSFUL).
- **Learnings for future iterations:**
  - Logout is a thin route handler — no service layer needed, just DenyList.deny(userId) directly in the route.
  - Protected routes under /api/* have JWT verified by the before("/api/*") middleware before the handler runs — the handler receives userId via context.attribute<String>("userId").
  - Integration tests for logout/token rejection use JwtService.generateToken + JwtService.verifyToken with a real DB user (UserRepository.authorize checks ACTIVE status).
  - DenyList.clear() must be called in setUp/tearDown to avoid cross-test pollution.
  - The route returns 200 OK with no body — no DTO needed.
---

## Codebase Patterns
- Use `sql<number>` template for aggregations
- Always use `IF NOT EXISTS` for migrations
- Export types from actions.ts for UI components
- **Before coding a story, read the relevant doc in `docs/`:**
  - `docs/architecture_implementation_plan.md` — architecture decisions, module map, sequencing
  - `docs/engine_specifications.md` — pseudocode + SQL for commission, delegate, and remittance engines
  - `docs/business_requirements.md` — domain terminology, business rules, constraints
  - `backend/src/main/resources/db/migration/V1__full_schema.sql` — authoritative schema (use this over any ERD)
- Backend module: Kotlin (JVM) + Javalin + Exposed (DSL) + Flyway, package root `com.companyb.companyapp`. Layers: `api/routes`, `service`, `repository` (+ `repository/model` for Exposed `Table` objects), `auth`, `database`, `logging`.
- Quality gate (run per backend change): `./gradlew :backend:detekt :backend:ktlintCheck :backend:test`. ktlint auto-fix: `./gradlew :backend:ktlintFormat`.
- Use `insert` for user_capability rows that always need a new row (no conflict expected); use `insertIgnore` for idempotent inserts with client-generated PKs.
- Follow the `val authorized = CapabilityService.hasCapability(...)` / `if (!authorized) throw ForbiddenResponse(...)` pattern for all capability checks.
- When revoking a delegate/access, update the associated `user_capability.valid_to = now()` to immediately expire the grant using Exposed DSL `update` with `and` conditions.
- detekt `MagicNumber` is strict: extract any literal (column widths, pool sizes, durations) to a `private const val`. Constants are exempt via `ignoreConstantDeclaration`. `ReturnCount` max is 2 — match the existing pattern of `@Suppress("ReturnCount")` used in AuthService.
- detekt requires the `detekt-formatting` plugin (wired in root `build.gradle.kts` `subprojects { dependencies { add("detektPlugins", ...) } }`) because `detekt.yml` enables the `formatting` ruleset.
- Authorization MUST go through `CapabilityService.hasCapability(userId, capabilityCode, contextType, contextId)` which queries the `active_user_capabilities` SQL view. Never check roles directly in business logic. The view already filters INACTIVE users and time windows.
- For Postgres enum columns in raw SQL, bind params as text and cast in SQL (e.g. `?::capability_context_type`, `?::uuid`) via Exposed `transaction { exec(sql, args = listOf(TextColumnType() to value)) { rs -> ... } }`. Avoids Exposed PG-enum operator mismatches. This also applies to writes: Exposed `enumerationByName` binds varchar, so native enum updates need raw SQL casts like `SET status = ?::user_status`.
- Immediate revocation: `DenyList` (in-memory `ConcurrentHashMap<UUID, Instant>`) is checked inside `JwtService.verifyToken` BEFORE the DB `authorize` call. Populated at startup via `DenyList.loadInactiveUsers()` in `Main.kt`. Entries auto-evict after 24h (JWT max expiry).
- DB integration tests connect to a real Postgres instance via `DatabaseTestHelper.ensureDatabase()`. Run tests with `./gradlew :backend:test`.
- Exposed needs `exposed-java-time` dependency for `timestampWithTimeZone`/`CurrentTimestampWithTimeZone`.
- HTTP errors are signalled by throwing Javalin's built-in response exceptions from the service layer (`ForbiddenResponse`=403, `BadRequestResponse`=400, `NotFoundResponse`=404, `UnauthorizedResponse`=401). No custom exception hierarchy.
- Day state is lazy: reuse `BranchDayService.evaluateStatus(status, date, today)` — an OPEN day with date < today(Asia/Manila) is treated as PAST without a DB write. Call `BranchDayService.assertEditable(branchDayId, userId, reason?)` before any operational/financial write.
- For testable time logic, keep pure functions taking explicit `today`/inputs and have the public method fetch `LocalDate.now(zone)` then delegate (see BranchDayServiceTest, DenyListTest).
- Protected (authenticated) routes MUST be registered under `/api/*` — only that path is guarded by the `before("/api/*")` filter which sets `context.attribute("userId", <jwt-subject String>)`. Public routes (auth) have no prefix. Read caller id with `UUID.fromString(context.attribute<String>("userId"))`. Register each route object in `Main.initializeJavalin`.
- Audit logging: call `AuditLogRepository.record(tableName, recordId, action, changedBy, oldValue?, newValue?, reason?)` from inside the repository's `transaction {}` — it nests/joins the outer transaction so the audit row commits atomically with the change. Build JSON values with `AuditLogRepository.jsonField(key, value)`. `old_value`/`new_value` are JSONB bound as text with `?::jsonb`.
- GLOBAL-scoped capability checks use `CapabilityService.GLOBAL_CONTEXT_ID` (nil all-zero UUID) as the contextId with `CapabilityContextType.GLOBAL`.
- Kotlin file naming: detekt `MatchingDeclarationName` requires the file name to match its single top-level declaration (e.g. enum `AuditAction` must live in `AuditAction.kt`, not `AuditLog.kt`).
- Ktor 3.5.x API notes: `retryOnServerErrors` takes Int, `defaultRequest` is installed via `install(DefaultRequest)`, `contentType`/`header` need explicit imports from `io.ktor.client.request`.
- New desktop JVM target requires actual declarations for all expect declarations (getPlatform(), createTokenStore(), defaultBaseUrl).
- `shared` module must include all iOS simulator targets that composeApp targets to resolve KMP dependency resolution.
- Android EncryptedSharedPreferences needs try-catch fallback (regular SharedPreferences) as MasterKey may fail on some devices.
- `sendWithoutRequest { !it.url.toString().contains("/auth/") }` skips bearer token on auth endpoints in Ktor 3.x.
- Compose Desktop entry point: `fun main() = application { Window(...) { App() } }` with `WindowState` for size/position.
- For KMP apps targeting desktop: `compose.desktop {}` block in build.gradle.kts + `desktopMain` source set + `compose.desktop.currentOs` dependency are required.
