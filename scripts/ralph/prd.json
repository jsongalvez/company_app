{
  "project": "CompanyApp",
  "branchName": "ralph/company-app-full-build",
  "description": "Full build of the internal physical therapy business app — Kotlin backend (Javalin + PostgreSQL) through Android, iOS, and Windows Desktop clients (Kotlin Multiplatform + Compose Multiplatform).",
  "technologyStack": {
    "backend": "Kotlin (JVM), Javalin web framework, HikariCP, Exposed ORM (DSL), kotlin-logging, Logback, dotenv-kotlin",
    "database": "PostgreSQL 15+ (required for partial predicate on EXCLUDE constraint), Flyway migrations",
    "infrastructure": "Docker, Docker Compose, Oracle Cloud Free Tier ARM VM (4 cores, 24GB RAM), Coolify PaaS, Let's Encrypt automated TLS",
    "mobileAndDesktopUI": "Kotlin Multiplatform (KMP), Compose Multiplatform, Ktor HTTP Client (platform engines: OkHttp for Android, Darwin for iOS, CIO for Desktop), kotlinx.serialization, kotlinx.coroutines",
    "security": "JWT authentication (24h expiry), bcrypt password hashing, in-memory ConcurrentHashMap JWT deny list for deactivations",
    "qualityAndLinting": "Detekt, Ktlint, SonarLint"
  },
  "domainModel": {
    "roles": {
      "SUPERUSER": "Full developer/owner god mode. Access to all operational and system data.",
      "OWNER": "Business owner. Edit access to home branches and currently checked-in branch. View-only access to all branches. Can manage users. Cannot edit PAST/REMITTED financial records.",
      "MANAGER": "Superset of COORDINATOR. Assigns home branches, manages medical mission delegates, can work on clients.",
      "COORDINATOR": "Handles finance/remittance for assigned branches. Sole editor of PAST/REMITTED records for their branch.",
      "PRACTITIONER": "Logs sessions, views clients, manages inventory. Full access to home branches and currently checked-in branch.",
      "ACCOUNTANT": "Read-only access to sales, product sales, and remittances for all branches.",
      "VIEWER": "Read-only access. Reserved for future use."
    },
    "branchTypes": {
      "CLINIC": "Permanent physical clinic location.",
      "PROVINCIAL_TOUR": "Temporary off-site event, treated as a standalone branch.",
      "MEDICAL_MISSION": "Free event branch. Always ₱0 sessions. Delegate-managed."
    },
    "sessionTypes": {
      "REGULAR": "First non-medical-mission visit anywhere, at a CLINIC branch (Default ₱2,500).",
      "PROVINCIAL_FIRST": "First non-medical-mission visit anywhere, at a PROVINCIAL_TOUR branch (Default ₱3,500).",
      "SECOND_SESSION": "Exactly one prior non-medical-mission session globally (Default ₱2,000).",
      "SUBSEQUENT": "Two or more prior non-medical-mission sessions globally (Default ₱1,500).",
      "MEDICAL_MISSION": "Any visit at a medical mission branch. Always free (₱0) and never counts toward history."
    },
    "inventoryMovements": {
      "RESTOCK": "New stock added to branch (quantity_change > 0).",
      "SALE": "Stock decremented due to sale (quantity_change < 0).",
      "TESTER": "Unit set aside for demonstration (quantity_change < 0).",
      "SAMPLE": "Unit given as a free sample (quantity_change < 0).",
      "MISSING": "Unit unaccounted for. Requires mandatory reason/notes (quantity_change < 0).",
      "ADJUSTMENT": "Manual inventory correction (quantity_change of either sign)."
    },
    "inventoryMetrics": {
      "Available": "Derived value: Available = Stock - Sales - Tester - Sample - Missing."
    }
  },
  "projectRules": {
    "capabilitySystem": "All operational permission checks MUST traverse the active_user_capabilities view. Role memberships are only used to seed initial capability assignments in the V2/V3 migrations and are never queried at runtime in the service logic. hasCapability(userId, capabilityCode, contextType, contextId) is checked at the top of every mutating service layer method. Inactive user immediate revocation is managed via a server-side deny list of user IDs in a ConcurrentHashMap, checked by JWT middleware.",
    "dayStateMachine": "Transitions from OPEN to PAST occur lazily at 04:00 AM Asia/Manila of the following day. All operational writes must check the day status via assertEditable. Edits on PAST or REMITTED days require EDIT_PAST_DAY capability. Edits on REMITTED days are flagged, require a non-null reason in the request body, and generate flagged audit entries that must be acknowledged by Owners or Accountants.",
    "concurrencyRules": "Optimistic locking is enforced on session, branch_inventory, and remittance tables using a version column (mismatches return HTTP 409). Row-level lock via SELECT FOR UPDATE on client row is acquired during session creation to prevent concurrent session booking races. For sales, SELECT FOR UPDATE on branch_inventory and stock level validation are executed in a single transaction. Remittance submission runs in SERIALIZABLE isolation and locks the remittance row via SELECT FOR UPDATE.",
    "idempotency": "All mutating INSERT operations use a client-generated UUID as primary key. Upon duplicate key insertion (Unique Violation), the server returns the existing row with HTTP 200/201. Clock-out is an UPDATE query designed to be idempotent (setting the same timestamp is a no-op).",
    "auditLogging": "Audit entries are written in the service layer only for all operational and financial tables. Every INSERT, UPDATE, and soft-DELETE must write an audit log entry. Changes on REMITTED days must be flagged (is_flagged = true) and require a reason.",
    "reliefDuty": "Clocking into a non-home branch automatically assigns view-only access. Edit access requires sending a request to a clocked-in user. Upon grant, a user_capability row is written with valid_to set to the 4 AM Manila boundary of the next day (stored as UTC) with priority 10. Compensation for relief duty is paid from the drawer of the performing branch.",
    "commissionPool": "Product commission is a flat amount added on top of unit price. All commissions collected for the day are pooled and split equally among all practitioners and coordinators clocked in at the sold_at time. Manual inclusions/exclusions can override this. No auto-recalculation on PAST/REMITTED days unless manually triggered.",
    "remittanceFlows": "Two independent flows: SESSION (net = gross session income - total compensation - total expenses) and PRODUCT (quantity * unit price). Draft remittances can overlap and are not constrained by no_remittance_overlap EXCLUDE constraint. Submission writes an immutable financial snapshot (SESSION only) and transitions the covered branch days to REMITTED.",
    "branchSlotOrdering": "Cosmetic slot numbers control practitioner display order in reports and sessions. Slot 1 is senior. Swapping is atomic. Determines display order in reports. Relief practitioners appear after home slots in reports.",
    "allowancesAndExpenses": "Expenses are company costs logged by anyone. Allowances are non-remitted payments (e.g. medical mission transport) paid by Owner, distinct from regular compensation.",
    "clientAnonymization": "Anonymization soft-deletes client and nullifies name, phone, address, conditions, BP. Gender and age are retained for reports.",
    "medicalMissionDelegate": "Managers assigned as medical mission delegates receive EDIT_BRANCH_DATA scoped to the mission branch via user_capability with priority 20 written in a single transaction.",
    "clientSearch": "Fuzzy client name matching via pg_trgm ILIKE plus exact phone prefix matching.",
    "nextAppointmentAlerts": "Daily scheduled task at 07:00 AM Manila queries completed sessions with next appointment in 2 days and writes notifications."
  },
  "engines": {
    "commissionRecalculation": {
      "input": "branch_day_id",
      "steps": [
        "1. Guard: Check if branch_day.status is OPEN. If not, abort unless manually run by Coordinator.",
        "2. Fetch all non-voided product sales for the branch_day.",
        "3. For each sale, find attendance-eligible users whose clock-in/out window covers sold_at.",
        "4. Apply manual inclusions (force add) and exclusions (force remove) from commission_manual_inclusion.",
        "5. If eligible users exist, split (sale.commission_amount_at_time * sale.quantity) equally using BigDecimal division.",
        "6. Accumulate totals per user and batch upsert to commission_split.",
        "7. Delete any users from commission_split for that branch_day who no longer have any eligible commission."
      ]
    },
    "medicalMissionDelegateHook": {
      "assign": [
        "1. Inside a single transaction, insert medical_mission_delegate record (target_user, assigned_by).",
        "2. Retrieve EDIT_BRANCH_DATA capability ID.",
        "3. Insert user_capability: user_id=target_user_id, capability_id=EDIT_BRANCH_DATA, context_type=BRANCH, context_id=branch_id, source_type=MEDICAL_MISSION_DELEGATE, source_id=delegate_id, priority=20, valid_from=now(), valid_to=NULL."
      ],
      "revoke": [
        "1. Inside a single transaction, set ended_at = now() on medical_mission_delegate.",
        "2. Update user_capability: set valid_to = now() where source_type=MEDICAL_MISSION_DELEGATE and source_id=delegate_id."
      ]
    },
    "remittanceSubmission": {
      "input": "remittance_id, expected_version, submitted_by_id",
      "steps": [
        "1. Begin transaction at SERIALIZABLE isolation level.",
        "2. Lock and fetch remittance: SELECT id, status, version FROM remittance WHERE id = :remittance_id FOR UPDATE.",
        "3. Assert status is DRAFT and version is expected_version.",
        "4. Calculate gross_income: SUM(remittance_line.amount) for non-deleted lines of type SESSION.",
        "5. Calculate total_compensation: SUM(compensation.amount) for paying_branch_day_ids in remittance_day_breakdown.",
        "6. Calculate total_expenses: SUM(expense.amount) for branch_day_ids in remittance_day_breakdown where deleted_at IS NULL.",
        "7. Compute net_income = gross_income - total_compensation - total_expenses.",
        "8. For SESSION remittances, insert remittance_financial_snapshot: gross_income, total_compensation, total_expenses, net_income (DB trigger fn_remittance_snapshot_immutable blocks subsequent updates/deletes).",
        "9. Update remittance: status=SUBMITTED, version=version+1, submitted_date=today (Manila date).",
        "10. Transition all covered branch_day rows to status=REMITTED.",
        "11. Write audit log entries for remittance and each branch_day update, then commit."
      ]
    },
    "sessionTypeAlgorithm": {
      "input": "client_id, branch_type",
      "output": "SessionType",
      "logic": [
        "1. If branch_type is MEDICAL_MISSION, return MEDICAL_MISSION.",
        "2. Count non-voided, non-MEDICAL_MISSION sessions for client_id across all branches (using active_session_voids view).",
        "3. If branch_type is PROVINCIAL_TOUR and count is 0, return PROVINCIAL_FIRST.",
        "4. If count is 0, return REGULAR.",
        "5. If count is 1, return SECOND_SESSION.",
        "6. If count is >= 2, return SUBSEQUENT."
      ]
    }
  },
  "userStories": [
    {
      "id": "US-001",
      "title": "CapabilityService.hasCapability + JWT deny list",
      "description": "As the backend, I need a central capability check method and an in-memory deny list so that all auth checks go through one path and inactive users are immediately blocked.",
      "type": "authorization",
      "priorityHint": 1,
      "dependsOn": [],
      "requirements": [
        "Implement CapabilityService.hasCapability(userId, capabilityCode, contextType, contextId): Boolean querying active_user_capabilities",
        "Ensure no direct role checks are executed in business logic",
        "Maintain ConcurrentHashMap<UUID, Instant> deny list populated from app_user WHERE status='INACTIVE' at startup",
        "Evict deny list records older than 24 hours (JWT max expiry)",
        "Check deny list in JWT middleware before making database requests"
      ],
      "acceptanceCriteria": [
        "Capability queries correctly inspect active_user_capabilities view",
        "Inactive users are immediately blocked with HTTP 401 on in-flight requests",
        "Auth check returns false if user is inactive, or has expired time window, or lacks matching row"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Detekt passes",
        "Unit tests for hasCapability and deny list pass"
      ],
      "notes": "active_user_capabilities view is already defined in V1__full_schema.sql. contextType maps to capability_context_type enum.",
      "passes": true
    },
    {
      "id": "US-002",
      "title": "BranchDayService — resolveOrCreate and assertEditable",
      "description": "As the backend, I need a shared service to resolve or create a branch_day row and assert it is editable so that every other module can call it before any write.",
      "type": "attendance",
      "priorityHint": 2,
      "dependsOn": [],
      "requirements": [
        "BranchDayService.resolveOrCreate(branchId: UUID, date: LocalDate): BranchDay executes an UPSERT (ON CONFLICT DO NOTHING) and returns the record",
        "BranchDayService.assertEditable(branchDayId: UUID, userId: UUID) evaluates day state: if date < today (Asia/Manila) and status is OPEN, treat as PAST",
        "Require EDIT_PAST_DAY capability for writes on PAST or REMITTED days (throw 403 Forbidden)",
        "Enforce mandatory non-null reason for writes on REMITTED days (throw 400 Bad Request if missing)",
        "Enforce 4 AM Manila boundary when converting branch calendar date to UTC expiration timestamps"
      ],
      "acceptanceCriteria": [
        "Branch day is lazily evaluated on each request based on current Asia/Manila time",
        "Throws 403 Forbidden on writes to PAST/REMITTED days for unauthorized users",
        "Throws 400 Bad Request on writes to REMITTED days if reason is absent"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Unit tests for resolveOrCreate and assertEditable pass"
      ],
      "notes": "4AM Manila boundary: branchDate.plusDays(1).atTime(4,0).atZone(ZoneId.of('Asia/Manila')). Called by every service before any financial or operational write.",
      "passes": true
    },
    {
      "id": "US-003",
      "title": "User deactivation endpoint",
      "description": "As an Owner or Manager, I want to deactivate a user so that they immediately lose all access.",
      "type": "security",
      "priorityHint": 3,
      "dependsOn": [
        "US-001"
      ],
      "requirements": [
        "Expose PATCH /users/{userId}/deactivate",
        "Require MANAGE_USERS capability with GLOBAL context",
        "Update app_user.status to INACTIVE",
        "Add deactivated user to in-memory deny list immediately",
        "Write audit log entry for the UPDATE (table=app_user, action=UPDATE)"
      ],
      "acceptanceCriteria": [
        "Deactivated user tokens are rejected immediately with HTTP 401",
        "Status is persisted as INACTIVE in app_user",
        "Audit log captures details of deactivation"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for user deactivation pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-004",
      "title": "Branch CRUD endpoints",
      "description": "As an Owner or Manager, I want to create and list branches so that the system knows which locations exist.",
      "type": "api",
      "priorityHint": 4,
      "dependsOn": [
        "US-001"
      ],
      "requirements": [
        "Expose POST /branches body: { id, name, branchType }",
        "Support client-generated UUIDs as PK with idempotency (duplicate ID returns existing row)",
        "Expose GET /branches (all authenticated users) and GET /branches/{branchId}",
        "Require MANAGE_USERS capability for creation"
      ],
      "acceptanceCriteria": [
        "Branches of type CLINIC, PROVINCIAL_TOUR, and MEDICAL_MISSION are successfully managed",
        "Duplicate inserts with same UUID are handled gracefully with HTTP 200/201 and existing record"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for branch CRUD and idempotency pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-005",
      "title": "User branch assignment and slot endpoints",
      "description": "As an Owner or Manager, I want to assign users to home branches and set their slot so that the system knows who belongs where.",
      "type": "api",
      "priorityHint": 5,
      "dependsOn": [
        "US-001",
        "US-004"
      ],
      "requirements": [
        "Expose POST /branches/{branchId}/assignments body: { userId, slot } to insert assignment with ended_at=NULL",
        "Verify uniqueness of active assignments via idx_one_active_assignment unique index",
        "Expose DELETE /branches/{branchId}/assignments/{userId} to set ended_at=now() on active assignment",
        "Expose PATCH /branches/{branchId}/assignments/{userId}/slot body: { slot } (requires user to edit self or hold MANAGE_USERS)",
        "Expose POST /branches/{branchId}/slots/swap body: { userIdA, userIdB } to swap slot numbers atomically in single transaction",
        "Write audit logs for all inserts and updates"
      ],
      "acceptanceCriteria": [
        "Home branch assignments and cosmetic slot order are managed successfully",
        "Swapping slots updates both assignments atomically",
        "Unique constraint prevents concurrent active home assignments for same user and branch"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for slot swapping and assignments pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-006",
      "title": "Clock-in endpoint",
      "description": "As a user, I want to clock into a branch so that my attendance is recorded and I can access that branch's data.",
      "type": "attendance",
      "priorityHint": 6,
      "dependsOn": [
        "US-002",
        "US-005"
      ],
      "requirements": [
        "Expose POST /attendance/clock-in body: { attendanceId, branchId }",
        "Execute resolveOrCreate(branchId, today) and upsert branch_day_assignment in same transaction",
        "Set is_relief=true if no active user_branch_assignment exists for this user and branch",
        "Insert attendance record with clock_in=now() and marked_by=caller",
        "Support idempotency on duplicate attendanceId (returns existing row with HTTP 200)",
        "Enforce single active clock-in via idx_one_active_clock_in unique index",
        "Trigger commission recalculation for the branch day",
        "Write audit log entry"
      ],
      "acceptanceCriteria": [
        "Clocking in at a non-home branch sets is_relief = true",
        "Duplicate clock-ins are handled gracefully without writing extra rows",
        "Attendance records open time windows for commission splits and payouts"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for clock-in and relief computation pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-007",
      "title": "Clock-out endpoint",
      "description": "As a user, I want to clock out so that my attendance window is closed.",
      "type": "attendance",
      "priorityHint": 7,
      "dependsOn": [
        "US-006"
      ],
      "requirements": [
        "Expose POST /attendance/clock-out body: { attendanceId }",
        "Update attendance SET clock_out=now() WHERE id=attendanceId AND clock_out IS NULL",
        "Support idempotency: if clock_out is already set, return existing row with HTTP 200",
        "Trigger commission recalculation for the branch day",
        "Write audit log entry"
      ],
      "acceptanceCriteria": [
        "Clocking out closes the active shift window",
        "Subsequent product sales after clock-out are excluded from the user's commission split"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for clock-out and commission updates pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-008",
      "title": "Relief access request endpoint",
      "description": "As a relief user, I want to request edit access from a checked-in user so that I can make changes at the branch.",
      "type": "attendance",
      "priorityHint": 8,
      "dependsOn": [
        "US-006"
      ],
      "requirements": [
        "Expose POST /relief-access/request body: { requestId, branchDayId, targetUserId }",
        "Validate target user has active clock-in on the branch day (throw 400 if not)",
        "Validate requester has is_relief=true in branch_day_assignment (throw 403 if not)",
        "Persist request as status=PENDING"
      ],
      "acceptanceCriteria": [
        "Relief requests are only accepted if target user is currently clocked in",
        "Rejects requests from non-relief users"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for relief request validations pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-009",
      "title": "Relief access grant/deny endpoint",
      "description": "As a checked-in user, I want to grant or deny a relief access request so that the relief user can or cannot edit.",
      "type": "attendance",
      "priorityHint": 9,
      "dependsOn": [
        "US-008"
      ],
      "requirements": [
        "Expose PATCH /relief-access/{requestId}/grant to set status=GRANTED, granted_by=caller, and granted_at=now()",
        "Under same transaction, insert user_capability: source_type=RELIEF_ACCESS, context_type=BRANCH_DAY, context_id=branch_day.id, valid_to=4 AM Manila next day (in UTC), priority=10",
        "Handle idx_one_grant_per_day unique index: silently return HTTP 200 on duplicate grants for requested_by + branch_day_id",
        "Expose PATCH /relief-access/{requestId}/deny to set status=DENIED"
      ],
      "acceptanceCriteria": [
        "Granting access writes temporary user capability valid until next day 4 AM Manila time",
        "Subsequent attempts to grant after first grant are safely ignored"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for relief grant/deny and capability expiration pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-010",
      "title": "Medical mission delegate assign/revoke",
      "description": "As an Owner or Manager, I want to assign and revoke medical mission delegates so that the right people can manage mission branches.",
      "type": "authorization",
      "priorityHint": 10,
      "dependsOn": [
        "US-001",
        "US-004"
      ],
      "requirements": [
        "Expose POST /delegates body: { delegateId, targetUserId, branchId }",
        "Require ASSIGN_DELEGATE capability on GLOBAL context",
        "Under transaction: insert medical_mission_delegate record + insert user_capability with source_type=MEDICAL_MISSION_DELEGATE, context_type=BRANCH, context_id=branchId, priority=20, valid_to=NULL",
        "Expose DELETE /delegates/{delegateId} to set ended_at = now() on delegate and valid_to = now() on user_capability in single transaction"
      ],
      "acceptanceCriteria": [
        "Assigning delegate automatically registers EDIT_BRANCH_DATA capability for target mission branch",
        "Revoking immediately expires capability and records ended timestamp"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for delegate transactions pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-011",
      "title": "Client create and search endpoints",
      "description": "As a practitioner, I want to create clients and search for them by name or phone so that I can find the right client when starting a session.",
      "type": "client",
      "priorityHint": 11,
      "dependsOn": [
        "US-001"
      ],
      "requirements": [
        "Add Flyway migration to enable pg_trgm extension",
        "Expose POST /clients body: { id, firstName, lastName, middleName, suffix, phoneNumber, address, gender, age, systolicBp, diastolicBp, medicalConditions }",
        "Validate bp_both_or_none database constraint (both systolic and diastolic BP must be set or both null)",
        "Expose GET /clients?q= executing fuzzy matching on full name (via pg_trgm ILIKE) or phone prefix match",
        "Expose GET /clients/{clientId} returning full client details"
      ],
      "acceptanceCriteria": [
        "Clients are successfully persisted and found using fuzzy name queries",
        "Malformed blood pressure inputs are rejected with database constraint validation errors",
        "Duplicate creation with same UUID returns existing record (idempotency)"
      ],
      "verification": [
        "Flyway migration applies successfully",
        "Integration tests for client fuzzy search and constraints pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-012",
      "title": "Client update and anonymize endpoints",
      "description": "As a coordinator, I want to update client info and anonymize a client on request so that we respect privacy.",
      "type": "client",
      "priorityHint": 12,
      "dependsOn": [
        "US-011"
      ],
      "requirements": [
        "Expose PATCH /clients/{clientId} (requires EDIT_BRANCH_DATA) and write audit log entry",
        "Expose POST /clients/{clientId}/anonymize to set deleted_at=now() and nullify all PII (name, phone, address, conditions, blood pressures) while retaining age and gender",
        "Write audit log entry for client updates and anonymization"
      ],
      "acceptanceCriteria": [
        "Anonymized clients have PII redacted but retain age and gender for reporting",
        "Anonymized clients are excluded from normal searches"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for client updates and anonymization pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-013",
      "title": "Session create endpoint",
      "description": "As a practitioner, I want to create a session for a client so that the visit is recorded with the correct type and price.",
      "type": "session",
      "priorityHint": 13,
      "dependsOn": [
        "US-011",
        "US-018"
      ],
      "requirements": [
        "Expose POST /sessions body: { id, clientId, branchId, isWalkIn, requestedPractitionerId, finalPrice, remarks, otherConcerns, bookedAt, nextAppointmentDate }",
        "Acquire client row-level lock: SELECT id FROM client WHERE id = :clientId FOR UPDATE",
        "Validate no active PENDING session exists globally for client via idx_client_one_pending_session unique index; throw 409 Conflict if active",
        "Resolve or create branch day for branch and today",
        "Compute SessionType via Session Type Algorithm (excludes voided and medical missions)",
        "Snapshot base price from current active session_base_rate",
        "Require EDIT_BRANCH_DATA capability. Write audit log entry"
      ],
      "acceptanceCriteria": [
        "Rejects concurrent pending session bookings for the same client with HTTP 409",
        "Session base rate and computed session type are permanently snapshotted at creation"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Unit tests for session type algorithm pass",
        "Integration tests for concurrent session guard pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-014",
      "title": "Session status update endpoint",
      "description": "As a practitioner or coordinator, I want to update session status so that the record is accurate.",
      "type": "session",
      "priorityHint": 14,
      "dependsOn": [
        "US-013"
      ],
      "requirements": [
        "Expose PATCH /sessions/{sessionId}/status body: { status, version }",
        "Check version against session.version for optimistic locking (throw 409 Conflict on mismatch)",
        "Require EDIT_BRANCH_DATA and call assertEditable on the session's branch day",
        "Enforce database and service validations preventing walk-ins from transitioning to NO_SHOW or CANCELLED",
        "Write audit log entry"
      ],
      "acceptanceCriteria": [
        "Status is successfully updated if version matches and day is open",
        "Rejects status modifications on closed days if caller lacks permissions",
        "Throws 409 Conflict on optimistic lock failures"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for status transitions and optimistic locking pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-015",
      "title": "Session void and un-void endpoints",
      "description": "As a coordinator, I want to void a session with a reason and un-void it if done in error so that financial records stay accurate.",
      "type": "session",
      "priorityHint": 15,
      "dependsOn": [
        "US-013"
      ],
      "requirements": [
        "Expose POST /sessions/{sessionId}/void body: { voidId, voidReason }",
        "Require VOID_SESSION capability; insert record into session_void",
        "Expose POST /sessions/{sessionId}/unvoid body: { unvoidedReason }",
        "Require VOID_SESSION capability; update session_void unvoided_at, unvoided_by, unvoided_reason",
        "Ensure all financial queries inspect active_session_voids view (excluding unvoided) to filter voids",
        "Write audit log entries"
      ],
      "acceptanceCriteria": [
        "Voided sessions are successfully excluded from daily sales and financial summaries",
        "Un-voiding restores the session in all calculations"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for session voiding and un-voiding pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-016",
      "title": "Session practitioner management endpoints",
      "description": "As a practitioner, I want to add myself to a session and add remarks so that the record shows who worked on the client.",
      "type": "session",
      "priorityHint": 16,
      "dependsOn": [
        "US-005",
        "US-013"
      ],
      "requirements": [
        "Expose POST /sessions/{sessionId}/practitioners body: { id, practitionerId, remarks }",
        "Snapshot practitioner slot_at_time from current active assignment",
        "Require EDIT_BRANCH_DATA and check optimistic version on session",
        "Expose PATCH /sessions/{sessionId}/practitioners/{practitionerId} body: { remarks }",
        "Expose DELETE /sessions/{sessionId}/practitioners/{practitionerId}"
      ],
      "acceptanceCriteria": [
        "Practitioners are added and ordered by slot in session reports",
        "Session version increments during practitioner modification"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for practitioner slot snapshotting pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-017",
      "title": "Session concerns endpoints",
      "description": "As a practitioner, I want to select concerns and promote free-text ones to structured concerns so that health info is captured.",
      "type": "session",
      "priorityHint": 17,
      "dependsOn": [
        "US-013"
      ],
      "requirements": [
        "Expose GET /concerns listing all concerns (system-seeded are created_by=NULL, promoted are NOT NULL)",
        "Expose POST /sessions/{sessionId}/concerns body: { concernId } and DELETE /sessions/{sessionId}/concerns/{concernId}",
        "Expose POST /sessions/{sessionId}/promote-concern body: { label } to insert a structured concern, link it, and set other_concerns=NULL",
        "Write audit log entries"
      ],
      "acceptanceCriteria": [
        "Promoting structured concern automatically nullifies session other_concerns field",
        "Newly promoted concerns are immediately discoverable"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for concern promotion pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-018",
      "title": "Session base rate management endpoints",
      "description": "As a coordinator, I want to set base rates per session type per branch so that new sessions snapshot the correct price.",
      "type": "session",
      "priorityHint": 18,
      "dependsOn": [
        "US-001",
        "US-004"
      ],
      "requirements": [
        "Expose POST /branches/{branchId}/rates body: { id, sessionType, rate }",
        "Require MANAGE_PRODUCTS capability",
        "Close previous active rate (set effective_until=now()) and insert new rate (effective_from=now()) to prevent overlaps",
        "Expose GET /branches/{branchId}/rates retrieving active rates"
      ],
      "acceptanceCriteria": [
        "No timeline gaps are allowed between closing an old rate and opening a new one",
        "Database exclusion constraint prevents concurrent overlapping rates"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for base rate timeline validations pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-019",
      "title": "Product category and product CRUD",
      "description": "As a coordinator or owner, I want to create and manage product categories and products so that inventory can be tracked.",
      "type": "inventory",
      "priorityHint": 19,
      "dependsOn": [
        "US-001"
      ],
      "requirements": [
        "Expose POST /product-categories body: { id, name } and GET /product-categories",
        "Expose POST /products body: { id, name, productCategoryId, unitPrice, commissionAmount }",
        "Expose PATCH /products/{productId} and GET /products (filters active products)",
        "Require MANAGE_PRODUCTS capability"
      ],
      "acceptanceCriteria": [
        "Products and categories are successfully created and listed",
        "Deactivated products are excluded from active product queries"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for product CRUD pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-020",
      "title": "Branch inventory management and restock",
      "description": "As a coordinator, I want to add products to a branch and restock so that inventory levels are tracked.",
      "type": "inventory",
      "priorityHint": 20,
      "dependsOn": [
        "US-002",
        "US-004",
        "US-019"
      ],
      "requirements": [
        "Expose POST /branches/{branchId}/inventory body: { productId } to insert inventory card with current_stock=0 if absent",
        "Expose POST /branches/{branchId}/inventory/{productId}/restock body: { movementId, quantity, branchDayId }",
        "Require MANAGE_PRODUCTS, call assertEditable on branch day, and apply optimistic locking on version",
        "Expose GET /branches/{branchId}/inventory listing stock counts"
      ],
      "acceptanceCriteria": [
        "Restocking increments inventory stock levels and registers movement logs",
        "Throws HTTP 409 on version mismatches"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for restocking and inventory locking pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-021",
      "title": "Product sale endpoint",
      "description": "As a practitioner, I want to record a product sale so that stock decreases and commission is recalculated.",
      "type": "inventory",
      "priorityHint": 21,
      "dependsOn": [
        "US-013",
        "US-020"
      ],
      "requirements": [
        "Expose POST /product-sales body: { id, branchDayId, sessionId, clientId, isWalkIn, productId, quantity, expectedVersion }",
        "Call assertEditable on the branch day first",
        "Perform under a single database transaction: lock branch inventory via SELECT FOR UPDATE, check stock levels, update stock with version verification, insert inventory movement (reason=SALE, change=-quantity), insert product sale",
        "Enforce sale type logical constraints",
        "Trigger complete commission recalculation (Engine 1) upon successful transaction commit",
        "Write audit log entry"
      ],
      "acceptanceCriteria": [
        "Sale decrements stock level and logs movement atomically",
        "Under-stock requests fail and roll back transaction",
        "Triggers daily commission pool updates"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for product sales transactions and stock validations pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-022",
      "title": "Non-sale inventory movement endpoint",
      "description": "As a practitioner, I want to record tester, sample, missing, and adjustment movements so that stock levels stay accurate.",
      "type": "inventory",
      "priorityHint": 22,
      "dependsOn": [
        "US-020"
      ],
      "requirements": [
        "Expose POST /branches/{branchId}/inventory/{productId}/movement body: { movementId, reason, quantityChange, notes, branchDayId, expectedVersion }",
        "Call assertEditable on the branch day",
        "Verify movement signs at service layer per reason rules; mandate notes for MISSING reason",
        "Update stock level with version verification, insert inventory movement, and write audit log"
      ],
      "acceptanceCriteria": [
        "Signs match requirement rules (e.g. MISSING must be negative)",
        "MISSING reports require non-empty notes fields",
        "Handles version checks on updates"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for inventory movements and checks pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-023",
      "title": "Compensation assign endpoint",
      "description": "As a coordinator or owner, I want to assign daily compensation to each user so that they know what they're owed.",
      "type": "commission",
      "priorityHint": 23,
      "dependsOn": [
        "US-002",
        "US-004"
      ],
      "requirements": [
        "Expose POST /compensation body: { id, workBranchDayId, payingBranchDayId, userId, amount, note }",
        "Require ASSIGN_COMPENSATION capability",
        "Call assertEditable on payingBranchDayId to enforce branch day state machine (OPEN/PAST/REMITTED)",
        "Enforce idx_compensation_unique: only one payout per user per paying branch day drawer",
        "Expose PATCH /compensation/{compensationId} body: { amount, note } (last-write-wins, also calls assertEditable)",
        "Write audit logs for inserts and updates"
      ],
      "acceptanceCriteria": [
        "Relief users can source compensation from the performing branch day (different work and paying days)",
        "Rejects multiple compensation payouts to the same user from one branch on the same calendar date"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for compensation unique constraints pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-024",
      "title": "Commission manual inclusion/exclusion and split read endpoint",
      "description": "As a coordinator, I want to manually include or exclude a user from a product sale's commission and view the daily split so that errors can be corrected.",
      "type": "commission",
      "priorityHint": 24,
      "dependsOn": [
        "US-007",
        "US-021"
      ],
      "requirements": [
        "Expose POST /commission-inclusions body: { id, productSaleId, userId, isIncluded, reason } (upserts over unique key)",
        "Recalculate daily commission split (Engine 1) upon successful commit",
        "Expose POST /commission/recalculate/{branchDayId} requiring EDIT_PAST_DAY capability for manual re-runs on PAST/REMITTED days",
        "Expose GET /commission-splits/{branchDayId} returning daily splits"
      ],
      "acceptanceCriteria": [
        "Commission recalculations apply manual inclusions and exclusions correctly",
        "Split payouts use exact high-precision numeric types"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Unit tests for commission recalculation overrides pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-025",
      "title": "Expense CRUD endpoints",
      "description": "As any user, I want to log and soft-delete branch expenses so that remittance calculations are accurate.",
      "type": "api",
      "priorityHint": 25,
      "dependsOn": [
        "US-002",
        "US-004"
      ],
      "requirements": [
        "Expose POST /expenses body: { id, branchDayId, amount, category, notes } (requires EDIT_BRANCH_DATA and assertEditable)",
        "Expose DELETE /expenses/{expenseId} body: { reason } to soft-delete record (set deleted_by, deleted_at)",
        "Expose GET /expenses?branchDayId= filtering soft-deleted lines",
        "Write audit logs"
      ],
      "acceptanceCriteria": [
        "Expenses are soft-deleted and properly removed from remittance calculations"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for expense CRUD and soft-deletion pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-026",
      "title": "Allowance assign endpoint",
      "description": "As an owner or coordinator, I want to assign transport allowances for medical mission users so that they are reimbursed.",
      "type": "api",
      "priorityHint": 26,
      "dependsOn": [
        "US-002",
        "US-004"
      ],
      "requirements": [
        "Expose POST /allowances body: { id, branchDayId, userId, amount } (requires ASSIGN_COMPENSATION)",
        "Expose GET /allowances?branchDayId= to list allocations"
      ],
      "acceptanceCriteria": [
        "Allowances are successfully recorded and treated as non-remitted travel costs"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for allowances pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-027",
      "title": "Remittance draft create endpoint",
      "description": "As a coordinator, I want to create a remittance draft covering a date range so that I can prepare before submitting.",
      "type": "remittance",
      "priorityHint": 27,
      "dependsOn": [
        "US-001",
        "US-004"
      ],
      "requirements": [
        "Expose POST /remittances body: { id, type, branchId, method, dateRangeStart, dateRangeEnd }",
        "Require SUBMIT_REMITTANCE capability",
        "Save record as status=DRAFT. Drafts bypass date range overlap checks"
      ],
      "acceptanceCriteria": [
        "Multiple overlapping remittance drafts can be created and saved"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for draft creation pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-028",
      "title": "Remittance line and day breakdown management",
      "description": "As a coordinator, I want to add and remove sessions or product sales from a remittance draft and link branch days so that the right items are covered.",
      "type": "remittance",
      "priorityHint": 28,
      "dependsOn": [
        "US-013",
        "US-021",
        "US-027"
      ],
      "requirements": [
        "Expose POST /remittances/{remittanceId}/lines body: { id, type, sessionId, productSaleId, amount } (verifies remittance.version on change)",
        "Expose DELETE /remittances/{remittanceId}/lines/{lineId} (sets deleted_at, deleted_by, checks version)",
        "Expose POST /remittances/{remittanceId}/day-breakdowns body: { branchDayId } (enforces unique remittance+branch_day relationship)",
        "Expose GET /remittances/{remittanceId} to load draft line aggregates"
      ],
      "acceptanceCriteria": [
        "Adding/removing lines alters draft financial totals",
        "Line types match constraints: SESSION matches sessionId, PRODUCT matches productSaleId"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for draft line edits and breakdowns pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-029",
      "title": "Remittance submission endpoint",
      "description": "As a coordinator, I want to submit a remittance so that it is finalized, snapshotted, and covered branch days are marked REMITTED.",
      "type": "remittance",
      "priorityHint": 29,
      "dependsOn": [
        "US-023",
        "US-025",
        "US-028"
      ],
      "requirements": [
        "Expose POST /remittances/{remittanceId}/submit body: { expectedVersion }",
        "Require SUBMIT_REMITTANCE capability",
        "Under SERIALIZABLE transaction: SELECT FOR UPDATE on remittance; assert status is DRAFT and version is expectedVersion; calculate gross, total compensation, and total expenses; write immutable financial snapshot (for SESSION remittances); update status to SUBMITTED and increment version; update branch days to status=REMITTED; commit transaction",
        "Write audit logs for all status updates"
      ],
      "acceptanceCriteria": [
        "Submitting remittance freezes the operational totals for covered branch days",
        "Any subsequent updates or deletions to the financial snapshot fail at database level",
        "Transitioned branch days are marked REMITTED"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for serializable submissions and snapshot immutability pass"
      ],
      "notes": "fn_remittance_snapshot_immutable trigger already exists in V1__full_schema.sql.",
      "passes": true
    },
    {
      "id": "US-030",
      "title": "Notification table migration and next appointment alert scheduler",
      "description": "As the backend, I need a notification table and a daily scheduler that alerts coordinators of upcoming appointments.",
      "type": "scheduler",
      "priorityHint": 30,
      "dependsOn": [
        "US-013"
      ],
      "requirements": [
        "Provide Flyway migration creating a notification table",
        "Configure task running daily at 07:00 AM Manila",
        "Find completed, non-voided sessions where next_appointment_date is 2 days in the future",
        "Write alert rows for each active coordinator of the booking branch"
      ],
      "acceptanceCriteria": [
        "Scheduled alerts are written precisely for booking branch coordinators 2 days before next appointments"
      ],
      "verification": [
        "Flyway migration applies successfully",
        "Unit tests for notification scheduler pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-031",
      "title": "Notifications read and mark-read endpoints",
      "description": "As a coordinator, I want to fetch my unread notifications and mark them as read so that I know about upcoming appointments.",
      "type": "notification",
      "priorityHint": 31,
      "dependsOn": [
        "US-030"
      ],
      "requirements": [
        "Expose GET /notifications returning unread notifications for caller",
        "Expose PATCH /notifications/{notificationId}/read to update read_at=now()"
      ],
      "acceptanceCriteria": [
        "Marked alerts are filtered out of subsequent unread listings"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for notification reading pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-032",
      "title": "Daily sales summary view and endpoint",
      "description": "As a coordinator or owner, I want to view the daily sales summary for a branch so that I can see income and expenses at a glance.",
      "type": "reporting",
      "priorityHint": 32,
      "dependsOn": [
        "US-013",
        "US-021",
        "US-023",
        "US-025"
      ],
      "requirements": [
        "Flyway migration to create daily_sales_summary view",
        "Aggregate: gross_income (completed, non-voided sessions), total_compensation, total_expenses, net_income, total_product_sales, total_commission",
        "Expose GET /branches/{branchId}/daily-summary?date= (requires VIEW_BRANCH_DATA)"
      ],
      "acceptanceCriteria": [
        "End-point returns verified daily operational aggregates"
      ],
      "verification": [
        "Flyway migration applies successfully",
        "Integration tests for daily summary aggregates pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-033",
      "title": "Monthly remittance summary view and endpoint",
      "description": "As a coordinator or accountant, I want to view a monthly summary of submitted remittances so that I can see the financial picture over time.",
      "type": "reporting",
      "priorityHint": 33,
      "dependsOn": [
        "US-029"
      ],
      "requirements": [
        "Flyway migration to create monthly_remittance_summary view",
        "Aggregate: dates, snapshots, counts from submitted remittances",
        "Expose GET /branches/{branchId}/monthly-summary?year=&month= (requires VIEW_BRANCH_DATA)"
      ],
      "acceptanceCriteria": [
        "Monthly view aggregates submitted records for the chosen periods"
      ],
      "verification": [
        "Flyway migration applies successfully",
        "Integration tests for monthly summaries pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-034",
      "title": "Export endpoints (PDF and CSV)",
      "description": "As a coordinator, I want to export daily, monthly, all-time, provincial, and medical mission reports as PDF or CSV.",
      "type": "reporting",
      "priorityHint": 34,
      "dependsOn": [
        "US-032",
        "US-033"
      ],
      "requirements": [
        "Expose daily, monthly, all-time, provincial, and medical mission export endpoints",
        "Support format=pdf|csv; set appropriate Content-Type on response",
        "Generate outputs on-demand, without database storage"
      ],
      "acceptanceCriteria": [
        "Reports output structured content matching chosen filters and formats",
        "Rejects invalid exports (e.g. medical-mission from clinic branch) with HTTP 400"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for export formats and structures pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-035",
      "title": "Audit log read and acknowledge endpoints",
      "description": "As any authorized user, I want to view the audit log for a record and acknowledge flagged entries so that accountability is maintained.",
      "type": "reporting",
      "priorityHint": 35,
      "dependsOn": [
        "US-001"
      ],
      "requirements": [
        "Expose GET /api/audit-log?tableName=&recordId= (no capability gate — all authenticated users)",
        "Expose GET /api/audit-log/flagged returning non-acknowledged flagged entries (no capability gate)",
        "Expose PATCH /api/audit-log/{entryId}/acknowledge (requires ASSIGN_COMPENSATION)"
      ],
      "acceptanceCriteria": [
        "Lists audit histories for specific operational lines",
        "Flagged edits on REMITTED days can be acknowledged to clear alerts"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for audit retrieval and acknowledgement pass"
      ],
      "notes": "ASSIGN_COMPENSATION capability is used for acknowledge gating (per the PRD). Read endpoints have no capability gate since audit trail is visible to all roles per design_specification.md.",
      "passes": true
    },
    {
      "id": "US-036",
      "title": "Shared HTTP client and token store (commonMain)",
      "description": "As a developer, I need a shared Ktor HTTP client (3.x) in commonMain that attaches the JWT to every request, handles retry, and delegates token storage to platform-specific secure stores — so that all platforms share one networking layer.",
      "type": "integration",
      "priorityHint": 36,
      "dependsOn": [
        "US-003",
        "US-004",
        "US-006",
        "US-011",
        "US-013",
        "US-020",
        "US-021",
        "US-023",
        "US-025",
        "US-029"
      ],
      "requirements": [
        "Add Ktor 3.x client libraries to gradle/libs.versions.toml and composeApp/build.gradle.kts: ktor-client-core, ktor-client-content-negotiation, ktor-serialization-kotlinx-json, ktor-client-auth, ktor-client-logging, and platform engines (ktor-client-okhttp for Android, ktor-client-darwin for iOS, ktor-client-cio for Desktop/JVM).",
        "Create ApiClient.kt in composeApp/src/commonMain/.../network/ configuring the Ktor HttpClient with plugins: ContentNegotiation (kotlinx.serialization JSON), Auth (bearer token loaded from TokenStore), HttpRequestRetry (3 retries on network/server errors, safe with UUID idempotency keys), HttpResponseValidator (check 401 → call onDeAuth callback).",
        "Create TokenStore.kt expect interface in commonMain with save(token) / getToken(): String? / clear() methods.",
        "Implement actual TokenStore per platform: Android — EncryptedSharedPreferences; iOS — Keychain via NSUserDefaults wrapper (or Security framework); Desktop — file in user home directory (~/.companyapp/token).",
        "Register a top-level onDeAuth callback (or Channel/MutableSharedFlow) that ViewModels observe to navigate to login on 401.",
        "Set BASE_URL for the client (from platform-specific config or hardcoded for now — defaults to http://10.0.2.2:3023 for Android emulator, http://localhost:3023 for everything else)."
      ],
      "acceptanceCriteria": [
        "Ktor client successfully signs requests with active token from secure store",
        "Retries network timeouts safely without duplicating database records (idempotent UUID keys)",
        "401 response triggers de-auth navigation on each platform",
        "KMP compilation succeeds for JVM (Desktop), Android, and iOS targets"
      ],
      "verification": [
        "KMP compilation for JVM, Android, and iOS targets succeeds",
        "Unit tests for client retry and token stores pass"
      ],
      "notes": "Ktor 3.x was chosen over alternatives (OkHttp KMP wrapper, custom expect/actual engines) because it is the only first-party KMP HTTP client with built-in plugins for auth, retry, content negotiation, and response validation — all in commonMain. Platform engines: OkHttp (Android, mature), Darwin (iOS, delegates to NSURLSession), CIO (Desktop/JVM, pure Kotlin). Pin version via ktorVersion in libs.versions.toml. Do NOT add ktor-server dependencies — client only.",
      "passes": true
    },
    {
      "id": "US-037",
      "title": "Shared ViewModels for all features (commonMain)",
      "description": "As a developer, I want shared ViewModels in commonMain for each feature so that business logic is not duplicated across platforms.",
      "type": "refactor",
      "priorityHint": 37,
      "dependsOn": [
        "US-036"
      ],
      "requirements": [
        "Create shared ViewModels exposing StateFlow UI states for each feature area",
        "Maintain zero platform-specific dependencies inside ViewModels"
      ],
      "acceptanceCriteria": [
        "Reactive flows provide stable, cross-platform state updates"
      ],
      "verification": [
        "KMP compilation succeeds",
        "Unit tests for ViewModels pass"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-038",
      "title": "Android app — login screen",
      "description": "As a user, I want to log in on Android so that I can access the app.",
      "type": "authentication",
      "priorityHint": 38,
      "dependsOn": [
        "US-037"
      ],
      "requirements": [
        "Build Compose UI for login and connect to AuthViewModel",
        "Persist JWT securely inside EncryptedSharedPreferences"
      ],
      "acceptanceCriteria": [
        "Secure storage handles tokens and handles authentication redirects on start"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-039",
      "title": "Android app — home screen with branch selector and clock-in",
      "description": "As a user, I want to see my branches and clock into one on Android so that I can start working.",
      "type": "attendance",
      "priorityHint": 39,
      "dependsOn": [
        "US-038"
      ],
      "requirements": [
        "Build branch listing with clock-in/out toggles",
        "Integrate HomeViewModel clock actions with optimistic UI responses"
      ],
      "acceptanceCriteria": [
        "Shows assignments and clock actions clearly, updating states immediately"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-040",
      "title": "Android app — client search screen",
      "description": "As a practitioner, I want to search for clients on Android so that I can find who I'm about to work on.",
      "type": "client",
      "priorityHint": 40,
      "dependsOn": [
        "US-039"
      ],
      "requirements": [
        "Create search list screen connecting to ClientSearchViewModel with 300ms input debounce"
      ],
      "acceptanceCriteria": [
        "Lists matching fuzzy name results and handles exact phone prefixes"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-041",
      "title": "Android app — session create screen",
      "description": "As a practitioner, I want to create a session from the client detail screen on Android.",
      "type": "session",
      "priorityHint": 41,
      "dependsOn": [
        "US-040"
      ],
      "requirements": [
        "Form fields for isWalkIn, price, remarks, checkboxes for concerns, next appointments",
        "Submit new sessions utilizing client-generated UUID keys"
      ],
      "acceptanceCriteria": [
        "Posts creation requests successfully and presents conflict warnings on HTTP 409 inline"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": true
    },
    {
      "id": "US-042",
      "title": "Android app — session detail screen",
      "description": "As a practitioner, I want to view and manage a session on Android.",
      "type": "session",
      "priorityHint": 42,
      "dependsOn": [
        "US-041"
      ],
      "requirements": [
        "Display aggregates of practitioners, concerns, base rate, void status, and action buttons"
      ],
      "acceptanceCriteria": [
        "Handles edits, status updates, and voiding requests based on active capability scopes"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-043",
      "title": "Android app — inventory screen",
      "description": "As a practitioner or coordinator, I want to view and update branch inventory on Android.",
      "type": "inventory",
      "priorityHint": 43,
      "dependsOn": [
        "US-039"
      ],
      "requirements": [
        "Group list of products by category. Highlight low-stock cards. Include restock actions"
      ],
      "acceptanceCriteria": [
        "Out of stock is visually alerted. Restocking logs update counts immediately"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-044",
      "title": "Android app — product sale screen",
      "description": "As a practitioner, I want to record a product sale on Android.",
      "type": "inventory",
      "priorityHint": 44,
      "dependsOn": [
        "US-043"
      ],
      "requirements": [
        "Implement dropdown selectors for products and session references, listing total costs"
      ],
      "acceptanceCriteria": [
        "Saves product sales with instant local UI feedback"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-045",
      "title": "Android app — daily finance screen",
      "description": "As a coordinator, I want to see daily compensation, commission splits, and expenses on Android.",
      "type": "commission",
      "priorityHint": 45,
      "dependsOn": [
        "US-039"
      ],
      "requirements": [
        "Build daily reports UI showing split allocations, cash-handouts, compensations, and expenses"
      ],
      "acceptanceCriteria": [
        "Coordinators can easily track P&L and allocate compensations inline through simple forms"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-046",
      "title": "Android app — remittance screen",
      "description": "As a coordinator, I want to create and submit a remittance on Android.",
      "type": "remittance",
      "priorityHint": 46,
      "dependsOn": [
        "US-045"
      ],
      "requirements": [
        "Compile drafts with date ranges and methods. Manage line inclusions with checkboxes. Enforce version confirmations"
      ],
      "acceptanceCriteria": [
        "Remittances can be drafted, customized, and finalized with clear confirmation dialogs"
      ],
      "verification": [
        "Android build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-047",
      "title": "iOS app — login and home screens",
      "description": "As a user, I want to log in and see my branches on iOS using shared Compose Multiplatform code.",
      "type": "integration",
      "priorityHint": 47,
      "dependsOn": [
        "US-037"
      ],
      "requirements": [
        "Configure actual token store with Keychain wrappers. Launch Compose UI on iOS Darwin target"
      ],
      "acceptanceCriteria": [
        "Renders login and home list without crashes on simulators"
      ],
      "verification": [
        "iOS simulator build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-048",
      "title": "iOS app — all remaining core screens",
      "description": "As a user, I want all remaining screens to work on iOS via shared Compose code.",
      "type": "integration",
      "priorityHint": 48,
      "dependsOn": [
        "US-041",
        "US-042",
        "US-043",
        "US-044",
        "US-045",
        "US-046",
        "US-047"
      ],
      "requirements": [
        "Compile remaining shared composables onto Darwin platforms and map routes"
      ],
      "acceptanceCriteria": [
        "All client, session, stock, and finance views run smoothly without memory errors on iOS"
      ],
      "verification": [
        "iOS simulator build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-049",
      "title": "Desktop app — Windows target setup and token storage",
      "description": "As a developer, I want the composeApp to target Windows Desktop so that practitioners on laptops can use the app.",
      "type": "integration",
      "priorityHint": 49,
      "dependsOn": [
        "US-037"
      ],
      "requirements": [
        "Configure compose.desktop JVM compilation tasks. Write token stores saving to home directory files"
      ],
      "acceptanceCriteria": [
        "Launches stable Compose window on desktop OS without environment failures"
      ],
      "verification": [
        "Gradle desktop build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-050",
      "title": "Desktop app — all core screens",
      "description": "As a practitioner on Windows, I want to use the same screens as Android and iOS.",
      "type": "integration",
      "priorityHint": 50,
      "dependsOn": [
        "US-041",
        "US-042",
        "US-043",
        "US-044",
        "US-045",
        "US-046",
        "US-049"
      ],
      "requirements": [
        "Adapt shared screens for wide desktop ratios using responsive panels (side-by-side split layouts)"
      ],
      "acceptanceCriteria": [
        "Practitioners can seamlessly navigate split master-detail views on Windows laptops"
      ],
      "verification": [
        "Gradle desktop build succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "",
      "passes": false
    },
    {
      "id": "US-051",
      "title": "k6 load testing for all API endpoints",
      "description": "As a developer, I want k6 load test scripts covering all critical API paths so that I can validate performance and concurrency behavior before go-live.",
      "type": "testing",
      "priorityHint": 51,
      "dependsOn": [
        "US-029",
        "US-032",
        "US-033",
        "US-034",
        "US-035"
      ],
      "requirements": [
        "Write k6 test scripts covering authentication (login), client CRUD, session lifecycle (create/update/void), session practitioner add/remove, clock-in/out, product sale, inventory restock, compensation, expense, remittance draft/submit, notification fetch, and daily/monthly report endpoints",
        "Test concurrency guards: session booking races (HTTP 409 on idx_client_one_pending_session), optimistic locking version mismatches (HTTP 409 on versioned endpoints), duplicate UUID idempotency responses",
        "Test authorization: inactive user deny list rejections (HTTP 401), insufficient capability (HTTP 403), EDIT_PAST_DAY gating on PAST/REMITTED days",
        "Test serializable isolation with concurrent remittance submissions",
        "Generate k6 summary output with percentile latencies and failure rates"
      ],
      "acceptanceCriteria": [
        "All critical API paths have k6 scripts that execute against a running backend",
        "Concurrency edge cases (409 conflicts, 403 rejections, 401 denials) produce correct HTTP status codes under load",
        "Remittance submission handles concurrent serializable conflicts without data corruption",
        "k6 summary shows p95/p99 latencies under acceptable thresholds"
      ],
      "verification": [
        "All k6 scripts run successfully with k6 v2.x",
        "Load test output confirms idempotency, locking, and auth behaviors hold under concurrent load"
      ],
      "notes": "Install k6 binary from https://grafana.com/docs/k6/latest/set-up/install-k6/. Run against local Docker Compose backend. k6 scripts live in tests/k6/.",
      "passes": true
    },
    {
      "id": "US-052",
      "title": "Logout endpoint",
      "description": "As any authenticated user, I want to log out so that my session token is immediately invalidated on all devices.",
      "type": "authentication",
      "priorityHint": 52,
      "dependsOn": [
        "US-001"
      ],
      "requirements": [
        "Expose POST /api/auth/logout (authenticated, under /api/*, no capability gate)",
        "Add the caller's userId to DenyList (in-memory ConcurrentHashMap, auto-evicts after 24h JWT max expiry)",
        "All subsequent requests with any token for this user are rejected with HTTP 401",
        "Idempotent: calling logout twice returns HTTP 200 both times",
        "No database writes required"
      ],
      "acceptanceCriteria": [
        "Logging out immediately invalidates the user's token — subsequent API requests return 401",
        "DenyList entry auto-expires after 24h matching JWT max lifetime"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Integration tests for logout and token rejection pass"
      ],
      "notes": "Reuses DenyList.deny(userId) built in US-001. Since deny list is keyed by userId (not jti), logout invalidates ALL tokens for the user across all devices — acceptable for a single-session business app. Same auto-eviction behavior as user deactivation (24h expiry).",
      "passes": true
    },
    {
      "id": "US-053",
      "title": "Desktop app — SLF4J provider for logging",
      "description": "As a developer, I want the desktop app to have an SLF4J provider so that Ktor and other library logs are captured instead of showing 'No SLF4J providers were found' warnings.",
      "type": "integration",
      "priorityHint": 53,
      "dependsOn": [
        "US-036"
      ],
      "requirements": [
        "Add an SLF4J implementation to the desktop dependency set (e.g., logback-classic or slf4j-simple) so SLF4J warnings are resolved and logs are emitted to stdout or a file.",
        "Ensure the SLF4J provider version aligns with the slf4j-api version transitively pulled by ktor-client-cio or other Ktor modules."
      ],
      "acceptanceCriteria": [
        "Desktop app launches without 'No SLF4J providers were found' warning",
        "Ktor client HTTP logs and other library logs appear on stdout"
      ],
      "verification": [
        "Desktop build succeeds",
        "SLF4J warning no longer appears at launch"
      ],
      "notes": "The backend already uses logback-classic. For the desktop client, either logback-classic or the lighter slf4j-simple is acceptable. slf4j-simple (org.slf4j:slf4j-simple) requires zero config and logs to stderr by default.",
      "passes": true
    },
    {
      "id": "US-054",
      "title": "expect/actual Log functions for composeApp (all KMP targets)",
      "description": "As a developer, I need zero-dependency logging functions that work on all KMP targets (JVM/Desktop, Android, iOS) so that every layer of the composeApp can emit traceable log output for debugging.",
      "type": "logging",
      "priorityHint": 54,
      "dependsOn": [
        "US-053"
      ],
      "requirements": [
        "Create composeApp/src/commonMain/kotlin/com/companyb/companyapp/util/Log.kt with expect top-level functions: logDebug(tag: String, message: String), logInfo(tag: String, message: String), logWarn(tag: String, message: String), logError(tag: String, message: String, throwable: Throwable? = null).",
        "Create composeApp/src/desktopMain/kotlin/com/companyb/companyapp/util/Log.desktop.kt: actual functions delegating to org.slf4j.LoggerFactory.getLogger(tag) (logback-classic already in desktopMain deps from US-053). Format: [HH:mm:ss.SSS] [LEVEL] [tag] message.",
        "Create composeApp/src/androidMain/kotlin/com/companyb/companyapp/util/Log.android.kt: actual functions delegating to android.util.Log.d/i/w/e(tag, message) with throwable passed as third arg for Log.e.",
        "Create composeApp/src/iosMain/kotlin/com/companyb/companyapp/util/Log.ios.kt: actual functions using println() with a prefix like \"[HH:mm:ss.SSS] [LEVEL] [tag] message\".",
        "Add a private expect fun currentTimestamp(): String to format timestamps consistently (use java.time.Instant on JVM, platform epoch millis on Android/iOS).",
        "Zero new Gradle dependencies — uses only existing libraries (slf4j-api on desktop, android.util.Log, kotlin stdlib println on iOS)."
      ],
      "acceptanceCriteria": [
        "logInfo(\"Test\", \"hello\") compiles and runs on desktop (output via SLF4J/logback), Android (output via Logcat), and iOS (output via println)",
        "Desktop log output includes timestamp, level, and tag",
        "Android log output uses correct android.util.Log priority mapping (debug→DEBUG, info→INFO, warn→WARN, error→ERROR)",
        "KMP compilation succeeds for JVM (Desktop), Android, and iOS targets"
      ],
      "verification": [
        "KMP compilation for JVM, Android, and iOS targets succeeds",
        "Manual smoke test: run desktop app, verify log output on stdout"
      ],
      "notes": "expect fun pattern follows the same convention as expect fun createTokenStore() in TokenStore.kt and expect val platformDefaultBaseUrl in AppConfig.kt. Avoid expect object due to Kotlin/Native limitations. The logback.xml from backend/ can be partially reused — create a minimal logback.xml in composeApp/src/desktopMain/resources/ that logs to stdout at INFO level by default. No need for kotlin-logging (io.github.oshai) in composeApp — that library is JVM-only and the backend already uses it.",
      "passes": true
    },
    {
      "id": "US-055",
      "title": "Install Ktor Logging plugin + add logging to ApiClient",
      "description": "As a developer, I want Ktor HTTP request/response logging enabled and ApiClient operations traced so that network issues (401 loops, retries, connection failures) are visible in logs.",
      "type": "logging",
      "priorityHint": 55,
      "dependsOn": [
        "US-036",
        "US-054"
      ],
      "requirements": [
        "In ApiClient.kt commonMain, install(Logging) plugin on the HttpClient with LogLevel.HEADERS (or BODY) — the ktor-client-logging library is already in commonMain deps.",
        "Add logInfo/logError calls at key ApiClient points: (a) client initialization/config loaded, (b) 401 response detected before onUnauthorized emission, (c) retry attempt with attempt number and URL, (d) token loaded/not-found from TokenStore at request time.",
        "Ensure Logging plugin respects platform-specific log output: on desktop it flows through SLF4J/logback, on Android through Logcat, on iOS through println."
      ],
      "acceptanceCriteria": [
        "HTTP request method, URL, and status code appear in logs for every API call",
        "401 responses are logged with the request URL that triggered them",
        "Retry attempts are logged with attempt count and target URL",
        "Desktop app shows HTTP request/response logs on stdout",
        "KMP compilation succeeds for all targets"
      ],
      "verification": [
        "KMP compilation for JVM, Android, and iOS targets succeeds",
        "Run desktop app, perform login — verify request/response headers logged"
      ],
      "notes": "Ktor Logging plugin supports LogLevel.NONE, INFO, HEADERS, BODY, ALL. Use HEADERS (or a configurable level) to avoid logging sensitive JWT token bodies while still seeing request paths and response codes. The plugin is already in commonMain as implementation(libs.ktor.client.logging). The onUnauthorized SharedFlow in ApiClient.kt is currently never collected — logging here is critical to detect 401-triggered de-auth cycles.",
      "passes": true
    },
    {
      "id": "US-056",
      "title": "Add logging to App.kt auth state machine, AuthViewModel, and TokenStore",
      "description": "As a developer, I need the auth state machine fully traced with logs so that I can diagnose the logout screen flash bug and any future auth state transitions that cause unexpected UI behavior.",
      "type": "logging",
      "priorityHint": 56,
      "dependsOn": [
        "US-054",
        "US-037"
      ],
      "requirements": [
        "App.kt: Add logInfo calls at every auth state transition: (a) initial auth check result (hasToken=true/false), (b) LaunchedEffect(logoutState) detecting Success and setting isLoggedIn=false, (c) onLogout callback executing client-side cleanup, (d) onLoginSuccess callback setting isLoggedIn=true. Log the current isLoggedIn value before and after each transition.",
        "AuthViewModel.kt: Add logInfo at (a) login attempt start with username (masked), (b) login success with userId from response, (c) login failure with status code, (d) logout attempt start, (e) logout success/failure, (f) register attempt start. Add logError on exceptions with stacktrace.",
        "TokenStore.desktop.kt: Add logInfo at (a) saveToken (log token length, not content), (b) getToken (log found/not-found), (c) clearToken. Same for TokenStore.android.kt and TokenStore.ios.kt.",
        "Use AuthViewModel's own tag (\"AuthVM\"), App.kt's own tag (\"App\"), and \"TokenStore\" tag for respective TokenStore implementations."
      ],
      "acceptanceCriteria": [
        "Every auth state change in App.kt produces a log entry showing old→new isLoggedIn value",
        "Every login/logout attempt in AuthVM produces start+result log pair",
        "Token save/load/clear operations are logged with result (found/length/cleared)",
        "KMP compilation succeeds for all targets",
        "Running the desktop app and performing login→logout produces a traceable sequence of log messages from TokenStore→AuthVM→App.kt→LoginScreen/HomeScreen"
      ],
      "verification": [
        "KMP compilation for JVM, Android, and iOS targets succeeds",
        "Manual smoke test on desktop: login, then logout — verify log output shows complete auth chain without gaps",
        "Specifically verify logout produces logs showing: AuthVM logout start → POST /api/auth/logout → AuthVM logout success → App.kt logoutState=Success → clearToken → isLoggedIn=false → LoginScreen rendered"
      ],
      "notes": "This story is the critical piece for diagnosing the logout screen flash bug (US-060 will add screen-level logging to complete the picture). The stale loginState=Success bug in AuthViewModel (loginState is never reset after login success) will be immediately visible in logs when LoginScreen re-renders and sees the stale Success state — log messages will show LoginScreen calling onLoginSuccess despite no fresh login request. Do NOT fix the bug in this story — just add logging. Fix comes after diagnosis.",
      "passes": true
    },
    {
      "id": "US-057",
      "title": "Add logging to all remaining ViewModels (Branch, Attendance, Client, Session, etc.)",
      "description": "As a developer, I want all ViewModels to emit trace logs for state transitions and API call outcomes so that bugs in any feature area can be diagnosed from log output.",
      "type": "logging",
      "priorityHint": 57,
      "dependsOn": [
        "US-054",
        "US-037"
      ],
      "requirements": [
        "For every ViewModel in composeApp/src/commonMain/.../viewmodel/: add logInfo at (a) method entry (e.g., \"loadBranches called\"), (b) API call start with endpoint path, (c) API call success/failure with status or error message, (d) StateFlow value transition (Idle→Loading→Success/Error).",
        "Use a consistent tag per ViewModel: \"BranchVM\", \"AttendanceVM\", \"ClientVM\", \"SessionVM\", etc.",
        "Add logError on every catch block with exception details.",
        "ViewModels to cover: BranchViewModel, AttendanceViewModel, and any other ViewModels present in the viewmodel/ package."
      ],
      "acceptanceCriteria": [
        "Each ViewModel method that makes an API call produces start+result log pair",
        "State transitions (Idle→Loading, Loading→Success, Loading→Error) are logged",
        "Exceptions are logged with full stacktrace via logError",
        "KMP compilation succeeds for all targets"
      ],
      "verification": [
        "KMP compilation for JVM, Android, and iOS targets succeeds"
      ],
      "notes": "Follow the pattern established in US-056 for AuthViewModel. If new ViewModels are added later, they should follow the same logging convention documented in AGENTS.md. The log tag naming convention: \"[Feature]VM\" (e.g., \"BranchVM\", \"AttendanceVM\").",
      "passes": true
    },
    {
      "id": "US-058",
      "title": "Add logging to HomeScreen and LoginScreen composables",
      "description": "As a developer, I need screen-level lifecycle logging so that UI navigation events (screen enter/exit, callback invocations) are visible in the log stream alongside ViewModel and network logs.",
      "type": "logging",
      "priorityHint": 58,
      "dependsOn": [
        "US-054",
        "US-056"
      ],
      "requirements": [
        "LoginScreen.kt: Add logInfo at (a) composable entry (first composition), (b) LaunchedEffect(loginState) detecting UiState.Success — log the userId from LoginResponse to identify whose stale state is triggering, (c) LaunchedEffect(loginState) detecting UiState.Error, (d) onLoginSuccess callback invocation, (e) login button onClick (with masked username).",
        "HomeScreen.kt: Add logInfo at (a) composable entry, (b) LaunchedEffect(logoutState) detecting UiState.Success — log that onLogout callback is about to be called, (c) logout button onClick, (d) LaunchedEffect(any other states observed from ViewModels).",
        "Use tags \"LoginScreen\" and \"HomeScreen\"."
      ],
      "acceptanceCriteria": [
        "LoginScreen entry + state observation + callback invocations all produce log entries",
        "HomeScreen entry + state observation + callback invocations all produce log entries",
        "The stale loginState=Success trigger (root cause of the flash bug) is identifiable in logs: a line 'LoginScreen: loginState=Success, calling onLoginSuccess (stale?)' appearing immediately after a logout",
        "KMP compilation succeeds for all targets"
      ],
      "verification": [
        "KMP compilation for JVM, Android, and iOS targets succeeds",
        "Manual smoke test on desktop: reproduce logout flash → verify logs show the cycle: App.kt isLoggedIn=false → LoginScreen entered → LoginScreen sees loginState=Success → onLoginSuccess → App.kt isLoggedIn=true → HomeScreen entered → HomeScreen sees logoutState=Success → onLogout → App.kt isLoggedIn=false → (repeat)"
      ],
      "notes": "Once US-056 and US-058 are both implemented, the logout flash bug's full cycle will be visible in logs. The fix (resetting loginState to Idle after logout) should be a separate PRD or ad-hoc fix after diagnosis. The key log message that identifies the bug: 'LoginScreen: loginState=Success, triggering onLoginSuccess' appearing after 'App: isLoggedIn=false, showing LoginScreen' without any preceding 'AuthVM: login called'.",
      "passes": true
    },
    {
      "id": "US-059",
      "title": "Fix URL encoding for multi-word search queries",
      "description": "As a user, I want to search for multi-word names like 'John Smith' or 'de la Cruz' and get results, so that names with spaces and special characters match correctly.",
      "type": "bugfix",
      "priorityHint": 59,
      "dependsOn": [
        "US-040"
      ],
      "requirements": [
        "ClientViewModel.search() must use Ktor's type-safe query parameter API (HttpRequestBuilder.parameter() or URLBuilder.parameters.append()) instead of Kotlin string interpolation 'get(\"/api/clients?q=$query\")'",
        "Multi-word queries with spaces must produce URL-encoded query strings (e.g. /api/clients?q=John%20Smith)",
        "Phone numbers with dashes or parens must still match the phone prefix pattern on the backend",
        "Backend ClientRepository.search() already supports firstName, lastName, and phoneNumber LIKE matching — no backend changes needed"
      ],
      "acceptanceCriteria": [
        "Typing 'John Smith' sends GET /api/clients?q=John%20Smith (URL-encoded)",
        "Typing 'de la Cruz' returns results matching across first and last names",
        "Phone-only searches with punctuation (0917-555-1234) still produce correct results via the phone prefix LIKE pattern",
        "Single-word queries still work identically to before"
      ],
      "verification": [
        "KMP compilation for Desktop + Android succeeds",
        "Manual smoke test: search 'John Smith' on desktop, verify results appear"
      ],
      "notes": "Root cause: Kotlin string interpolation in get(\"/api/clients?q=$query\") does not URL-encode the query value. Ktor 3.x HttpRequestBuilder provides parameter(key, value) and URLBuilder.parameters.append() for this. ApiClient already sets defaultRequest { url(baseUrl) } so relative paths work with the builder.",
      "passes": true
    },
    {
      "id": "US-060",
      "title": "Keep previous search results visible during loading",
      "description": "As a user, I want to see my previous results while a new search loads, so that the UI doesn't flash blank between keystrokes when I refine my query.",
      "type": "enhancement",
      "priorityHint": 60,
      "dependsOn": [
        "US-040"
      ],
      "requirements": [
        "When a new search fires (after 300ms debounce), the previous search results must remain on screen — do not replace the content area with a full-screen CircularProgressIndicator",
        "Add a subtle loading cue (small spinner or 'Searching...' text) near the search bar, not replacing the results area",
        "Results update atomically when the new response arrives — no intermediate blank/empty state",
        "If the user clears the search bar, results clear and idle state reappears"
      ],
      "acceptanceCriteria": [
        "Typing 'Sm' then 'Smi' then 'Smith' keeps the previous result set visible until the new response arrives",
        "No full-screen spinner flash between keystrokes",
        "A small inline loading indicator appears near the search bar during network requests",
        "After clearing the search bar, idle state shows and results are cleared"
      ],
      "verification": [
        "KMP compilation for Desktop + Android succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "This is the single biggest UX win for search feel. The current pattern replaces the entire content area with a full-screen spinner on every UiState.Loading transition, which disorients the user. Instead, keep the results area stable and show a subtle cue that work is happening. The LaunchedEffect(query) with delay(300) pattern stays unchanged.",
      "passes": true
    },
    {
      "id": "US-061",
      "title": "Add result count and search term header above results",
      "description": "As a user, I want to see how many results matched and what I searched for, so that I know my query was processed correctly.",
      "type": "enhancement",
      "priorityHint": 61,
      "dependsOn": [
        "US-060"
      ],
      "requirements": [
        "When results arrive, show 'N results for <query>' header above the results list",
        "When zero results, show 'No results for <query>' with the actual query string",
        "Header updates immediately when new search results arrive",
        "Error state includes the query that failed"
      ],
      "acceptanceCriteria": [
        "Searching 'Smith' shows '3 results for Smith' above 3 result cards",
        "Searching 'xyzzy' shows 'No results for xyzzy' with suggestion to try different spelling",
        "Searching a term with spaces shows the full query in the header",
        "Error state shows 'Search failed for <query>' or similar contextual message"
      ],
      "verification": [
        "KMP compilation for Desktop + Android succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "The current empty state says just 'No clients found' with no context about what was searched. Adding the query to every state (success, empty, error) gives the user confidence their input was received and processed.",
      "passes": true
    },
    {
      "id": "US-062",
      "title": "Improve idle and empty state messaging",
      "description": "As a user, I want meaningful prompts at every stage so that I know what the system expects and what happened.",
      "type": "enhancement",
      "priorityHint": 62,
      "dependsOn": [
        "US-060"
      ],
      "requirements": [
        "Idle state (no query entered) shows 'Start typing to search by name or phone' instead of the passive 'Type a name or phone number to search'",
        "Empty results state shows 'No results for <query>. Try a different spelling.' with the actual query",
        "After the user clears the search bar, results clear and idle state reappears",
        "Error state shows the query that failed so user doesn't need to retype"
      ],
      "acceptanceCriteria": [
        "Idle prompt is action-oriented and tells the user what to do",
        "Empty state includes the query and a suggestion",
        "Clearing the search bar restores idle state with results cleared",
        "Error state shows contextual message with the failed query"
      ],
      "verification": [
        "KMP compilation for Desktop + Android succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "The current idle message 'Type a name or phone number to search' is passive. 'Start typing to search' is more direct. The empty 'No clients found' tells the user nothing about what they typed.",
      "passes": true
    },
    {
      "id": "US-063",
      "title": "Add inline loading indicator near search bar",
      "description": "As a user, I want a loading cue that doesn't interrupt my results view, so that I know the search is processing without losing visual context.",
      "type": "enhancement",
      "priorityHint": 63,
      "dependsOn": [
        "US-060"
      ],
      "requirements": [
        "During a search request, show a small CircularProgressIndicator (18dp) near the search bar — NOT a full-screen replacement",
        "Search bar trailing icon or adjacent text shows 'Searching...' while request is in flight",
        "Indicator disappears when results, empty, or error state arrives",
        "Search bar remains enabled and focused during loading (already fixed — cursor loss bug resolved)"
      ],
      "acceptanceCriteria": [
        "A small spinner appears near the search bar during network requests",
        "The results area below does not clear or flash during loading",
        "Spinner disappears when response arrives",
        "Search bar stays focused and accepts input throughout"
      ],
      "verification": [
        "KMP compilation for Desktop + Android succeeds",
        "Verify in browser using dev-browser skill"
      ],
      "notes": "This is the companion to US-060 — US-060 keeps results visible, this adds a loading cue so the user knows work is happening. Together they replace the full-screen spinner pattern with a stable-results + subtle-indicator pattern.",
      "passes": true
    },
    {
      "id": "US-064",
      "title": "Add per-run file logging to backend",
      "description": "As a developer, I want each backend run to produce its own log file on disk so that an agent or I can read the complete bounded log of a single run without sifting through other runs.",
      "type": "logging",
      "priorityHint": 64,
      "dependsOn": [],
      "requirements": [
        "Replace backend's RollingFileAppender with a simple ch.qos.logback.core.FileAppender writing to logs/app.log",
        "Set append=false so each run starts fresh — the file contains only the current run's log",
        "Wire <appender-ref ref=\"FILE\"/> under <root> alongside STDOUT (already done)",
        "Add logs/ to root .gitignore so generated log files are never committed (already done)",
        "Verify the app starts and writes to logs/app.log with content from the current run only"
      ],
      "acceptanceCriteria": [
        "Backend logs/app.log is created on each startup with entries from INFO+ level",
        "A second run replaces logs/app.log entirely (no old-run content mixed in)",
        "logs/ is git-ignored",
        "Kotlin compilation succeeds"
      ],
      "verification": [
        "Kotlin compilation succeeds",
        "Start backend, verify logs/app.log contains only that run's log entries"
      ],
      "notes": "Per-run semantics: each ./gradlew :backend:run overwrites logs/app.log. For agent debugging this is ideal — the agent reads one file and sees a complete bounded run. If history is needed later, the agent can look at git history or CI artifacts.",
      "passes": true
    },
    {
      "id": "US-065",
      "title": "Add per-run file logging to desktop client",
      "description": "As a developer, I want each desktop client run to produce its own log file so that an agent or I can read a complete bounded log of a single client session.",
      "type": "logging",
      "priorityHint": 65,
      "dependsOn": [
        "US-053"
      ],
      "requirements": [
        "In composeApp/src/desktopMain/resources/logback.xml, add a ch.qos.logback.core.FileAppender with file=${companyApp.logDir}/app.log (system property)",
        "In composeApp/build.gradle.kts, add jvmArgs += \"-DcompanyApp.logDir=${rootProject.projectDir}/logs/client\" to the compose.desktop.application block",
        "Set append=false so each run starts fresh",
        "Encoder pattern: %d{HH:mm:ss.SSS} [%-5level] [%logger{0}] %msg%n (same as current stdout pattern, no color)",
        "Wire the FILE appender as a second appender under <root> alongside STDOUT — both console and file output remain active"
      ],
      "acceptanceCriteria": [
        "Desktop client logs/client/app.log is created at project root on launch with log entries at INFO+ level",
        "A second client run replaces app.log entirely (no old-run content)",
        "Console output still works alongside file output",
        "Desktop build succeeds"
      ],
      "verification": [
        "Desktop build succeeds",
        "Start desktop client, verify logs/client/app.log exists with content"
      ],
      "notes": "Uses jvmArgs to pass absolute path as system property, so logs land in project-root logs/client/ regardless of Gradle working directory. Per-run semantics keep every agent debugging session reading a clean, bounded file.",
      "passes": true
    }
  ]
}
